# 리팩토링 2 실행 지시서 — 대시보드 수직 통합 + 작업결과표 + 999점 표시 점수 + PDF 버그 수정 (v1, 2026-07-12)

> **사용법** — 이 리팩토링은 **배치 8개**로 나뉘며, **배치마다 새 대화**에서 실행한다(맥락 오염·할루시네이션 방지).
> 새 대화에 **[공통 프롬프트] 절 전체 + 해당 [배치 N 프롬프트] 절 전체**를 붙여넣거나, 파일을 읽을 수 있는
> 환경(Claude Code 등)이면 *"docs/리팩토링_2.md의 공통 프롬프트를 따르고 배치 N을 실행해"* 라고만 지시한다.
> 한 대화 = 한 배치. 배치가 끝나면(커밋·푸시까지) 대화를 종료하고, 다음 배치는 새 대화에서 시작한다.
>
> 배치 구성(공정·난이도 순 — 앞 배치의 산출물이 뒤 배치의 재료가 된다):
> **배치 1** = PDF 파이프라인 버그 수정 + 기반 타입(순수 로직, 소규모) · **배치 2** = 999점 표시 점수 체계(서버 로직 + 유일한 마이그레이션)
> · **배치 3** = 작업결과표 코어(표 엔진·다운로드, 대규모) · **배치 4** = 작업결과표 상호작용 + 구 표·명단 정리
> · **배치 5** = 대시보드 수직 개편 + 페이즈 0 · **배치 6** = 페이즈 1 수합 개편(업로드 분리 + 수합&매칭 연쇄 실행)
> · **배치 7** = 페이즈 2 루브릭·페이즈 3 생기부 이식 · **배치 8** = SPEC 갱신 + 최종 검증·인수(문서, 소규모).
> 진행 상태는 `docs/리팩토링_2_프로그레스.md`가 단일 기준이다.

---

## [공통 프롬프트] — 모든 배치 대화의 서두에 포함

너는 수행평가 수합·평가·생기부 생성 웹앱(**soohaing_jongchongo** — Next.js App Router + TypeScript strict + Tailwind + Supabase, 배포 Vercel) 저장소에서 `docs/리팩토링_2.md`의 **배치 하나만** 실행하는 개발자다.

### 시작 절차 (반드시 순서대로)

1. CLAUDE.md 세션 프로토콜대로 `docs/SPEC.md` → `docs/DATA_MODEL.md` → `docs/PROGRESS.md` → `docs/DECISIONS.md`를 읽는다.
2. `docs/리팩토링_2_프로그레스.md`를 읽고 체크리스트에서 **앞 배치가 완료됐는지**, 워킹 트리가 깨끗한지(`git status`) 확인한다. 미완이거나 기록과 실제 코드가 어긋나면 작업을 멈추고 사용자에게 보고한다.
3. `docs/리팩토링_2.md`에서 지시받은 배치 절만 정독한다. **다른 배치의 작업은 하지 않는다.**
4. 배치가 수정할 파일은 **수정 전에 반드시 전체를 읽는다**. 이 문서와 실제 코드가 어긋나면 **코드가 기준** — 확인 후 프로그레스 문서에 특이사항으로 기록한다.

### 배경 (요약 — 상세 근거는 각 배치 절)

- **PDF 버그 2건(2026-07-12 탐침 실증)**: ① `parsePdfText`(unpdf/pdf.js)가 입력 `Uint8Array`의 ArrayBuffer를 **detach**시켜(워커로 소유권 이전), 이후 스캔 PDF OCR 경로가 **빈 base64**를 전송 → Google `400 "The document has no pages"`. 어떤 모델을 골라도 실패한다(데이터 자체가 빈 것). ② OpenAI 형식은 PDF 파트를 무조건 throw하도록 하드코딩돼 있으나, 실제 OpenAI Chat Completions는 `file` 콘텐츠 파트로 PDF 입력을 지원한다.
- **UX 전면 개편(사용자 지시, 2026-07-12)**: 프로젝트 대시보드를 **페이즈 0(준비)→1(수합)→2(평가)→3(생기부)이 수직으로 이어지는 단일 작업 화면**으로 재구성하고, 하단에 **[작업결과표]**(8열 엑셀 시트형, 전폭)를 둔다. 기존 /ingest·/evaluate·/results·/students 라우트는 대시보드로 흡수·제거(사용자 확정). 매칭·확인(/submissions), 제출물 상세, 루브릭 편집(/rubric), 프롬프트 프로필(/profile), 생기부 문장 검증(/records)은 세부 화면으로 유지.
- **점수 체계(사용자 지시)**: 현행 루브릭 합산 점수는 폭이 좁아 상대 조정이 불가 → **999점 만점 표시 점수** 도입. 초기 15~25명 채점 완료까지 점수 미확정(표시 유보), 완료 시 800~200 범위에 최소 15점 간격으로 스프레드, 이후 학생은 기존 점수 사이 중간값으로 삽입. 루브릭 원점수·등급 파생(INV-6)은 유지된다.
- 실행 인프라(리팩토링 1 산출물)는 재사용한다: `useSequentialRun` 훅 + `RunTerminal`(1건 단위 실행·일시정지/재개/긴급중단·서킷 브레이커). **서버 상주 잡·폴링·잡 테이블 없음.** DB 변경은 **마이그레이션 0012 하나**(student_scores.display_score)뿐이다.

### 전체 그림 — 목표 화면 구조 (배치 8 완료 시점)

```text
/projects/[id]  (프로젝트 대시보드 — 단일 수직 작업 화면, maxDuration=120)
├─ 헤더(← 프로젝트 목록 · 프로젝트명)
├─ <section id="phase-0"> 페이즈 0 · 준비
│    API 키 상태(없으면 개인 키 등록 인라인) · 기본 AI 모델 선택(+전 페이즈 교체 질문) · 프로젝트 설정 링크
├─ <section id="phase-1"> 페이즈 1 · 수합            ─ [작업결과표 바로보기]
│    실행 컨트롤(OCR 모델 드롭다운·[수합 & 매칭]) → RunTerminal(상시) → 업로드 박스 → 업로드 파일 목록(체크박스·삭제)
│    → 확인 대기 큐 배지 + [매칭·확인 →](/submissions 유지)
├─ <section id="phase-2"> 페이즈 2 · 평가            ─ [작업결과표 바로보기]
│    실행 컨트롤(모델 배지·[채점 실행]·[재계산]) → RunTerminal(상시) → 등급 분포 요약 → [평가 루브릭 편집 →](/rubric 확장)
├─ <section id="phase-3"> 페이즈 3 · 생기부          ─ [작업결과표 바로보기]
│    실행 컨트롤(모델 배지·가성비 추천·[생기부 일괄 생성]) → RunTerminal(상시)
│    → [프롬프트 프로필 →](/profile) · [학생별 상세·문장 검증 →](/records)
└─ <section id="worksheet"> 작업결과표 (전폭 full-bleed, 엑셀 시트형)
     선택☑ | ID | 학번 | 이름 | 업로드 된 제출물 갯수 | 반영 점수 | 등급 | 생성된 생기부 | 교사 관찰 메모창
     정렬·필터·열 감춤·열 너비·행 높이·전체 접기/펼치기·다운로드(xlsx/csv/md, 전체/선택)·행 즉시 갱신
```

### 공유 계약 (배치 간 고정 이름 — 임의 변형 금지. 어긋나면 뒤 배치가 깨진다)

| 계약 | 값 |
| --- | --- |
| 섹션 앵커 id | `phase-0` · `phase-1` · `phase-2` · `phase-3` · `worksheet` |
| 작업결과표 열 키 `WorksheetColumnKey` (이 순서) | `internal_id`, `student_number`, `name`, `submission_count`, `score`, `grade`, `record`, `memo` |
| 워크시트 갱신 이벤트 | `lib/worksheet/refresh.ts`의 `WORKSHEET_REFRESH_EVENT = "worksheet:refresh"` + `emitWorksheetRefresh()` (window CustomEvent) |
| 표시 점수 상수 (`lib/scores/display.ts`) | `DISPLAY_MAX=999` · `SPREAD_TOP=800` · `SPREAD_BOTTOM=200` · `MIN_INITIAL_GAP=15` · `initialConfirmCount(total)` = total<15 → total, 아니면 clamp(round(total×0.25), 15, 25) |
| model_routing 확장 키 (jsonb, 마이그레이션 불필요) | 기존 4키(extract/evaluate/generate/verify) 불변 + 선택 키 `default`(기본 AI 모델)·`rubric`(루브릭 전담). 폴백 순서: `rubric ?? default ?? evaluate` |
| 다운로드 파일명 | `${sanitize(프로젝트명)}-${yymmddHHmm}.xlsx|.csv|.md` (로컬 시간, 예: `Tongsa-2607121544.xlsx`). `formatDownloadStamp()`는 `lib/worksheet/download.ts` |
| 제거 라우트의 처리 | page.tsx를 `redirect("/projects/[id]#앵커")`로 교체(폴더의 actions.ts는 유지 가능) |
| 배치 간 인수인계 | `docs/리팩토링_2_프로그레스.md`의 진행 기록만 사용 |

### 불변 규칙 (위반 금지)

- CLAUDE.md의 INV-1~6. 특히 `stepOne`류 서버 액션의 클라이언트 입력은 **id(또는 storage 경로)뿐** — 내용·루브릭·명단은 서버가 DB에서 조립(INV-2). 등급은 계속 파생 계산만(INV-6): 999점 체계는 **표시 점수**를 바꾸는 것이지 등급 직접 입력이 아니다.
- **비범위**: OpenAI responses API, 스트리밍, 서버 잡 큐, Supabase Realtime(행 갱신은 window 이벤트+refetch), 마이그레이션은 0012 하나만, 루브릭 xlsx 재가져오기, NEIS 연동.
- **불가침(건드리지 말 것)**: `suggestMatchCandidates`(확인 큐 LLM 후보)·`reassignSubmission`(재귀속)·확인 큐 액션(attributeExisting/attributeNew/accept·rejectPendingContent)·`lib/crypto`·`lib/llm/keys.ts`·검증 패스(`parseVerification`)·등급 경계(`GRADE_BOUNDARIES`)·프롬프트 프로필 파이프라인(진입 위치만 옮김, 기능 불변).
- 리팩토링 1(docs/리팩토링.md)의 불가침 목록 중 `OcrModelSelect`·`VISION_MODELS`는 **그 리팩토링의 스코프 제한이었다** — 이번 지시서(배치 1·6)가 이 둘을 개편하는 것이 맞다.
- API 키 평문을 로그 라인·감사 로그·터미널에 넣지 않는다. 사용자에게 보여줄 에러 메시지는 300자 절단.
- 새 의존성 추가 금지(xlsx·SheetJS는 이미 dependencies에 있음 — 클라이언트에서 동적 import로 사용).

### 배치 종료 절차 (모든 배치 공통)

1. `npm test` · `npx tsc --noEmit` · `npx eslint` · `npx next build` **전부 통과** — 통과 없이는 커밋 금지. 신규 테스트 파일은 package.json `test` 스크립트에 **명시적으로 추가**해야 한다(테스트는 `node --conditions=react-server --import tsx --test`로 돌므로 `server-only` 모듈도 import 가능).
2. `docs/리팩토링_2_프로그레스.md` 체크리스트 갱신 + 진행 기록 1건 **추가**(날짜/배치/결과/검증 출력 요약/특이사항 — 누적만, 기존 기록 수정 금지).
3. `docs/DECISIONS.md`에 이 배치의 설계 판단을 "날짜 / 판단 / 이유" 형식으로 한 줄씩 추가.
4. `feat/*` 브랜치에서 커밋(한국어 메시지, 말미 `Co-Authored-By: Claude <noreply@anthropic.com>`) → main ff-merge → `git push origin main`(Vercel 자동 배포).
5. 사용자에게 결과·다음 배치 안내를 보고하고 대화를 종료한다.

---

## [배치 1 프롬프트] — PDF 파이프라인 버그 수정 + 기반 타입

`docs/리팩토링_2.md` 배치 1을 실행하라. 범위: PDF detach 수정, OpenAI PDF file 파트 지원, 비전 모델 판별 순수 함수 신설, ModelRouting 타입 확장. **UI 작업은 하지 않는다.**

### 확정 근거 — detach 탐침 실측 (2026-07-12, `node --conditions=react-server --import tsx`)

| 단계 | 관측 |
| --- | --- |
| parsePdfText 호출 전 | `bytes.length = 328`, base64 길이 440 |
| parsePdfText(최소 1페이지 PDF) | pages=1, 텍스트 0자 → `isLikelyScan=true` (스캔 판정 정상) |
| 호출 후 | **`bytes.length = 0`, `bytes.buffer.byteLength = 0`** (detach), base64 길이 **0** |
| `parsePdfText(bytes.slice())` 사용 시 | 원본 base64 길이 440 그대로 보존 |

즉 `ingestDocuments`(app/projects/[id]/ingest/actions.ts)의 스캔 PDF 경로가 `parsePdfText(bytes)` **후에** `Buffer.from(bytes).toString("base64")`를 하므로 빈 PDF가 프로바이더로 간다 → Google 400 "The document has no pages", 사용자 보고 오류와 정확히 일치. **수정 전략은 사용자 선택(2026-07-12): 라이브러리 경계 방어.**

### 1단계 — parsePdfText 방어 사본 + OCR 빈 데이터 가드

**파일 `lib/parsing/document.ts`** — `parsePdfText`에서 pdf.js에 **사본**을 넘긴다:

```ts
export async function parsePdfText(bytes: Uint8Array): Promise<PdfTextResult> {
  // pdf.js(getDocumentProxy)는 전달받은 ArrayBuffer를 워커로 이전(detach)한다.
  // 호출자가 bytes를 계속 쓸 수 있도록(스캔 PDF OCR의 base64 인코딩 등) 사본을 넘긴다.
  const pdf = await getDocumentProxy(bytes.slice());
  …이하 기존과 동일…
}
```

**파일 `app/projects/[id]/ingest/actions.ts`** — `ocrExtract` 서두에 빈 페이로드 가드(향후 다른 detach·다운로드 실패 사고 시 프로바이더 400 대신 명확한 원인 표시):

```ts
if (!part.dataBase64) {
  throw new Error("원본 파일 데이터가 비어 있습니다 — 업로드된 원본을 다시 확인하세요.");
}
```

**회귀 테스트** — `tests/parsing.test.ts`에 케이스 추가(파일을 열어 기존 스타일에 맞춰 append). 최소 PDF는 아래 헬퍼를 테스트 파일 안에 그대로 넣는다:

```ts
function minimalPdf(): Uint8Array {
  const objs = [
    "1 0 obj\n<< /Type /Catalog /Pages 2 0 R >>\nendobj\n",
    "2 0 obj\n<< /Type /Pages /Kids [3 0 R] /Count 1 >>\nendobj\n",
    "3 0 obj\n<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] >>\nendobj\n",
  ];
  let pdf = "%PDF-1.4\n";
  const offsets: number[] = [];
  for (const o of objs) { offsets.push(pdf.length); pdf += o; }
  const xrefStart = pdf.length;
  pdf += "xref\n0 4\n0000000000 65535 f \n";
  for (const off of offsets) pdf += off.toString().padStart(10, "0") + " 00000 n \n";
  pdf += `trailer\n<< /Size 4 /Root 1 0 R >>\nstartxref\n${xrefStart}\n%%EOF`;
  return new TextEncoder().encode(pdf);
}
```

검증 항목: ① `parsePdfText(bytes)` 후 `bytes.length`가 호출 전과 같다(detach 안 됨) ② 반환 pages=1 ③ `isLikelyScan(text, pages) === true`.

### 2단계 — OpenAI PDF file 파트 지원 (스캔 PDF OCR을 전 프로바이더에서)

OpenAI Chat Completions는 PDF를 `{ type: "file", file: { filename, file_data: "data:application/pdf;base64,…" } }` 파트로 받는다(gpt-4o 계열 등 비전 모델). 현재는 무조건 throw — 이것이 "OPENAI 모델은 전부 PDF 입력 불가" 오류의 원인.

1. **`lib/llm/types.ts`** — `LLMDocumentPart`에 `filename?: string` 추가(선택 필드, 기존 호출부 무영향).
2. **`lib/llm/content.ts`** — `toOpenAIContent`의 document 분기에서 throw 제거, 다음으로 교체. `OpenAIPart` 유니언에 file 파트 추가:

```ts
type OpenAIPart =
  | { type: "text"; text: string }
  | { type: "image_url"; image_url: { url: string } }
  | { type: "file"; file: { filename: string; file_data: string } };
…
// document 분기:
return {
  type: "file",
  file: {
    filename: p.filename ?? "document.pdf",
    file_data: `data:application/pdf;base64,${p.dataBase64}`,
  },
};
```

3. **`lib/llm/adapters/openai.ts`** — 상단 주석의 "PDF 파트는 미지원(에러)" 문구를 "PDF는 file 파트로 매핑(비전 모델)"로 갱신(로직 변경 없음).
4. **`app/projects/[id]/ingest/actions.ts`** — 스캔 PDF의 `ocrExtract` document 파트에 `filename: f.filename` 전달.
5. **`lib/llm/vision-models.ts`** — 상단 주석의 "openai: 이미지만 — PDF 문서 파트 미지원" 문구 갱신(카탈로그 값 자체는 유지 — 동적 드롭다운은 배치 6).
6. **`tests/vision-content.test.ts`** 갱신: openai document 파트가 throw하는 기대가 있으면 **file 파트 매핑 기대로 반전**. filename 기본값("document.pdf")·전달값 케이스 각 1건.

### 3단계 — 비전(OCR) 가능 모델 판별 순수 함수

**신규 `lib/llm/vision-capability.ts`** (server-only 아님 — 클라이언트 select 필터에서도 사용):

```ts
import type { ApiFormat } from "@/lib/supabase/types";

// 키에 저장된 모델 목록(api_keys.models)에서 OCR(이미지·PDF 비전) 가능 모델만 남기는 휴리스틱.
// 보수적 원칙: 확신 없는 모델은 제외하고 [직접 입력] 폴백에 맡긴다.
export function isVisionCapableModel(format: ApiFormat, modelId: string): boolean {
  if (format === "anthropic") return modelId.startsWith("claude-");
  if (format === "google") return modelId.startsWith("gemini-");
  // openai: gpt-4o·gpt-4.x·gpt-4-turbo·gpt-5·chatgpt-4o·o1(=미니 제외)·o3(=미니 제외)·o4 계열이 비전 지원
  return /^(gpt-4o|gpt-4\.|gpt-4-turbo|gpt-5|chatgpt-4o|o1(?!-mini)|o3(?!-mini)|o4)/.test(modelId);
}
```

**신규 테스트 `tests/vision-capability.test.ts`** (package.json test 목록에 추가): true — `claude-sonnet-4-6`, `gemini-2.0-flash`, `gpt-4o`, `gpt-4o-mini`, `gpt-4.1`, `gpt-4-turbo`, `gpt-5.4`, `chatgpt-4o-latest`, `o3`, `o4-mini` / false — `gpt-3.5-turbo`, `o1-mini`, `o3-mini`, `gpt-4`(무접미), `text-embedding-3-small`, (google) `aqa`, (anthropic 형식에 `gpt-4o` 같은 교차 케이스는 판정 기준이 format이므로 anthropic+`claude-` 외 false).

### 4단계 — ModelRouting 타입 확장 + 기존 저장 로직의 키 보존

1. **`lib/llm/types.ts`**:

```ts
export type ModelRouting = Record<RoutingKey, ModelTarget> & {
  default?: ModelTarget; // 페이즈 0의 기본 AI 모델 (배치 5)
  rubric?: ModelTarget;  // 루브릭 전담 모델 (배치 7). 폴백: rubric ?? default ?? evaluate
};
```

2. **`app/projects/[id]/evaluate/actions.ts`의 `updateModelRouting`** — 현재 4키만으로 객체를 **재조립해 통째로 덮어쓴다** → 이대로 두면 배치 5의 `default`가 설정 저장 때마다 소실되는 버그. 기존 model_routing을 먼저 조회해 spread로 보존하도록 수정:

```ts
const routing: ModelRouting = { ...(현재 DB의 model_routing) };
for (const key of ROUTING_KEYS) { …기존 검증… routing[key] = { provider_id, model }; }
```

3. `app/projects/[id]/ingest/actions.ts`의 `saveOcrModel`은 이미 spread(`...routing`)라 안전 — 확인만 한다.

### 배치 1 완료 기준

- `npm test` 전건 통과(신규: parsing detach 1~3건 + vision-capability ≥ 14건 + vision-content 반전), typecheck/lint/build 통과.
- grep 확인: `toOpenAIContent`에 PDF throw 잔존 없음, `getDocumentProxy(bytes)`(사본 아닌 원본 전달) 잔존 없음.
- 인계 기록: 스캔 PDF OCR 실검증(통사 9반 PDF 재업로드)은 브라우저·LLM 과금 필요 → 배포 후 사용자 확인 항목.

---

## [배치 2 프롬프트] — 999점 표시 점수 체계

`docs/리팩토링_2.md` 배치 2를 실행하라. 전제: 배치 1 완료. 범위: 표시 점수 순수 모듈 + 마이그레이션 0012 + 재계산 배치 개편 + override 검증 변경. **작업결과표·대시보드 UI는 하지 않는다**(배치 3~5).

### 설계 (사용자 지시 요약)

- 점수는 **999점 만점 정수 "표시 점수"**. 루브릭 채점(evaluations)·합성 원점수(composite)는 지금처럼 산출하되, 화면·조정에 쓰는 점수는 표시 점수다.
- **초기 확정 인원**: 채점 대상 학생 수(total)의 25%를 15~25명으로 클램프(대상이 15명 미만이면 전원). 이 인원이 채점되기 전에는 **표시 점수를 확정하지 않는다**(작업결과표 반영 점수 = 빈 칸). 원점수는 evaluations에 이미 저장되므로 "메모리에만 기록" 요구는 **student_scores 행을 만들지 않는 것**으로 실현한다(컬럼 nullable 변경 불필요).
- 인원 충족 시 원점수 순위대로 **800→200 등간격 스프레드**(15~25명이면 간격 25~43점 ≥ 15점 보장). 이후 채점되는 학생은 **기존 배정 학생들 사이 중간값**으로 삽입(최상위 진입 = 기존 최고와 999의 중간, 최하위 진입 = 기존 최저와 0의 중간). 정수 여유가 소진되면 **전체 재배치**(현재 원점수 순서로 재스프레드).
- 한 번 배정된 표시 점수는 **재계산에도 유지(sticky)** — 교사가 조정한 감각이 흔들리지 않게. 교사 오버라이드(`students.score_override`)는 이제 **0~999 정수**이며 표시 점수보다 우선한다(사유 필수 — DB check 제약 유지).
- 순위·등급은 `effective(= override ?? display)` 내림차순에서 파생(INV-6 불변, `computeStandings` 재사용).

### 1단계 — DATA_MODEL 갱신 → 마이그레이션 0012

`docs/DATA_MODEL.md` 10절(student_scores)에 `display_score integer` 행 추가 + 의미(999점 표시 점수, sticky, 미확정 국면에는 행 자체가 없음, effective_score = coalesce(override, display)) 기술. `students.score_override` 설명에 "0~999 정수(2026-07-12 점수 체계 전환)" 추가. **문서 갱신 후** 마이그레이션 작성:

**신규 `supabase/migrations/0012_display_score.sql`**:

```sql
-- 999점 표시 점수 체계 (리팩토링 2 배치 2, SPEC 6절 개정 예정)
alter table public.student_scores add column display_score integer;
comment on column public.student_scores.display_score is
  '999점 만점 표시 점수. 초기 확정 인원 채점 후 800~200 스프레드로 배정, 이후 중간 삽입. 재계산에도 유지(sticky).';
-- 점수 스케일이 바뀌므로 구(루브릭 합산) 스케일의 보정값은 초기화한다.
update public.students set score_override = null, override_reason = null where score_override is not null;
```

**DB 적용 관행**: `.env.local`의 `SUPABASE_DB_URL`은 직접 호스트(IPv6)라 로컬 접속 불가 — hostname을 `aws-1-ap-northeast-2.pooler.supabase.com`, port `5432`, username `postgres.<ref>`(ref는 원 호스트 `db.<ref>.supabase.co`에서 추출)로 치환해 `supabase db push --db-url`. 검증 스크립트는 node + `pg`(devDependency) + `ssl:{rejectUnauthorized:false}`, **스크래치패드에 .cjs 파일로 저장 후 실행**(인라인 heredoc 금지 — 호스트명 오염 사고 전례). 적용 후 컬럼 존재를 재조회로 확인한다.

### 2단계 — 순수 모듈 `lib/scores/display.ts` (신규)

```ts
export const DISPLAY_MAX = 999;
export const SPREAD_TOP = 800;
export const SPREAD_BOTTOM = 200;
export const MIN_INITIAL_GAP = 15;

// 초기 확정 인원: 대상 15명 미만이면 전원, 아니면 대상의 25%를 15~25로 클램프.
export function initialConfirmCount(totalTargets: number): number;

// n명을 SPREAD_TOP→SPREAD_BOTTOM 등간격 내림차순 배치(round 정수). n===1 → [500].
// n ≤ 41이면 간격 ≥ MIN_INITIAL_GAP이 자동 보장된다(600/40=15).
export function spreadScores(n: number): number[];

// 위/아래 이웃 표시 점수 사이의 삽입값. above 없음 → round((below+DISPLAY_MAX)/2),
// below 없음 → round(above/2), 둘 다 있으면 중간값(min/max 정규화 후). 정수 여유가 없으면 null.
export function insertBetween(above: number | null, below: number | null): number | null;

export type RawRankedStudent = { studentId: string; raw: number }; // 원점수 내림차순 정렬 입력(동점 허용)
export type AssignResult = {
  displays: Map<string, number>; // 전원 확정 시 rawRanked 전원의 표시 점수. 미확정 국면이면 빈 Map
  confirmed: boolean;            // false = 초기 확정 인원 미달(전부 미확정)
  respread: boolean;             // true = 공간 소진으로 전체 재배치가 일어남
};
export function assignDisplayScores(input: {
  rawRanked: RawRankedStudent[];          // 호출부가 원점수 내림차순 정렬 보장
  existing: ReadonlyMap<string, number>;  // 이전 배정(sticky). rawRanked에 없는 학생은 무시
  totalTargets: number;                   // 채점 대상 학생 총수(미채점 포함)
}): AssignResult;
```

`assignDisplayScores` 의미론(이 순서대로 구현):

1. `existing`에서 `rawRanked`에 없는 studentId 제거(탈락자 정리) → `kept`.
2. `kept`가 비어 있고 `rawRanked.length < initialConfirmCount(totalTargets)` → `{ displays: 빈 Map, confirmed: false, respread: false }`.
3. `kept`가 비어 있고 인원 충족 → **초기 스프레드**: `spreadScores(n)`을 rawRanked 순서대로 배정. **원점수 동점 그룹은 그룹 첫 위치의 값으로 통일**(같은 raw = 같은 display).
4. `kept`가 있으면: 기존 학생은 그대로 유지. 신규 학생을 raw 내림차순으로 하나씩 배치 — anchors = 이미 display를 가진 (raw, display) 집합(기존+이번에 배정된 신규 포함). ① 같은 raw의 anchor가 있으면 그 display 그대로(동점) ② above = raw가 더 큰 anchor들의 display 최솟값, below = raw가 더 작은 anchor들의 display 최댓값 → `insertBetween(above, below)` ③ null(소진)이면 **전량 재배치**: kept를 버리고 rawRanked 전체를 3번 규칙으로 재스프레드, `respread: true`. (재채점으로 기존 anchor의 raw 순서와 display 순서가 어긋난 경우에도 insertBetween의 min/max 정규화로 동작한다.)
5. confirmed=true면 displays는 rawRanked 전원을 포함해야 한다.

**신규 테스트 `tests/display-score.test.ts`** (package.json 추가, ≥ 14건): initialConfirmCount(3→3, 14→14, 15→15, 60→15, 100→25, 400→25) / spreadScores(1→[500], 2→[800,200], 25명 간격 25, 41명 간격 15, 등간격·내림차순) / 미달 시 빈 Map·confirmed=false / 초기 확정 동점 그룹 동일 점수 / 신규 중간 삽입 = 이웃 중간값 / 최상위 진입 = (최고+999)/2 / 최하위 진입 = 최저/2 / 신규 동점 = anchor와 동일 / 이웃 붙음(예: 415·416) → respread=true·순서 보존·전원 배정 / existing의 탈락자 제거 / 같은 입력 2회 → 동일 출력(결정성).

### 3단계 — `recomputeAndSave` 개편 (app/projects/[id]/evaluate/actions.ts)

기존 "삭제 → 재삽입" 구조를 유지하되 표시 점수를 끼워 넣는다:

1. 삭제 **전에** 기존 배정을 조회: `admin.from("student_scores").select("student_id, display_score")` → `existing` Map(값이 null인 행은 제외).
2. 원점수 계산(기존 로직 그대로: perStudent → composite → effective는 아래에서 재정의).
3. `totalTargets` = 채점 대상 학생 수 = subToStudent 값(학생 id)의 고유 개수(제출물이 있는 학생 전원 — 아직 채점 안 된 학생 포함). **override만 있고 제출물 평가가 없는 학생은 totalTargets에 넣지 않는다.**
4. `rawRanked` = rows(평가 있는 학생)를 composite 내림차순 정렬(동률은 studentId 사전순 — 결정성).
5. `assignDisplayScores({ rawRanked, existing, totalTargets })` 호출.
6. **confirmed=false(미확정 국면)**: student_scores를 전부 삭제만 하고 삽입하지 않는다. needs_recalc 해제는 기존대로. 반환값에 미확정 정보를 담는다(아래 7).
7. **confirmed=true**: 각 row에 대해 `display = displays.get(studentId)`, `effective = override ?? display`(override는 이제 999 스케일). override만 있고 평가 없는 학생은 display=null·effective=override로 포함(랭킹 대상 기존 규칙 유지). `computeStandings(effective 배열)`로 rank·grade → insert 컬럼에 `display_score` 추가.
8. 반환 타입을 `{ ranked: number; pendingConfirm: { scored: number; required: number } | null }`로 변경 — 호출부 3곳(`finalizeEvaluation`·`recalculate`·`setScoreOverride`/`clearScoreOverride`)을 갱신: finalize/recalculate는 미확정이면 로그·메시지를 "채점 {scored}/{required}명 — 표시 점수 확정 대기(최소 {required}명 채점 후 확정)"로, 확정이면 기존 "재계산 — N명 순위 산출".

### 4단계 — override 검증·표시 소폭 수정

- `setScoreOverride`: 검증을 **정수 0~999**로 변경(`Number.isInteger(value) && value >= 0 && value <= 999`, 위반 메시지 "보정 점수는 0~999 정수입니다."). 사유 필수·감사 로그·재계산 호출은 불변.
- `evaluateOne` 성공 메시지 `` `${total}점` `` → `` `원점수 ${total}점` ``(확정 표시 점수는 재계산 후 산출됨을 로그에서 구분).
- `components/projects/evaluate/results-table.tsx`(현행 평가 화면 학생 표 — 파일을 열어 구조 확인): 점수 열을 표시 점수 기준으로 — `display_score`(또는 override 시 "999점 스케일 보정 값 + 배지") 우선 표시, composite는 "원점수 n" 부가 표기. 이 표는 배치 7에서 작업결과표로 대체·제거될 예정이므로 **최소 수정**만 한다. run-panel의 "채점 대상 …" 안내문에 한 문장 추가: "표시 점수는 초기 확정 인원(15~25명) 채점 후 999점 만점으로 확정됩니다."
- 페이지가 student_scores를 select하는 곳에 `display_score` 컬럼 추가(`lib/supabase/types.ts`의 student_scores Row 타입에 `display_score: number | null` 추가 — 수동 관리 타입임).

### 배치 2 완료 기준

- 테스트 전건 통과(신규 display-score ≥ 14건), typecheck/lint/build 통과, 0012 원격 적용·재조회 확인.
- 수동 시나리오 점검(코드 리딩으로): 학생 20명 프로젝트에서 5명 채점 → 재계산 → student_scores 0행(미확정 메시지) / 15명 채점 → 800~200 스프레드 / 16번째 채점 → 사이 삽입.
- 인계: 실제 채점 흐름 검증은 LLM 과금 필요 — 배포 후 사용자 확인. 기존에 score_override가 있던 프로젝트는 0012가 초기화했음을 사용자에게 고지할 것.

---

## [배치 3 프롬프트] — 작업결과표 코어 (표 엔진 + 다운로드)

`docs/리팩토링_2.md` 배치 3을 실행하라. 전제: 배치 2 완료. 범위: 작업결과표의 데이터 조립·렌더·정렬·필터·열 감춤·열 너비·행 높이·전체 접기/펼치기·다운로드·레이아웃 저장·갱신 이벤트 수신, 그리고 **현행 대시보드 하단에 임시 장착**. 셀 편집·앵커 스크롤은 배치 4.

### 1단계 — 타입·순수 모듈 (신규 `lib/worksheet/`)

**`lib/worksheet/types.ts`**:

```ts
export const WORKSHEET_COLUMNS = [
  "internal_id", "student_number", "name", "submission_count",
  "score", "grade", "record", "memo",
] as const;
export type WorksheetColumnKey = (typeof WORKSHEET_COLUMNS)[number];
export const COLUMN_LABELS: Record<WorksheetColumnKey, string> = {
  internal_id: "ID", student_number: "학번", name: "이름",
  submission_count: "업로드 된 제출물 갯수", score: "반영 점수", grade: "등급",
  record: "생성된 생기부", memo: "교사 관찰 메모창",
};
export type WorksheetSubmission = { id: string; title: string };
export type WorksheetRow = {
  studentId: string;            // = students.id (제품 부여 고유 번호). 셀에는 앞 8자 + title 속성으로 전문
  studentNumber: string | null;
  name: string;
  submissionCount: number;
  submissions: WorksheetSubmission[]; // title = source_filename ?? submission_key ?? id 앞 8자
  displayScore: number | null;  // override ?? student_scores.display_score ?? null
  hasOverride: boolean;
  overrideReason: string | null;
  grade: number | null;         // student_scores.grade 스냅샷(INV-6 파생 저장값)
  recordContent: string | null; // 현재(is_current) 생기부 본문
  recordVersion: number | null;
  memo: string;                 // students.teacher_memo ?? ""
};
```

**`lib/worksheet/layout.ts`** (순수 — 저장·복원 공용 새니타이즈, `lib/records/layout.ts`의 normalizeLayout을 참조 구현으로):

```ts
export type WorksheetSort = { key: WorksheetColumnKey; dir: "asc" | "desc" } | null;
export type WorksheetLayout = {
  widths: Partial<Record<WorksheetColumnKey, number>>; // px, 60~800 클램프
  hidden: WorksheetColumnKey[];
  sort: WorksheetSort;
  rowHeights: Record<string, number>; // studentId → px, 28~600 클램프
  allCollapsed: boolean;              // 전체 접기(행 1줄 말줄임)
};
export function normalizeWorksheetLayout(raw: unknown, studentIds: string[]): WorksheetLayout; // garbage·구형(results) shape 방어, stale studentId 제거
export function applySort(rows: WorksheetRow[], sort: WorksheetSort): WorksheetRow[];   // null·빈 값은 항상 끝. 숫자 열은 수치 비교, 텍스트는 localeCompare("ko")
export function applyFilters(rows: WorksheetRow[], filters: Partial<Record<WorksheetColumnKey, string[]>>): WorksheetRow[];
// 필터 지원 열: student_number·name·submission_count·grade 만(고유값 체크박스). 값은 문자열화해 비교("" = 빈 값 항목).
```

필터는 **세션 상태만**(레이아웃에 저장하지 않음 — 재방문 시 "행이 왜 없지" 혼란 방지, 문서화). sort·widths·hidden·rowHeights·allCollapsed는 저장 대상.

**`lib/worksheet/refresh.ts`** (클라이언트 유틸):

```ts
export const WORKSHEET_REFRESH_EVENT = "worksheet:refresh";
export function emitWorksheetRefresh(): void; // window.dispatchEvent(new CustomEvent(...)) — SSR 가드(typeof window)
```

**`lib/worksheet/download.ts`**: `formatDownloadStamp(d: Date): string`(yymmddHHmm, 로컬 시간) · `sanitizeFilename(name: string): string`(`[\\/:*?"<>|]` 제거·trim, 빈 값 → "project") · `buildWorksheetAoA(rows: WorksheetRow[]): (string | number)[][]`(헤더 8열 + 행; 제출물 갯수·점수·등급은 숫자, 생기부·메모는 전문) — 순수, 테스트 대상. 실제 파일 생성은 컴포넌트에서: xlsx는 `const XLSX = await import("xlsx")` **동적 import**(SheetJS `aoa_to_sheet`→`writeFile`), csv는 BOM(`"﻿"`) prepend 후 Blob 다운로드, md는 표 문법 문자열(셀 내 개행은 `<br>`, `|`는 `\|` 이스케이프) Blob.

**신규 테스트** `tests/worksheet-layout.test.ts`(normalize garbage 방어·클램프·stale 제거·applySort null-끝·숫자/텍스트·applyFilters 빈 값)·`tests/worksheet-download.test.ts`(stamp 자릿수·sanitize·AoA 헤더/행 수/타입) — package.json 추가.

### 2단계 — 데이터 조립 서버 액션 (신규 `app/projects/[id]/worksheet-actions.ts`, "use server")

```ts
export async function fetchWorksheetRows(projectId: string): Promise<WorksheetRow[]>;
```

`requireProjectOwner` 후 owner 클라이언트로 4쿼리(전부 `.eq("project_id", projectId)`): ① students(id, student_number, name, teacher_memo, score_override, override_reason) ② submissions(id, student_id, source_filename, submission_key) — **student_id NOT NULL만**(귀속분 전부, 상태 무관) ③ student_scores(student_id, display_score, grade) ④ records(student_id, content, version) — **is_current=true만**. 학생 기준 left-join 조립(학생 없으면 빈 배열). 정렬 기본: 학번 asc(null 끝)·이름 asc. `displayScore = score_override ?? display_score ?? null`, `hasOverride = score_override != null`.

레이아웃 저장 액션도 이 파일에(참조 구현: `app/projects/[id]/results/actions.ts`의 saveLayout — 복사하되 normalizeWorksheetLayout 사용):

```ts
export async function saveWorksheetLayout(projectId: string, layout: unknown): Promise<void>; // ui_layouts upsert onConflict(user_id,project_id)
```

### 3단계 — 표 컴포넌트 (신규 `components/projects/worksheet/worksheet-table.tsx`, 파일 분할 자유)

"use client". props: `{ projectId, projectName, initialRows: WorksheetRow[], initialLayout: unknown }`. 내부 상태: rows·layout(normalize 후)·filters·selected(Set<studentId>)·expandedCounts(Set — 배치 4에서 사용, 이번엔 선언만 해도 됨).

- **엑셀 시트 룩**: `border-collapse` 표, 전 셀 얇은 경계선(`border border-zinc-200 dark:border-zinc-800`), 1행 헤더 sticky(`sticky top-0 z-10 bg-zinc-100 dark:bg-zinc-900`), 행 hover 강조, `table-fixed` + colgroup(widths).
- **헤더 셀**: 라벨 + `▾` 버튼 → 드롭다운 메뉴: [오름차순]/[내림차순]/[정렬 해제] · (필터 지원 열이면) 고유값 체크박스 목록(전체 선택/해제) · [이 열 감추기]. 정렬 상태 화살표(↑/↓) 표시. 헤더 우측 경계 드래그로 열 너비 조절(참조 구현: `components/projects/results/results-grid.tsx`의 열 경계 드래그 — 파일을 열어 패턴 복사).
- **툴바(표 상단)**: `[전체 펼치기] [전체 접기]`(allCollapsed 토글 — 접기 시 record·memo 셀 1줄 말줄임, rowHeights 무시) · 감춘 열이 있으면 "감춘 열 N개 [모두 표시]" · `[다운로드 ▾]`(xlsx/csv/md — **정렬 반영·필터 무시·전체 열** 전체 행) · 선택 행이 있으면 `[선택 N행 다운로드 ▾]` · 행 수 표기.
- **행**: 맨 앞 체크박스 열(내보내기 선택용, 헤더 체크박스 = 전체 토글) + 8열. 행 하단 경계 드래그로 개별 행 높이(rowHeights). ID 셀은 studentId 앞 8자 + `title` 속성에 전문. 빈 값 셀은 빈 칸으로 렌더(클릭 동작은 배치 4).
- **레이아웃 저장**: layout 변경 시 700ms 디바운스로 `saveWorksheetLayout`.
- **갱신 수신**: `useEffect`로 `WORKSHEET_REFRESH_EVENT` listen → 800ms 디바운스 → `fetchWorksheetRows` 재호출로 rows 교체. 수동 [새로고침] 버튼도 툴바에.

### 4단계 — 대시보드 하단 임시 장착 (app/projects/[id]/page.tsx)

기존 구조(준비 카드 + 평가 흐름)는 이번 배치에서 **건드리지 않고**, 최하단에 섹션 추가:

```tsx
<section id="worksheet" className="relative left-1/2 w-screen -translate-x-1/2 px-3 mt-12">
  <h2 …>작업결과표</h2>
  <WorksheetTable … />
</section>
```

(페이지 컨테이너가 `max-w-4xl`이므로 위 full-bleed 클래스로 전폭 확보 — 좌우 여백은 px-3만.) 서버에서 `fetchWorksheetRows` 로직과 동일한 조립을 직접 수행하지 말고 **액션을 서버에서 호출하지도 말 것** — page.tsx가 같은 4쿼리를 직접 수행해 initialRows를 만들거나, 조립 함수를 `lib/worksheet/assemble.ts`(server-only 아님·주입식)로 빼 페이지와 액션이 공유한다(중복 조립 로직 금지). ui_layouts select로 initialLayout 전달.

### 배치 3 완료 기준

- 테스트 전건 통과(신규 worksheet 2파일), typecheck/lint/build 통과.
- 수동 확인 목록(프로그레스에 기록): 표가 대시보드 하단에 전폭 렌더, 정렬/필터/감춤/열너비/행높이/전체접기 동작, xlsx·csv·md 다운로드 파일명 `프로젝트명-yymmddHHmm.*`, 선택 다운로드.
- 인계: 셀 클릭 동작(편집·스크롤)은 배치 4. 대시보드 개편(배치 5) 전까지 기존 준비 카드·평가 흐름과 공존한다.

---

## [배치 4 프롬프트] — 작업결과표 상호작용 + 구 표·명단 정리

`docs/리팩토링_2.md` 배치 4를 실행하라. 전제: 배치 3 완료. 범위: 셀 편집·오버라이드·학생 편집 모달·제출물 확장·빈 칸 앵커 스크롤, /results·/students 제거.

### 1단계 — 셀 상호작용 (components/projects/worksheet/)

- **제출물 갯수 셀 클릭**: 행 바로 아래 확장 서브행(colSpan 전체)에 제출물 제목 목록(`submissions[].title`, 디폴트 감춤) 토글. 재클릭으로 접힘. 툴바 [전체 접기]는 확장 서브행도 모두 닫는다.
- **빈 칸 클릭 → 해당 페이즈로 스크롤**: `submissionCount === 0`인 갯수 셀 → `#phase-1` / `displayScore === null`인 점수 셀 → `#phase-2` / `grade === null`인 등급 셀 → `#phase-2` / `recordContent === null`인 생기부 셀 → `#phase-3`. 공통 헬퍼 `scrollToAnchor(id)` = `document.getElementById(id)?.scrollIntoView({ behavior: "smooth" })` — 앵커 요소가 아직 없으면(배치 5 전) 무동작이어도 정상(안전 설계, 프로그레스에 명시).
- **반영 점수 셀(값 있음) 클릭 → 인라인 오버라이드 편집**: 숫자 입력(0~999 정수) + 사유 입력(필수 — DB check 제약) + **[저장] [취소]** 버튼(사용자 요구: 액션 실행/취소 선택권). 저장 = 기존 `setScoreOverride(projectId, studentId, value, reason)` 호출 → 성공 시 `emitWorksheetRefresh()`. `hasOverride`면 현재 사유 표시 + [보정 해제](`clearScoreOverride`) 버튼. 오버라이드된 셀은 배지(예: 앰버 점)로 구분.
- **학번·이름 셀 클릭 → [학생별 데이터 편집] 모달**: 제목 "학생별 데이터 편집". 필드 — ID(studentId, 읽기 전용·복사 가능), 학번, 이름, 교사 관찰 메모, (있으면) 보정 점수·사유 안내(편집은 점수 셀에서). [저장](기존 `updateStudent` FormData 액션 재사용 — projectId·studentId hidden) / [취소] / [학생 삭제](confirm 후 기존 `deleteStudent` — cascade로 제출물·점수·생기부 삭제됨을 경고 문구로 명시). 저장·삭제 후 `emitWorksheetRefresh()`.
- **툴바 [+ 학생 추가]**: 같은 모달의 신규 모드(기존 `addStudent` FormData 액션 재사용). — 학생 명단 페이지의 역할 대체(사용자 지시).
- **생기부 셀**: 클릭 = 펼침(전문·글자수 `countText` 재사용 표시), [편집] 진입 시 textarea + [저장][취소] — 기존 `saveRecordEdit`(app/projects/[id]/records/actions.ts — **파일을 열어 시그니처 확인** 후 재사용, 내용 실변경 시에만 새 'edited' 버전) → `emitWorksheetRefresh()`.
- **메모 셀**: 클릭 = textarea 인라인 편집, blur 저장(기존 `saveTeacherMemo` FormData 액션 재사용) + "저장됨" 표시.
- 학번·이름·메모·생기부·점수(오버라이드) 편집이 곧 "부여된 아이디를 제외한 대부분의 데이터 오버라이드" 요구의 실현이다 — ID는 어디서도 편집 불가.

### 2단계 — /results(구 결과 표) 제거 · 대체

- `app/projects/[id]/results/page.tsx` → 내용 전부 삭제하고 `redirect(\`/projects/${id}#worksheet\`)`(next/navigation, 함수 시그니처는 params 유지). `app/projects/[id]/results/actions.ts` 삭제.
- `components/projects/results/`(results-grid·text-cell) 삭제, `lib/records/layout.ts` 삭제, `tests/layout.test.ts` 삭제 + package.json test 목록에서 제거.
- `app/projects/[id]/records/page.tsx` 헤더의 "결과 표" 링크 → "작업결과표"(`/projects/${id}#worksheet`)로 교체.
- grep 잔존 0 확인: `results-grid`, `text-cell`, `normalizeLayout`, `LayoutState`, `setAllCellModes`(docs 제외).

### 3단계 — /students(학생 명단) 제거 · 대체

- `app/projects/[id]/students/page.tsx` → `redirect(\`/projects/${id}#worksheet\`)`. `components/projects/student-list.tsx` 삭제(`TeacherMemoBox`는 records가 쓰므로 **유지**).
- `app/projects/[id]/students/actions.ts`는 **유지**(작업결과표가 재사용) — 내부 `revalidatePath("/projects/…/students")`들을 `revalidatePath(\`/projects/${projectId}\`)`로 교체.
- 프로젝트 홈 준비 카드에서 "학생 명단" 카드 제거(홈 전면 개편은 배치 5 — 여기서는 카드 한 장만 제거).
- grep 잔존 0 확인: `student-list`, `StudentList`(docs 제외).

### 배치 4 완료 기준

- 테스트·typecheck·lint·build 통과. 삭제 심볼 grep 0건.
- 수동 확인 목록: 갯수 클릭 확장/접힘, 빈 점수·생기부 셀 클릭 시 무동작(앵커 미존재 — 배치 5 후 스크롤), 점수 오버라이드 저장/취소/해제(사유 필수 검증), 학생 편집·추가·삭제, 생기부·메모 인라인 편집 저장.
- 인계: /results·/students 링크가 남아 있을 수 있는 곳(홈 하단 문구 등)은 배치 5의 홈 개편이 정리.

---

## [배치 5 프롬프트] — 대시보드 수직 개편 + 페이즈 0

`docs/리팩토링_2.md` 배치 5를 실행하라. 전제: 배치 4 완료. 범위: 프로젝트 대시보드(app/projects/[id]/page.tsx) 전면 개편 — 페이즈 0~3 수직 섹션 + 페이즈 0(API 키·기본 AI 모델) 완성. **페이즈 1~3 섹션은 이번 배치에서는 골격**(기존 페이지로의 링크 카드)이며 실행 UI 이식은 배치 6·7이 한다.

### 1단계 — 페이지 구조 개편

`app/projects/[id]/page.tsx`를 [공통 프롬프트]의 "전체 그림" 구조로 재작성한다. `export const maxDuration = 120;` 추가(배치 6·7에서 이 라우트의 서버 액션이 OCR·LLM 1건 호출을 수행). 규칙:

- 페이즈 섹션 래퍼는 공통 컴포넌트 `components/projects/dashboard/phase-section.tsx`: `{ id, step, title, desc, children }` — `<section id={id} className="mx-auto w-full max-w-4xl scroll-mt-6 …">` + 번호 원형 배지 + 제목 + 우측 **[작업결과표 바로보기]** 버튼(`scrollToAnchor("worksheet")` — 클라이언트 소자로 분리).
- 섹션 순서: phase-0 → phase-1 → phase-2 → phase-3 → worksheet(배치 3 장착분을 이 구조의 최하단으로 이동). 기존 "준비" 카드 그리드와 "평가 흐름" 목록은 **제거**하고 내용을 다음처럼 흡수: 설정 → 페이즈 0 안 링크 카드 / 루브릭 → 페이즈 2 안 링크 카드(임시) / 프롬프트 프로필 → 페이즈 3 안 링크 카드(임시) / Phase 1·2·3 카드 → 각 섹션의 "이 단계 화면으로 →" 링크 카드(임시 — 배치 6·7에서 실행 UI로 대체).
- 페이즈 1 섹션에는 확인 대기 배지도 표시: pending_confirm·update_pending 제출물 수를 조회해 "확인 대기 N건 [매칭·확인 →]"(/submissions 링크).

### 2단계 — 페이즈 0: API 키 상태 + 등록 인라인

서버에서 `listRoutableProviders(user.id)`(lib/llm/available.ts — 서버 전용) 결과를 페이즈 0에 전달:

- **키 있음**(keySource != null인 프로바이더 ≥ 1): 초록 체크 + "API 키가 등록되어 있습니다({프로바이더명 나열}) — 아래 기본 AI 모델을 확인하고 페이즈 1로 진행하세요." + 프로바이더별 keySource 배지(개인 키/기본 키) + [키 관리 →](/account 링크).
- **키 없음**: 앰버 경고 + "API 키가 없습니다. 아래에서 개인 키를 등록해야 AI 기능(수합 OCR·평가·생기부)을 쓸 수 있습니다." + **`PersonalKeys` 컴포넌트 인라인 재장착**(components/account/personal-keys.tsx — `app/account/page.tsx`를 열어 providers·personalKeys props 조립 방식을 그대로 복사. 키 저장 액션은 account의 것 재사용 — 이 페이지에서 동작하는지 확인).

### 3단계 — 페이즈 0: 기본 AI 모델 선택 + 전 페이즈 교체 질문

**신규 `components/projects/dashboard/default-model-picker.tsx`** ("use client") + 서버 액션(신규 `app/projects/[id]/dashboard-actions.ts`, "use server"):

```ts
export async function saveDefaultModel(projectId: string, providerId: string, model: string):
  Promise<{ differing: { key: "extract" | "evaluate" | "generate" | "verify"; providerName: string; model: string }[] }>;
// requireProjectOwner → 기존 model_routing 조회 → { ...routing, default: { provider_id, model } } 저장(스프레드 보존 — 배치 1의 updateModelRouting 수정과 동일 원칙)
// → 4개 용도 중 default와 (provider_id, model)이 다른 항목 목록을 반환(providers 이름 조인).

export async function applyDefaultToAllRouting(projectId: string): Promise<void>;
// routing.default가 없으면 throw. extract/evaluate/generate/verify 4키 전부 default 값으로 교체 저장(rubric·default 키 보존).
```

UI 흐름(사용자 지시 그대로): 프로바이더 select(키 보유만 — `keySource != null`, models는 저장 목록·비면 직접 입력 폴백) + 모델 select + [기본 모델로 저장] → 저장 성공 후 `differing`이 비어 있지 않으면 **교체 질문 패널** 표시: "다른 페이즈에 개별 선택된 모델이 있습니다: {목록}. 전부 기본 AI 모델로 교체할까요?" — **[전부 교체]**(applyDefaultToAllRouting) / **[개별 설정 유지]**(패널 닫기). 현재 default 값과 4개 용도 요약을 항상 아래에 작게 표기. `revalidatePath(\`/projects/${projectId}\`)`.

- 프로젝트 설정 화면(`ModelRoutingForm`)은 그대로(용도별 세부 편집 창구로 유지) — 페이즈 0 안 "프로젝트 설정" 링크 카드에서 접근.

### 배치 5 완료 기준

- 테스트·typecheck·lint·build 통과.
- 수동 확인 목록: 대시보드가 페이즈 0→3→작업결과표 수직 구조, 각 섹션 [작업결과표 바로보기] 스크롤, 작업결과표 빈 점수/생기부 셀 클릭 → #phase-2/#phase-3 스크롤(배치 4의 핸들러가 이제 동작), 키 없음 계정에서 페이즈 0 인라인 등록, 기본 모델 저장 → 교체 질문 → [전부 교체] 후 설정 화면 4용도 일치.
- 인계: 페이즈 1~3 섹션은 아직 링크 카드(구 페이지 살아 있음). 구 라우트 제거는 배치 6(/ingest)·배치 7(/evaluate)에서 실행 UI 이식과 함께.

---

## [배치 6 프롬프트] — 페이즈 1 수합 개편 (업로드 분리 + 수합&매칭 연쇄 실행 + OCR 드롭다운)

`docs/리팩토링_2.md` 배치 6을 실행하라. 전제: 배치 5 완료. 범위: 업로드-즉시-파싱 제거(토큰 소모 시점 분리), 업로드 파일 목록(체크·삭제), [수합 & 매칭] 단일 실행(수합 후 매칭 자동 연쇄, 공용 터미널), OCR 모델 동적 드롭다운, 수합된 제출물 후보 섹션 제거, /ingest 라우트 제거. **참조 구현: `components/projects/submissions/matching-panel.tsx`와 채점 run-panel.**

### 1단계 — useSequentialRun에 연쇄 실행(nextStage) 확장

**파일 `lib/hooks/use-sequential-run.ts`** — 기존 소비자(채점·매칭 패널) **무변경으로 동작**해야 한다:

```ts
export type RunPlan = {
  prepare: () => Promise<PrepareResult>;
  stepOne: (t: SequentialTarget) => Promise<StepResult>;
  finalize: (r: { succeeded: number; failed: number; aborted: boolean }) => Promise<string | null>;
};
// args에 선택 인자 추가:
//   nextStage?: (r: { succeeded: number; failed: number; aborted: boolean }) => RunPlan | null;
```

구현: 기존 루프 본문을 `runPlan(plan: RunPlan): Promise<{ succeeded; failed; aborted }>` 내부 함수로 추출(로그·progress·pause/stop·서킷 브레이커 동작 그대로 — progress는 plan마다 리셋). start()는 첫 plan 실행 후 `!aborted && nextStage`면 반환 계획을 이어 실행한다: `append("system", "── 다음 단계 ──")` 후 두 번째 plan의 prepare→step×N→finalize. **lines는 초기화하지 않고 이어 붙는다.** 연쇄는 1회만(재귀 없음). stop/pause는 두 단계 모두에 유효(ref 공유).

### 2단계 — 서버 액션 개편 (app/projects/[id]/ingest/actions.ts)

기존 `ingestDocuments`·`ingestSpreadsheet`를 **제거**하고 다음으로 대체(`previewSpreadsheet`·`saveOcrModel`·내부 헬퍼 `persistSubmission`·`ocrExtract` 등은 유지·재사용). 모든 신규 액션은 `requireProjectOwner` + **storage 경로 소유 검증**(경로가 `${userId}/${projectId}/`로 시작하는지 — 아니면 throw)을 한다:

```ts
export type UploadedFile = {
  path: string;          // originals 버킷 경로
  filename: string;      // 경로 마지막 세그먼트에서 "uuid__" 이후 복원
  createdAt: string | null;
  ingested: boolean;     // 이 path를 storage_path로 참조하는 제출물 존재 여부
};
export async function listUploadedFiles(projectId: string): Promise<UploadedFile[]>;
// supabase.storage.from("originals").list(`${userId}/${projectId}`, { limit: 1000, sortBy: { column: "created_at", order: "desc" } })
// + submissions.select("storage_path").eq("project_id", …)로 ingested 판정.

export async function deleteUploadedFile(projectId: string, path: string): Promise<void>;
// ingested면 throw("이미 수합된 파일입니다 — 원본 삭제는 제출물 상세의 추출 확인(승인) 절차를 따르세요.") ← INV-5 우회 금지.
// 미수합 파일만 storage.remove. (업로드 직후 불필요 파일 정리 용도)

export type IngestTarget = { id: string; label: string }; // id = storage path, label = filename
export async function prepareIngest(projectId: string, paths: string[]):
  Promise<{ targets: IngestTarget[]; prelude: { level: "info" | "system"; text: string }[] }>;
// 소유 검증 + 존재 검증. prelude: "수합 대상 N개 파일"(0개면 "수합할 새 파일 없음 — 매칭만 진행합니다").

export async function ingestOneFile(projectId: string, path: string, mapping?: ColumnMapping):
  Promise<{ ok: boolean; message: string }>;
// fileKind(filename)로 분기해 기존 1건 로직을 그대로 수행(다운로드→파싱/OCR→persistSubmission):
//   docx/pdf/image → 구 ingestDocuments의 단일 파일 본문 이동(스캔 PDF는 ocrExtract + filename 전달 — 배치 1).
//   spreadsheet → mapping 필수(없으면 ok:false "열 매핑이 필요합니다"). 구 ingestSpreadsheet의 행 루프 이동.
//     message 예: "12행 → 신규 10 · 중복 2 · 변경 대기 0"
//   문서·이미지 message 예: "신규(pdf_scan)" / "중복 스킵" / "변경 대기".
// throw하지 않고 { ok:false, message: 에러 300자 }. mapping은 교사가 확정한 열 인덱스뿐(내용 주입 아님 — 기존 ingestSpreadsheet과 동일 신뢰 수준).

export async function finalizeIngest(projectId: string, counts: { succeeded: number; failed: number }):
  Promise<string>; // revalidatePath(`/projects/${projectId}`) + "수합 완료 — 성공 N·실패 M" 반환
```

기존 `revalidatePath(".../ingest")` 호출들은 `/projects/${projectId}`로 교체.

### 3단계 — OCR 모델 동적 드롭다운

**`components/projects/ingest/ocr-model-select.tsx` 개편**: props의 `providers`를 `RoutableProvider[]`(lib/llm/available.ts의 반환 — id·name·api_format·models·keySource)로 교체. 모델 후보 = `provider.models.filter(m => isVisionCapableModel(provider.api_format, m))`(배치 1의 순수 함수). 규칙: keySource가 null인 프로바이더는 목록에서 **비활성(회색)** + "키 없음" 표기 / 저장 모델이 비어 있으면(0010 이전 키) `VISION_MODELS[api_format]` 정적 카탈로그로 폴백 + "[모델 갱신] 후 전체 목록이 나옵니다" 안내 / [직접 입력…] 폴백 유지. 저장은 기존 `saveOcrModel` 그대로. 호출부(대시보드 페이즈 1 섹션)는 서버에서 `listRoutableProviders(userId)`를 조회해 전달한다.

### 4단계 — 페이즈 1 섹션 완성 (신규 `components/projects/dashboard/phase1-panel.tsx`)

"use client". 배치 5의 페이즈 1 링크 카드를 이 패널로 교체. **섹션 내 배치 순서(터미널 상단 규칙)**: ① 실행 컨트롤 행 — OCR 모델 드롭다운 + [수합 & 매칭] 버튼 + 진행 배지 ② `RunTerminal` — **상시 렌더**(lines 없어도, idle 시 "로그가 여기에 표시됩니다") ③ 업로드 박스(기존 IngestClient의 드래그앤드롭 로직 이식 — Storage 업로드까지만, 파싱 호출 제거) ④ 업로드 파일 목록 ⑤ 확인 대기 배지 + [매칭·확인 →].

- **업로드 파일 목록**: `listUploadedFiles` 결과. 각 행 = 체크박스 + 파일명 + 업로드 시각 + (ingested면 "수합됨" 배지) + [삭제]. **체크 기본값: 미수합 = checked, 수합됨 = unchecked**(사용자 지시: 업로드된 파일은 디폴트 체크, 해제하면 제외 — 이미 수합된 파일을 재수합하면 중복 감지가 스킵하므로 unchecked가 자연 기본). [삭제]는 미수합만 동작(수합분은 서버가 거부 — 에러 메시지 표시).
- **[수합 & 매칭]** 클릭 흐름: ① 체크된 파일 중 spreadsheet가 있으면 파일마다 `previewSpreadsheet` → 기존 `ColumnMapper` 재사용으로 순차 열 매핑 확정(취소한 시트는 대상에서 제외) ② `useSequentialRun` start — stage1(수합): prepare=`prepareIngest(projectId, checkedPaths)`, stepOne=`ingestOneFile(projectId, path, mappings[path])`, finalize=`finalizeIngest`; `nextStage` → stage2(매칭): prepare=`prepareMatching(projectId)`(기존 — 결정적 열·파일명 처리 + prelude 로그), stepOne=`matchOneByLlm(projectId, id)`(기존), finalize=`finalizeMatching(projectId)`(기존) + "매칭 반영 완료 — 동명이인·식별 불가는 확인 대기 큐에서 지정하세요." ③ **stepOne 래퍼마다 `emitWorksheetRefresh()`** — 1건 끝날 때마다 작업결과표 행 즉시 갱신(사용자 요구) ④ 종료 후 `router.refresh()` + 파일 목록 refetch.
- 체크 파일 0개여도 실행 가능(수합 스킵, 매칭만 — 매칭 재실행 경로).

### 5단계 — 구 화면 정리

- `app/projects/[id]/ingest/page.tsx` → `redirect(\`/projects/${id}#phase-1\`)`. `components/projects/ingest/staged-submissions.tsx` 삭제(수합된 제출물 후보 화면 제거 — 사용자 지시), `components/projects/ingest/ingest-client.tsx` 삭제(phase1-panel로 대체).
- `app/projects/[id]/submissions/page.tsx`에서 `MatchingPanel` 제거(매칭 실행은 대시보드 단일 창구 — 확인 큐·학생별 제출물 상세·재귀속 전담 화면으로 유지). `components/projects/submissions/matching-panel.tsx` 삭제. submissions 페이지의 모델 배지·prepareMatching import 등 잔여 정리.
- grep 잔존 0 확인(docs 제외): `ingestDocuments`, `ingestSpreadsheet`, `StagedSubmissions`, `IngestClient`, `MatchingPanel`.

### 배치 6 완료 기준

- 테스트·typecheck·lint·build 통과. grep 0건. 기존 채점 화면(run-panel)이 훅 확장 후에도 무변경 동작(코드 리딩 + build).
- 수동 확인 목록: 업로드 시 토큰 소모 없음(터미널·네트워크 확인), 파일 목록 체크/해제·삭제(수합분 거부), [수합 & 매칭] 한 번으로 수합→매칭 연쇄(터미널에 "── 다음 단계 ──"), 시트 열 매핑 확정 흐름, 1건마다 작업결과표 갱신, 일시정지/재개/긴급 중단, 중단 후 재실행 시 증분(중복 스킵·미매칭만).
- 인계: PDF(통사 9반) 재업로드 → OCR 성공 확인은 LLM 과금 필요 — 배포 후 사용자 체크리스트로.

---

## [배치 7 프롬프트] — 페이즈 2 루브릭 + 페이즈 3 생기부 이식

`docs/리팩토링_2.md` 배치 7을 실행하라. 전제: 배치 6 완료. 범위: 페이즈 2(채점 실행 이식 + 루브릭 확장) · 페이즈 3(일괄 생성 터미널 + 프로필 배치 + 가성비 추천) · /evaluate 라우트 제거.

### 1단계 — 페이즈 2 섹션 (채점 이식)

- `app/projects/[id]/evaluate/page.tsx`를 열어 구성 요소를 확인한 뒤, **실행 패널(RunPanel)·"재계산 필요" 배지·등급제 토글·등급 분포 요약·override 안내**를 대시보드 페이즈 2 섹션으로 이식한다(데이터 조회는 대시보드 page.tsx에 통합). **학생별 점수 표(results-table)는 이식하지 않는다** — 작업결과표가 대체(컴포넌트 삭제, grep). RunPanel은 `RunTerminal`을 **상시 렌더**로 변경(lines 조건 제거)하고 stepOne 래퍼에서 성공 시 `emitWorksheetRefresh()`를 호출한다(반영 점수·등급 열은 finalize의 재계산 후 반영되므로 finalize 후에도 1회 — 채점 중에는 확정 표시 점수가 없다는 배치 2 설계를 주석으로 명시).
- 섹션 배치 순서(공통 규칙): 실행 컨트롤 행(모델 배지 + [채점 실행]·[재계산] + needs_recalc 배지) → RunTerminal(상시) → 등급 분포 요약 → [평가 루브릭 편집 →] 카드(기준 개수 표기).
- `app/projects/[id]/evaluate/page.tsx` → `redirect(\`/projects/${id}#phase-2\`)`. `finalizeEvaluation`·`recalculate`·override 액션들의 `revalidatePath(".../evaluate")` → `/projects/${projectId}`. actions.ts 자체는 유지(대시보드가 호출).

### 2단계 — 루브릭 화면 확장 (/rubric 유지·확장 — 페이즈 2의 하위 행동)

`app/projects/[id]/rubric/page.tsx` + `rubric/actions.ts` 확장(기존 `RubricEditor`·`saveRubric` 수동 CRUD는 그대로 유지):

1. **루브릭 전담 모델 선택**(화면 상단): 신규 `RubricModelSelect` — 페이즈 0 default-model-picker와 동일 패턴(키 보유 프로바이더의 저장 모델 select). 저장 액션 `saveRubricModel(projectId, providerId, model)` → `routing.rubric` 갱신(스프레드 보존). 현재 적용 모델 표기: `rubric ?? default ?? evaluate` 폴백 순서 그대로 표시.
2. **평가계획서 업로드 → AI 분석 → diff 승인 반영**(자동 저장 금지 — CLAUDE.md 보수 원칙, 참조 구현: 프롬프트 프로필의 example-ingest 파이프라인):
   - `callLLM`에 선택 인자 추가(**lib/llm/index.ts**): `overrideTarget?: ModelTarget` — 있으면 `modelRouting[key]` 대신 이 target 사용(키 해석·어댑터 경로는 동일). 기존 호출부 무영향.
   - 액션 `extractPlanText(projectId, formData)`: 업로드 파일(txt/md/docx/pdf 텍스트레이어/xlsx/csv)에서 텍스트 추출 — **`lib/records/example-file.ts`를 열어 참조 구현으로 동일 패턴**(파싱 lib 재사용, hwp·스캔 PDF는 안내 에러, 크기 상한 동일).
   - 액션 `analyzeRubricPlan(projectId, planText)`: `callLLM({ purpose: "평가", overrideTarget: rubric ?? default ?? evaluate 해석값, temperature: 0, … })` — 프롬프트: 계획서 텍스트에서 평가 기준을 추출해 **JSON 배열만** 반환 `[{"name":"...","description":"...","max_score":<정수>,"weight":<숫자>}]`(기준 2~8개, max_score 1~100). 서버가 파싱·클램프 후 `RubricCriterion[]`(id는 서버가 `crypto.randomUUID()` 부여)로 반환. **쓰기 없음.**
   - UI: 제안 목록을 현재 기준과 나란히 diff 표시(추가/대체 선택 — 단순하게 "현재 루브릭을 이 제안으로 **교체**" + 항목별 체크박스 채택/제외) → [선택 항목 반영] 클릭 시에만 기존 `saveRubric` 경로로 저장(needs_recalc 트리거는 기존 DB 트리거가 처리).
3. **다운로드**: [xlsx 다운로드] — 기준·설명·배점·가중치 4열 AoA → SheetJS 동적 import(배치 3의 download 패턴 재사용), 파일명 `${sanitize(프로젝트명)}-루브릭-${stamp}.xlsx`.

### 3단계 — 페이즈 3 섹션 (일괄 생성 터미널 + 배치)

- **가성비 추천 순수 함수** 신규 `lib/llm/recommend.ts`(server-only 아님):

```ts
// 가격 대비 성능 우선순위(위가 우선). 저장 모델 목록에서 첫 매칭을 추천한다.
const COST_EFFECTIVE_PRIORITY = [
  "gemini-2.5-flash", "gpt-4o-mini", "claude-haiku", "gemini-2.0-flash", "gpt-4o", "claude-sonnet",
];
export type ModelCandidate = { providerId: string; providerName: string; model: string };
export function recommendCostEffective(candidates: ModelCandidate[]): ModelCandidate | null; // model이 우선순위 문자열로 시작(startsWith)하는 첫 후보(우선순위 순회가 바깥 루프)
```

  테스트 `tests/recommend.test.ts`(우선순위·없음 null·결정성 ≥ 4건, package.json 추가).
- **페이즈 3 섹션**(신규 `components/projects/dashboard/phase3-panel.tsx`): 배치 순서 — ① 실행 컨트롤 행: 모델 배지 "생성: {name}/{model} · 검증: {name}/{model}" + generate 라우팅이 키 없는 프로바이더를 가리키거나 미설정이면 **추천 배지** "추천: {name}/{model} (가격 대비 성능)" + [적용](서버 액션 `applyRecommendedGenerate(projectId)` — generate·verify를 추천값으로 저장) + [생기부 일괄 생성] 버튼 ② RunTerminal(상시) ③ [프롬프트 프로필 →](/profile — 페이즈 3 하위 배치, 사용자 지시) + [학생별 상세·문장 검증 →](/records) 링크 카드.
- **일괄 생성 실행**(INV-1 — 내부는 학생별 순차 단일 호출, SPEC 7.2 그대로):
  - 신규 서버 액션(records/actions.ts 또는 dashboard-actions.ts): `prepareRecordRun(projectId): Promise<{ targets: { id: string; label: string }[] }>` — 대상 = 반영(include_in_record)·매칭 확정 제출물이 1건 이상이거나 교사 메모가 있는 학생(기존 records-client의 generateAll 대상 조건과 동일 — `components/projects/records/records-client.tsx`를 열어 확인), label = "학번 이름". 스킵 없음(재생성 = 새 버전, 기존 동작 유지).
  - stepOne = 기존 `generateRecord(projectId, studentId)` 재사용 — 반환 `GenerateResult`를 열어 확인하고 message를 "v{version} 생성·검증 완료(미근거 {n}문장)" 형태로 구성. 실패는 `{ ok:false, message: 300자 }`. 래퍼에서 `emitWorksheetRefresh()`.
  - finalize = `revalidatePath(\`/projects/${projectId}\`)` + "일괄 생성 완료 — 성공 N·실패 M".
- **records 화면 정리**: `records-client.tsx`의 generateAll(일괄 생성 UI·진행 바)을 **제거**(대시보드 터미널로 대체 — 중복 실행 창구 금지). 학생별 개별 생성·검증·버전 UI는 유지.
- **내보내기**: 페이즈 3 섹션에 안내 한 줄 — "생기부 내보내기(xlsx/csv/md, 전체/선택)는 아래 작업결과표의 [다운로드]를 사용" + [작업결과표 바로보기] (배치 3에서 이미 구현 — **중복 구현 금지**).

### 배치 7 완료 기준

- 테스트·typecheck·lint·build 통과. grep 0건: `results-table`(evaluate), `generateAll`(docs 제외).
- 수동 확인 목록: 대시보드에서 채점 실행(터미널·일시정지·중단·증분), 루브릭 계획서 업로드→제안 diff→선택 반영(자동 저장 없음 확인), 루브릭 xlsx 다운로드, 전담 모델 저장·폴백 표기, 일괄 생성 터미널(1건마다 작업결과표 생기부 열 갱신), 추천 배지·[적용].
- 인계: 배치 8은 문서(SPEC 개정)와 최종 검증만.

---

## [배치 8 프롬프트] — SPEC 갱신 + 최종 검증·인수

`docs/리팩토링_2.md` 배치 8을 실행하라. 전제: 배치 1~7 완료. 코드 변경은 SPEC 문구뿐이다. SPEC은 "읽기 전용"이나 이 리팩토링은 사용자 지시·선택(2026-07-12)으로 개정이 승인된 것이며, 아래 개정 문구가 그 지시의 명문화다 — 반영 후 DECISIONS에 근거를 기록한다.

### 1단계 — SPEC 개정 (해당 절만, 최소 편집)

- **4절(프로젝트 구조)**: 프로젝트 홈 설명을 개정 — "프로젝트 선택 시 **프로젝트 대시보드**로 진입한다. 대시보드는 페이즈 0(준비: API 키 상태·기본 AI 모델·설정) → 1(수합) → 2(평가) → 3(생기부) 섹션이 수직으로 배열된 단일 작업 화면이며, 최하단에 **작업결과표**가 전폭으로 표시된다. 각 페이즈에 [작업결과표 바로보기]가 있고, 작업결과표의 빈 칸을 누르면 해당 작업 페이즈로 스크롤한다." + 설정 항목 목록에 "기본 AI 모델(default 라우팅) — 각 용도 선택의 기본값. 변경 시 개별 설정을 일괄 교체할지 질문" 추가.
- **5.1(업로드)**: "업로드는 원본 저장까지만 수행하고 토큰을 소모하지 않는다. 업로드된 파일 목록(체크박스 기본 선택, 미수합 파일 삭제 가능)을 교사가 확인한 뒤 [수합 & 매칭] 버튼으로 추출을 실행한다. 실행은 파일 1건 단위로 진행 터미널에 표시되고 일시정지·재개·긴급 중단할 수 있다." + "OCR 모델은 등록된 키의 저장 모델 중 비전 가능 모델을 드롭다운으로 고른다(커스텀은 직접 입력). 스캔 PDF는 anthropic·google·openai(file 파트) 모두 지원."
- **5.2**: 매칭 실행 문단에 "수합 실행이 끝나면 같은 터미널에서 매칭이 자동으로 이어진다(동명이인·식별 불가는 종전대로 확인 대기 큐)." 추가.
- **6절(Phase 2)**: 파이프라인 뒤에 **표시 점수** 소절 추가 — "합성 원점수는 순위 산출용이며, 화면·조정에는 **999점 만점 표시 점수**를 쓴다. 초기 확정 인원(채점 대상의 25%를 15~25명으로 클램프, 15명 미만이면 전원)이 채점되기 전에는 표시 점수를 확정하지 않고, 충족 시 원점수 순위대로 800~200 범위 등간격(최소 15점)으로 배정한다. 이후 채점 학생은 기존 배정 사이의 중간값으로 삽입하고, 정수 여유가 소진되면 순서를 보존해 전체 재배치한다. 배정된 표시 점수는 재계산에도 유지된다. 교사 보정(score_override)은 0~999 정수(사유 필수)로 표시 점수보다 우선한다. 등급은 표시 점수 순위에서 파생 계산한다(INV-6 불변)." / 루브릭 항목에 "루브릭 편집은 Phase 2의 하위 행동이다. 평가계획서 파일을 업로드하면 전담 모델(라우팅 rubric ?? default ?? evaluate)이 기준·배점을 제안하고, 교사가 승인한 항목만 반영된다(자동 반영 금지). 루브릭은 xlsx로 내보낼 수 있다."
- **7.5**: 첫머리에 "프롬프트 프로필 진입은 Phase 3(생기부)의 하위 배치다. 생성 모델이 미설정이면 등록 키의 모델 중 가격 대비 성능 우선순위로 추천해 보여준다." 추가.
- **8절(결과 화면)**: 절 제목을 "8. 작업결과표"로 바꾸고 본문 대체 — "대시보드 하단의 전폭 엑셀 시트형 표. 열: ID(제품 부여 고유 번호)·학번·이름·업로드 된 제출물 갯수·반영 점수·등급·생성된 생기부·교사 관찰 메모. 1행 헤더에서 정렬·값 필터·열 감추기, 열 너비·행 높이 조절, 전체 접기/펼치기. 제출물 갯수를 누르면 제출물 제목 목록이 펼쳐진다. 반영 점수는 저장/취소 버튼이 있는 오버라이드 편집, 학번·이름은 [학생별 데이터 편집](ID 제외 대부분 수정 가능·학생 추가/삭제 포함 — 학생 명단 화면을 대체), 생기부·메모는 인라인 편집. 각 페이즈 실행에서 학생 1명 처리가 끝날 때마다 행이 즉시 갱신된다. xlsx/csv/md 다운로드(전체/선택, 파일명 `프로젝트명-yymmddHHmm`). 레이아웃은 (사용자, 프로젝트) 단위 저장·복원."
- 12절(범위 밖)에 "루브릭 파일(xlsx) 재가져오기(계획서 분석 업로드로 대체)" 한 줄 추가.

### 2단계 — 최종 검증·기록

- 공통 종료 절차의 4개 검증 + **INV-4 번들 스캔(전부 0건)**:

```bash
for pat in "x-api-key" "api.anthropic.com" "generativelanguage" "encrypted_key" "APP_ENCRYPTION_KEY" "createDecipheriv" "SUPABASE_SERVICE_ROLE_KEY"; do grep -rl "$pat" .next/static | wc -l; done
```

- 제거 심볼 최종 grep(전부 0건, docs 제외): `ingestDocuments` `ingestSpreadsheet` `StagedSubmissions` `IngestClient` `MatchingPanel` `results-grid` `StudentList` `normalizeLayout` `generateAll`.
- `docs/DECISIONS.md`에 배치 1~7 기록이 다 있는지 점검(누락분 보충). `docs/DATA_MODEL.md`에 0012·display_score가 반영돼 있는지 점검.
- `docs/PROGRESS.md`에 "리팩토링 2" 요약 섹션 + **배포 후 사용자 체크리스트**: ① 통사 9반 PDF 재업로드 → [수합 & 매칭] → OCR 성공(Google·OpenAI 모델 각각) ② 업로드 시 토큰 미소모·파일 목록 체크/삭제 ③ 수합→매칭 연쇄 터미널·1건마다 작업결과표 갱신 ④ 채점 → 초기 확정 인원 전 "확정 대기" → 충족 후 800~200 배정 → 추가 채점 사이 삽입 ⑤ 작업결과표 정렬/필터/감춤/행높이/다운로드(전체·선택) ⑥ 점수 오버라이드(저장/취소)·학생별 데이터 편집 ⑦ 페이즈 0 기본 모델 저장 → 교체 질문 ⑧ 루브릭 계획서 업로드→승인 반영 ⑨ 생기부 일괄 생성 터미널 ⑩ 빈 칸 클릭 → 페이즈 스크롤.
- 리팩토링_2_프로그레스.md 최종 기록 후 커밋·푸시.

---

## 부록 — 전체 수용 기준 (사용자 요구 ↔ 검증)

1. 통사 9반 PDF: 업로드 → 수합 실행 시 "The document has no pages" **재발 없음**, OpenAI 모델 선택 시에도 PDF OCR 동작(배치 1 + 배포 후 확인).
2. 업로드 즉시 토큰 소모 없음 — [수합 & 매칭] 클릭 시에만 추출·매칭 실행, 업로드 파일 확인·체크 제외·삭제 가능(배치 6).
3. 수합 완료 후 매칭이 같은 터미널에서 자동 연쇄, 애매 건만 확인 대기 큐(배치 6).
4. OCR 모델이 프로바이더 저장 목록 기반 드롭다운(비전 가능 필터), gpt-4o만 고정 제공되던 제약 소멸(배치 1·6).
5. 점수 999점 만점: 초기 15~25명 확정 대기 → 800~200 스프레드(간격 ≥ 15) → 사이 삽입 → 소진 시 재배치, 배정 점수는 재계산에도 유지(배치 2).
6. 대시보드 = 페이즈 0~3 수직 + 하단 작업결과표(전폭), 각 페이즈 [작업결과표 바로보기], 빈 칸 클릭 → 해당 페이즈 스크롤(배치 3~5).
7. 페이즈 0: API 키 상태·인라인 등록·기본 AI 모델·전 페이즈 교체 질문(배치 5).
8. 공용 실행 터미널이 페이즈 1·2·3 전부에 상시 표시, 상단 배치, 1명 처리마다 작업결과표 즉시 갱신(배치 6·7).
9. 작업결과표 8열 + 정렬·필터·감춤·열너비·행높이·전체 접기/펼치기 + 제출물 갯수 펼침 + 오버라이드(저장/취소·사유) + 학생별 데이터 편집(+추가/삭제 — 학생 명단 대체) + xlsx/csv/md 다운로드(전체/선택, `프로젝트명-yymmddHHmm`)(배치 3·4).
10. 루브릭 = 페이즈 2 하위: 전용 편집 화면 + 평가계획서 업로드 → AI 제안 → 교사 승인 반영 + 전담 모델 + 다운로드(배치 7).
11. 프로필 = 페이즈 3 하위 + 생성 모델 가성비 추천, 생기부 일괄 생성 터미널(INV-1 유지)(배치 7).
12. `npm test`·typecheck·lint·build 통과, INV-4 번들 스캔 7종 0건, INV-1~6 위반 없음(배치 8).
