# 리팩토링 2 프로그레스 — 대시보드 수직 통합 + 작업결과표 + 999점 표시 점수 + PDF 버그 + 진실성 검증·도서팩트시트

> 실행 지시서는 `docs/리팩토링_2.md`(v2 — [공통 프롬프트] + [배치 1~12 프롬프트] 구조). 이 문서는 **실제 진행의 누적 기록**이며, 배치 간 유일한 인수인계 통로다.
>
> **진행 방식**: 배치마다 **새 대화**에서 실행한다. 새 대화에는 리팩토링_2.md의 [공통 프롬프트] + 해당 [배치 N 프롬프트]를 붙여넣거나, 파일 접근이 되는 환경이면 "docs/리팩토링_2.md의 공통 프롬프트를 따르고 배치 N을 실행해"라고 지시한다. 각 배치는 검증(test/typecheck/lint/build) 통과 → 이 문서 기록 → DECISIONS 기록 → **커밋·main 푸시**까지 마치고 대화를 끝낸다.
>
> **기록 규칙**: 배치를 끝낼 때마다 ① 아래 체크리스트 갱신 ② "진행 기록" 절에 `날짜 / 배치 / 결과 / 검증 출력 요약 / 특이사항(계획과 달라진 점·발견한 문제·다음 배치 인계)`를 **추가**한다(누적만 — 기존 기록 수정·삭제 금지). 계획과 다르게 구현했으면 반드시 사유를 남기고, 설계 자체가 바뀌면 리팩토링_2.md도 함께 고친 뒤 그 사실을 기록한다.
>
> **배치 시작 전 확인**: 직전 배치가 체크리스트에서 완료 상태인지, 워킹 트리가 깨끗한지(`git status`) 확인. 어긋나면 작업을 멈추고 사용자에게 보고.

## 배치 체크리스트

- [x] 준비 — 원인 규명(PDF detach 탐침)·사용자 전략 선택·문서 생성 (리팩토링_2.md v1 + 이 문서)
- [x] **배치 1** — PDF detach 방어 사본 + OCR 빈 데이터 가드 / OpenAI PDF file 파트 / `isVisionCapableModel` / ModelRouting `default`·`rubric` 키 + `updateModelRouting` 키 보존
- [x] **배치 2** — 마이그레이션 0012(display_score) / `lib/scores/display.ts`(스프레드·삽입·재배치) + 테스트 / `recomputeAndSave` 개편(sticky·확정 대기) / override 0~999
- [x] **배치 3** — 작업결과표 코어: `lib/worksheet/*`(types·layout·refresh·download·assemble) / `fetchWorksheetRows`·`saveWorksheetLayout` / `WorksheetTable`(정렬·필터·감춤·너비·행높이·접기·다운로드) / 대시보드 하단 임시 장착
- [x] **배치 4** — 작업결과표 상호작용(제출물 펼침·빈 칸 앵커·오버라이드 저장/취소·학생별 데이터 편집·추가/삭제·생기부/메모 인라인) / /results·/students 제거
- [x] **배치 5** — 대시보드 수직 개편(phase-0~3 + worksheet 앵커) / 페이즈 0(API 키 상태·인라인 등록·기본 AI 모델·전 페이즈 교체 질문)
- [x] **배치 6** — `useSequentialRun` nextStage 연쇄 / 업로드-파싱 분리·파일 목록(체크·삭제) / [수합 & 매칭](수합→매칭 자동 연쇄·터미널·행 즉시 갱신) / OCR 동적 드롭다운 / /ingest 제거·MatchingPanel 제거
- [ ] **배치 7** — 페이즈 2 이식(/evaluate 제거) / 루브릭 확장(평가계획서 AI 분석·전담 모델·xlsx 다운로드) / 페이즈 3(일괄 생성 터미널·프로필 배치·가성비 추천)
- [ ] **배치 8** — 도서팩트시트 기반: 마이그레이션 0013(factsheets·entries·submissions 진실성 3컬럼·RLS) / 알라딘·네이버 검색 클라이언트(env 키 3종) / /factsheets 페이지(검색·생성·수동 편집·촬영본 OCR 보강·공유 신청) / 페이즈 2 [도서팩트시트 →] 버튼
- [ ] **배치 9** — 자동 생성·보강 파이프라인: `filterBySnippetMatch`(할루시네이션 구조 차단) / planCollection·collectOneSource / [자동 보강] 터미널(문서 1건 단위·중복 방지·새 챕터 우선)
- [ ] **배치 10** — 평가 통합: prepareAuthenticity·verifyAuthenticityOne(식별→팩트시트 확보/자동 생성→대조→플래그) / 페이즈 2 실행 = 진실성→채점 2스테이지 연쇄 / 배지(/submissions·작업결과표 서브행·페이즈 2 요약)
- [ ] **배치 11** — 공유·관리자 승인: /admin 팩트시트 승인 큐 / AI 엄격 검증 실행(entry 1건 단위: 출처 재fetch·발췌 실존 대조·LLM 판정 리포트) / 승인·반려·철회 + audit
- [ ] **배치 12** — SPEC 4·5.1·5.2·6(표시 점수·진실성·팩트시트)·7.5·8·9·12절 개정 / INV-4 번들 스캔(9패턴) 포함 최종 검증 / DECISIONS·DATA_MODEL·README 점검·PROGRESS 요약·배포 후 사용자 체크리스트(15항)

## 진행 기록

### 2026-07-12 / 준비(원인 규명·문서) / 완료

- **PDF "no pages" 원인 실증**: `parsePdfText`(unpdf/pdf.js `getDocumentProxy`)가 입력 Uint8Array의 ArrayBuffer를 detach시킴을 탐침으로 확정 — 호출 전 `bytes.length=328` → 호출 후 **0**, base64 길이 440 → **0**. 스캔 PDF OCR 경로(`ingestDocuments`)가 파싱 **후** `Buffer.from(bytes).toString("base64")`를 하므로 빈 PDF가 전송돼 Google `400 "The document has no pages"` — 모델 무관 재현. `parsePdfText(bytes.slice())` 사본 전달 시 원본 보존 확인. OpenAI PDF 거부는 `toOpenAIContent`의 하드코딩 throw(실제 API는 file 파트로 PDF 지원)로 확인.
- **사용자 선택(2026-07-12) 3건**: ① PDF 수정 = **라이브러리 경계 방어**(parsePdfText 내부 사본 + OCR 빈 데이터 가드 + 회귀 테스트) ② 페이즈 실행 UI를 **대시보드로 통합하고 /ingest·/evaluate 라우트 제거**(리다이렉트, 세부 화면은 유지) ③ 기존 /results 결과 표는 **작업결과표가 흡수·대체**(SPEC 8절 개정 포함).
- 실행 지시서 `docs/리팩토링_2.md`(배치 8개, 공정 순: 버그·기반 → 점수 체계 → 작업결과표 코어 → 상호작용·구화면 정리 → 대시보드·페이즈 0 → 페이즈 1 → 페이즈 2·3 → SPEC·최종)와 이 문서를 생성. 배치 간 고정 계약(앵커 id·워크시트 열 키·`worksheet:refresh` 이벤트·표시 점수 상수·model_routing `default`/`rubric` 키·다운로드 파일명 규칙)을 공통 프롬프트에 명문화 — 하위 모델이 배치를 이어받아도 이름이 어긋나지 않게 함.
- 설계 판단은 `docs/DECISIONS.md` 2026-07-12 항목들로 기록(표시 점수 공식·sticky·재배치, 초기 확정 인원 산식, 작업결과표가 학생 명단·결과 표 대체, 기본 AI 모델 라우팅 키, OCR 비전 판별 휴리스틱, xlsx 확장자 채택 등).
- 특이사항: 이후 배치는 **다른(하위) 모델이 새 대화에서** 실행하는 것을 전제로 지시서를 작성했다 — 수정 대상 파일은 "수정 전 전체 읽기", 문서≠코드 시 코드 기준, 참조 구현 명시(채점 run-panel·matching-panel·results-grid 드래그·example-ingest diff 승인 패턴). 리팩토링 1의 불가침이던 `OcrModelSelect`·`VISION_MODELS`는 이번 스코프에서 개편 대상임을 공통 프롬프트에 명시.

### 2026-07-13 / 준비(진실성 검증·도서팩트시트 추가 — 지시서 v2) / 완료

- 사용자 추가 지시: 페이즈 2에서 **점수 부여에 앞서 제출물 진실성 검증** — 독서 활동은 인용 도서를 특정해 **도서팩트시트**(메타·목차·소개 + 챕터별 내용, 재사용으로 토큰 절약)와 교차 대조, URL 인용(기사·웹페이지)은 원문 fetch 대조. 팩트시트 전용 페이지(도서 검색 모듈·수동 편집·촬영본 OCR 보강), 평가 실행 중 자동 생성/보강(중복 방지·새 챕터 우선), **팩트시트 자체의 할루시네이션 절대 금지**.
- **사용자 선택 3건(2026-07-13)**: ① 수집 채널 = **오픈 API 직접 수집**(알라딘 메타·목차 + 네이버 검색 → 원문 fetch, LLM은 추출만 — 관리자 환경변수 키 3종) ② 의심 판정 = **플래그만, 채점은 진행**(자동 감점·제외 없음) ③ 공유 범위 = **생성 계정 소유 → 교사 신청 → 관리자 승인 시 전체 공유**, 관리자 승인 페이지에 **AI 엄격 자동 검증**(출처 재수집·발췌 실존 대조 리포트) 포함, shared는 읽기 전용(복제 후 보강).
- 지시서를 v2로 개정: 배치 8(팩트시트 기반 — 0013 마이그레이션·검색 클라이언트·/factsheets·수동 보강) · 배치 9(자동 생성·보강 — `filterBySnippetMatch` 구조 차단) · 배치 10(평가 2스테이지 연쇄 — 진실성→채점, `nextStage` 재사용) · 배치 11(공유·관리자 승인·AI 엄격 검증) 신설, 구 배치 8(SPEC·최종)은 **배치 12로 개명**·확장(SPEC 6절 진실성·팩트시트 소절, 번들 스캔 9패턴, 체크리스트 15항). 공통 프롬프트에 공유 계약(share_status·authenticity_status·source_type·env 키·/factsheets)과 무할루시네이션 원칙(서버 스니펫 대조 강제)을 추가.
- 특이사항: 마이그레이션은 0012·0013 두 개로 확정(0013은 배치 8에서 스키마 일괄 — 이후 배치 스키마 불변). 작업결과표 8열 계약은 불변 — 진실성 배지는 제출물 펼침 서브행·/submissions·페이즈 2 요약에 표시(열 추가 아님). 논문은 URL·DOI 명시분만 대조(제목만 인용은 unverifiable로 정직 표시 — 비범위 기록).

### 2026-07-13 / 배치 1 (PDF 파이프라인 버그 수정 + 기반 타입) / 완료

- **1단계 — PDF detach 방어 + OCR 빈 데이터 가드**: `lib/parsing/document.ts`의 `parsePdfText`가 `getDocumentProxy(bytes.slice())`로 **사본**을 넘겨 입력 Uint8Array의 detach를 제거(호출 후 base64 인코딩 가능). `app/projects/[id]/ingest/actions.ts`의 `ocrExtract` 서두에 `if (!part.dataBase64) throw` 가드 추가(향후 detach·다운로드 실패 시 프로바이더 400 대신 명확한 원인). 회귀 테스트 `tests/parsing.test.ts`에 최소 PDF로 ①호출 후 `bytes.length` 보존 ②pages=1 ③`isLikelyScan=true` 검증 1건 추가.
- **2단계 — OpenAI PDF file 파트**: `LLMDocumentPart.filename?` 추가(`lib/llm/types.ts`), `toOpenAIContent`의 PDF throw 제거 → `{type:"file", file:{filename, file_data:"data:application/pdf;base64,…"}}` 매핑(`OpenAIPart` 유니언에 file 파트 추가). 스캔 PDF `ocrExtract`가 `filename: f.filename` 전달. adapters/openai.ts·vision-models.ts 주석 갱신(로직 불변). `tests/vision-content.test.ts`의 throw 기대를 file 파트 매핑 기대로 반전(filename 기본값·전달값 2케이스).
- **3단계 — 비전 판별 순수 함수**: 신규 `lib/llm/vision-capability.ts`(`isVisionCapableModel(format, modelId)`, server-only 아님). 신규 `tests/vision-capability.test.ts` 14건(true 10·false 6·format 교차 2 — 실제 케이스 배분) + package.json test 목록 추가.
- **4단계 — ModelRouting 확장 + 키 보존**: `ModelRouting`에 `default?`·`rubric?` 선택 키 추가. `updateModelRouting`이 저장 전 현재 `model_routing`을 조회해 스프레드 보존(4키 통째 덮어쓰기 → 확장 키 소실 버그 제거). `saveOcrModel`은 이미 `...routing` 스프레드라 안전(확인 완료).
- **검증**: `npm test` **107/107**(직전 102 + parsing detach 1 + vision-capability 3 + vision-content 반전 +1) 통과, `tsc --noEmit`·`eslint`·`next build`(19라우트) 전부 통과. grep 확인: `toOpenAIContent` PDF throw 잔존 0건, `getDocumentProxy(bytes)`(원본 전달) 잔존 0건(사본 `.slice()`만).
- **특이사항/인계**: 문서와 실제 코드 일치(어긋남 없음). 스캔 PDF OCR 실검증(통사 9반 PDF 재업로드 → detach 수정으로 Google 400 해소 확인)은 브라우저·LLM 과금 필요 → **배포 후 사용자 확인 항목**. OpenAI 형식 스캔 PDF도 이제 file 파트로 시도되나 실제 모델 응답 검증은 동일하게 배포 후 확인. DB 스키마 변경 없음(마이그레이션 미발생 — 0012는 배치 2).

### 2026-07-13 / 배치 2 (999점 표시 점수 체계) / 완료

- **1단계 — DATA_MODEL 갱신 + 마이그레이션 0012**: `docs/DATA_MODEL.md` 10절(student_scores)에 `display_score integer`(sticky·미확정 국면 행 미생성·nullable) 행 추가, `effective_score = coalesce(override, display)`로 의미 갱신, `composite_score` = "원점수" 명기. 7절 `students.score_override`에 "0~999 정수(2026-07-12 점수 체계 전환)" 추가. 신규 `supabase/migrations/0012_display_score.sql`(display_score 컬럼 + 주석 + 구 스케일 override 초기화 UPDATE).
- **2단계 — 순수 모듈 `lib/scores/display.ts`**: `DISPLAY_MAX=999`·`SPREAD_TOP=800`·`SPREAD_BOTTOM=200`·`MIN_INITIAL_GAP=15` 상수, `initialConfirmCount`(15미만 전원·25% 클램프), `spreadScores`(등간격 내림차순, n=1→[500]), `insertBetween`(최상위=(below+999)/2·최하위=above/2·중간값·소진 시 null), `assignDisplayScores`(탈락자 정리→미달 국면 빈 Map→초기 스프레드(동점 그룹 통일)→sticky 유지 + 신규 이웃 중간 삽입→소진 시 respread 전체 재배치). 5단계 의미론 그대로 구현.
- **3단계 — 신규 테스트 `tests/display-score.test.ts` 18건**(package.json test 목록 추가): initialConfirmCount 6케이스·spreadScores 4·insertBetween 4·assignDisplayScores 8(미달·초기 확정 동점·중간 삽입·최상/최하 진입·신규 동점·respread 순서 보존·탈락자 제거·결정성).
- **4단계 — `recomputeAndSave` 개편**: 삭제 전 기존 display_score 조회(sticky `existing`, null 제외), `totalTargets` = 제출물 귀속 학생 고유 수(override만 있는 학생 제외), `rawRanked` = 평가 있는 학생 composite 내림차순(동률 studentId 사전순). `assignDisplayScores` 호출 → **미확정(confirmed=false)**: student_scores 삭제만·needs_recalc 해제·`{ranked:0, pendingConfirm:{scored,required}}` 반환. **확정**: `effective = override ?? display`, override만 있는 학생 display=null, `computeStandings`로 rank·grade, insert에 `display_score` 추가. 반환 타입 `RecomputeResult` 신설. 호출부 3곳 갱신: `finalizeEvaluation`·`recalculate`는 `RecomputeResult` 반환, `setScoreOverride`/`clearScoreOverride`는 반환 무시. run-panel finalize/recalc 메시지를 미확정이면 "채점 N/required명 — 표시 점수 확정 대기…"로 분기.
- **4단계(계속) — override·표시**: `setScoreOverride` 검증을 정수 0~999로(위반 "보정 점수는 0~999 정수입니다."), `evaluateOne` 성공 메시지 `${total}점`→`원점수 ${total}점`. `results-table.tsx`(최소 수정): ScoreRow에 `displayScore` 추가, 헤더 "합성 점수"→"원점수"·"반영 점수"→"반영 점수(999)", 보정 기본값 `override ?? displayScore`, 입력 step=1·min0·max999·클라 검증 0~999. run-panel 안내문에 "표시 점수는 초기 확정 인원(15~25명) 채점 후 999점 만점으로 확정됩니다." 추가. `page.tsx` student_scores select에 `display_score` 추가·ScoreRow 채움. `lib/supabase/types.ts` StudentScore Row·Insert·Update에 `display_score: number | null` 추가.
- **검증**: `npm test` **125/125**(직전 107 + display-score 18) 통과, `tsc --noEmit`·`eslint`·`next build`(19라우트) 전부 통과. 마이그레이션 0012 **원격 적용·재조회 확인**(pooler + pg .cjs 스크립트): `display_score` integer/nullable + 주석 존재 확인. 수동 시나리오(코드 리딩): 20명·5명 채점 → 미확정(0행, required=15) / 15명 → 800~200 스프레드 / 16번째 → 이웃 중간 삽입.
- **특이사항/인계**: ① 배치 2 지시서는 `supabase db push --db-url`를 명시했으나, dry-run 결과 원격 마이그레이션 히스토리에 **0010·0011이 미기록**(prior 배치가 .cjs raw SQL로 out-of-band 적용) → `db push`는 0010·0011 재적용을 시도해 "column already exists"로 실패. 코드=진실 원칙에 따라 **pg .cjs raw SQL 직접 적용**(리팩토링 1 관행과 동일). 실측: api_keys.models·submissions.identity_source 컬럼 이미 존재, override_not_null 0행(0012 UPDATE는 no-op — 초기화할 구 보정값 없음, 사용자 고지 불필요). ② 실제 채점 흐름(표시 점수 확정·스프레드·삽입·sticky)은 LLM 과금 필요 → **배포 후 사용자 확인 항목**. ③ 표시 점수 UI 전면(작업결과표 반영 점수 칸)은 배치 3~5. 현행 results-table은 배치 7에서 대체 예정이라 최소 수정만 함.

### 2026-07-13 / 배치 3 (작업결과표 코어 — 표 엔진 + 다운로드) / 완료

- **1단계 — 순수 모듈 `lib/worksheet/`**: `types.ts`(`WORKSHEET_COLUMNS` 8열 계약·`COLUMN_LABELS`·`WorksheetRow`·`WorksheetSubmission`), `layout.ts`(`WorksheetLayout`·`normalizeWorksheetLayout`(garbage·구형 results shape 방어·stale studentId 제거·너비 60~800·행높이 28~600 클램프)·`applySort`(null/빈 값 항상 끝·숫자 열 수치 비교·텍스트 localeCompare("ko"))·`applyFilters`(세션 상태·`FILTERABLE_COLUMNS`=학번·이름·갯수·등급, "" = 빈 값)·`worksheetFilterValue`·`DEFAULT_COLUMN_WIDTHS`), `refresh.ts`(`WORKSHEET_REFRESH_EVENT`·`emitWorksheetRefresh` SSR 가드), `download.ts`(`formatDownloadStamp` yymmddHHmm 로컬·`sanitizeFilename` 금지 문자 제거→빈 값 project·`buildWorksheetAoA` 헤더 8열+행, 갯수·점수·등급 숫자/생기부·메모 전문). **추가**: `assemble.ts`(순수·주입식 `assembleWorksheetRows` — 페이지·액션 공용 조립, 기본 정렬 학번 asc(null 끝)·이름 asc).
- **2단계 — 서버 액션 `app/projects/[id]/worksheet-actions.ts`("use server")**: `fetchWorksheetRows`(requireProjectOwner → owner RLS 4쿼리: students / submissions(`student_id` NOT NULL만) / student_scores / records(is_current) → `assembleWorksheetRows`), `saveWorksheetLayout`(참조: results/actions.ts saveLayout — ui_layouts upsert onConflict(user_id,project_id) + 서버 `normalizeWorksheetLayout` 재정규화).
- **3단계 — `components/projects/worksheet/worksheet-table.tsx`("use client")**: 엑셀 시트형(border-collapse·전 셀 경계선·sticky 헤더·table-fixed+colgroup·hover). 헤더 ▾ 메뉴(오름/내림/정렬 해제·필터 고유값 체크박스(전체/해제)·이 열 감추기)+정렬 화살표·필터 깃발·우측 경계 드래그 너비. 툴바(전체 접기/펼치기 토글·감춘 열 N개[모두 표시]·다운로드▾ xlsx/csv/md(정렬 반영·필터 무시·전체 열)·선택 N행 다운로드·새로고침·행 수·저장 상태). 행 = 체크박스(전체 토글)+8열, ID 셀 앞 8자+title 전문, 행 하단 드래그 높이, 빈 값 빈 칸(클릭 동작은 배치 4). 레이아웃 변경 700ms 디바운스 저장, `WORKSHEET_REFRESH_EVENT` 수신 800ms 디바운스 재조회. xlsx는 `await import("xlsx")` 동적 import(`aoa_to_sheet`→`writeFile`), csv BOM+`\r\n`, md 표 문법(`\|` 이스케이프·개행 `<br>`).
- **4단계 — 대시보드 하단 임시 장착(page.tsx)**: 기존 구조 불변, 최하단에 `<section id="worksheet">`(full-bleed `left-1/2 w-screen -translate-x-1/2 px-3`) 추가. 페이지가 액션을 호출하지 않고 **동일 4쿼리 + `assembleWorksheetRows`**로 initialRows 직접 조립(조립 로직 중복 금지), ui_layouts select로 initialLayout(unknown) 전달.
- **검증**: `npm test` **140/140**(직전 125 + worksheet-layout 12 + worksheet-download 3) 통과, `tsc --noEmit`·`eslint`·`next build`(19라우트) 전부 통과.
- **특이사항/인계**: ① `ui_layouts.layout` Insert/Update 타입이 `LayoutState`로 고정돼 있어 `WorksheetLayout` 저장 시 TS2739 → `lib/supabase/types.ts`에 `StoredLayout = LayoutState | WorksheetLayout` 유니언 도입(Row는 이미 unknown). **구 결과 표(/results)와 작업결과표가 같은 (user_id, project_id) 행을 공유**하므로 배치 3 기간엔 두 화면의 레이아웃이 서로 덮어쓴다 — 각 normalize가 상대 shape를 기본값으로 방어하므로 무해(크래시 없음). /results는 배치 4에서 제거되어 해소된다. ② 셀 클릭 동작(제출물 펼침·빈 칸 앵커 스크롤·오버라이드/학생/생기부/메모 편집)은 **배치 4**. `expandedCounts` 상태는 배치 4에서 도입(미선언 — lint 클린 유지). ③ 실제 표 렌더·정렬/필터/감춤/너비/행높이/전체접기·다운로드 파일명 `프로젝트명-yymmddHHmm.*`·선택 다운로드는 브라우저 실동작이 필요 → **배포 후 사용자 확인 항목**(순수 로직·조립은 테스트로 증명). ④ 대시보드 개편(배치 5) 전까지 기존 준비 카드·평가 흐름과 공존.

### 2026-07-13 / 배치 4 (작업결과표 상호작용 + 구 표·명단 정리) / 완료

- **1단계 — 셀 상호작용(`components/projects/worksheet/worksheet-table.tsx`)**: 기존 표 엔진(배치 3) 유지 위에 상호작용 추가. **제출물 갯수 셀**(count>0) 클릭 → 행 아래 확장 서브행(colSpan 전체)에 `submissions[].title` 목록 토글(`title`=제출물 id), [전체 접기]는 `expanded` Set을 비워 서브행도 닫는다. **빈 칸 앵커**(`EmptyCellButton`→`scrollToAnchor`): 갯수 0→`#phase-1`·점수 null→`#phase-2`·등급 null→`#phase-2`·생기부 null→`#phase-3`(앵커 미존재 시 무동작 — 배치 5 전 안전 설계). **반영 점수 셀(값)**→인라인 오버라이드(`ScoreCell`): 숫자 0~999 + 사유 필수 + **[저장][취소]**, `hasOverride`면 [보정 해제], 저장/해제 성공 시 `emitWorksheetRefresh()`; 검증(0~999 정수·사유)은 클라 선검증 + 서버 `setScoreOverride`가 재검증. **학번·이름 셀**→`StudentModal`("학생별 데이터 편집": ID 읽기전용·복사, 학번·이름 편집, 메모·보정은 셀 안내, [저장]=`updateStudent`/[학생 삭제]=`deleteStudent` cascade 경고 confirm). **툴바 [+ 학생 추가]**=같은 모달 신규 모드(`addStudent`). **생기부 셀**(`RecordCell`): 클릭=펼침(전문+`countText`자수+버전) → [편집] textarea + [저장](`saveRecordEdit(...,[])` 새 edited 버전)[취소]. **메모 셀**(`MemoCell`): 클릭=textarea, blur 저장(`saveTeacherMemo`)+"저장됨"(TeacherMemoBox 패턴, 로컬 authoritative). 모든 편집 액션은 **기존 서버 액션 재사용**(INV 준수 — id/사유만 전달, 서버가 DB 조립). ID(studentId)는 어디서도 편집 불가. `page.tsx`에 `count_method` select 추가 + `countMethod` prop 전달.
- **2단계 — /results 제거·대체**: `results/page.tsx`→`redirect("/projects/${id}#worksheet")`, `results/actions.ts`·`components/projects/results/{results-grid,text-cell}.tsx`·`lib/records/layout.ts`·`tests/layout.test.ts` 삭제(package.json test 목록에서 제거). `records/page.tsx` 헤더 "결과 표"→"작업결과표"(`#worksheet`). `lib/supabase/types.ts`의 `StoredLayout`를 `WorksheetLayout` 단독으로 축소(LayoutState import 제거). 잔존 참조 주석 2건(worksheet-actions·types) 정리.
- **3단계 — /students 제거·대체**: `students/page.tsx`→redirect, `components/projects/student-list.tsx` 삭제(`TeacherMemoBox`는 records가 써서 유지). `students/actions.ts`는 유지(작업결과표가 `addStudent`/`updateStudent`/`deleteStudent`/`saveTeacherMemo` 재사용) — 내부 `revalidatePath(.../students)`를 `revalidatePath("/projects/${projectId}")`로 교체. 홈 준비 카드에서 "학생 명단" 카드 1장 제거(홈 전면 개편은 배치 5).
- **검증**: `npm test` **131/131**(직전 140 − layout.test.ts 9건) 통과, `tsc --noEmit`·`eslint`·`next build`(19라우트, /results·/students는 이제 redirect) 전부 통과. grep 잔존 0 확인: `results-grid`·`text-cell`·`normalizeLayout`·`LayoutState`·`setAllCellModes`·`student-list`·`StudentList`(docs 제외).
- **특이사항/인계**: ① 문서와 코드 일치. 지시서의 `expandedCounts`는 실제로는 studentId 기준 `expanded: Set<string>`로 구현(갯수가 아니라 제출물 목록 펼침이므로 이름을 정정 — 계약 아님, 내부 상태). ② worksheet-table(클라이언트)이 `evaluate/actions`(setScoreOverride·clearScoreOverride)를 import — "use server" 액션 참조라 서버 전용 코드 클라 유입 없음(build 통과로 확인, INV-4). ③ 홈 하단 "결과 표" 문구 링크(`/results`)는 그대로 두어 redirect로 동작 — 배치 5 홈 개편이 정리(지시서 인계 지침). ④ **배포 후 사용자 확인 항목**(브라우저 실동작 필요): 갯수 펼침/접힘, 빈 점수·생기부 셀 클릭 시 무동작(앵커 미존재 — 배치 5 후 스크롤), 점수 오버라이드 저장/취소/해제(사유 필수), 학생 편집·추가·삭제(cascade), 생기부·메모 인라인 저장. ⑤ 메모 셀은 로컬 값 authoritative(refetch로 덮지 않음) — 메모는 이 셀에서만 편집되므로 충돌 없음.

### 2026-07-13 / 배치 5 (대시보드 수직 개편 + 페이즈 0) / 완료

- **1단계 — 페이지 구조 개편(`app/projects/[id]/page.tsx`)**: 기존 "준비 카드 그리드 + 평가 흐름 목록 + 하단 작업결과표"를 [공통 프롬프트] "전체 그림"대로 **phase-0→1→2→3→worksheet 수직 구조**로 재작성. `export const maxDuration = 120` 추가(배치 6·7 서버 액션 대비). 페이즈 래퍼 = 신규 `components/projects/dashboard/phase-section.tsx`("use client" — `{ id, step, title, desc, children }`, 번호 원형 배지·우측 [작업결과표 바로보기]→`#worksheet` scrollIntoView, `scroll-mt-6`). 페이즈 1~3 섹션은 골격(구 페이지로의 링크 카드): phase-1=/ingest·[매칭·확인 →](/submissions), phase-2=/evaluate·루브릭(/rubric), phase-3=/records·프롬프트 프로필(/profile). 설정은 phase-0 안 [프로젝트 설정 →](/settings) 링크 카드. worksheet 섹션(전폭 full-bleed)은 배치 3 장착분을 최하단으로 이동(`scroll-mt-6` 추가). 페이즈 1에 확인 대기 배지: `submissions` count(head) `.in("match_status", ["pending_confirm","update_pending"])` > 0이면 "확인 대기 N건".
- **2단계 — 페이즈 0 API 키 상태 + 인라인 등록**: 서버에서 `listRoutableProviders(user.id)` 조회 → keySource != null 프로바이더가 ≥1이면 **키 있음**(초록 체크·프로바이더 나열·개인/기본 키 배지·[키 관리 →]/account), 없으면 **키 없음**(앰버 경고 + `PersonalKeys` 인라인 재장착 — account/page.tsx의 providers·personalKeys 조립을 그대로 복사: `providers.select("*")` + `api_keys.select("provider_id,key_last4,models,models_synced_at").eq("owner_id",user.id)`). 키 저장 액션은 account의 것 재사용(컴포넌트 그대로).
- **3단계 — 페이즈 0 기본 AI 모델 + 전 페이즈 교체 질문**: 신규 `components/projects/dashboard/default-model-picker.tsx`("use client") + 신규 `app/projects/[id]/dashboard-actions.ts`("use server"). `saveDefaultModel(projectId, providerId, model)` = requireProjectOwner → 기존 model_routing 스프레드 보존(배치 1 updateModelRouting 원칙) → `default` 저장 → extract/evaluate/generate/verify 중 default와 다른 항목 목록(`differing`, providers 이름 조인) 반환. `applyDefaultToAllRouting(projectId)` = default 없으면 throw, 4키 전부 default로 교체(rubric·default 보존). 피커 UI: 프로바이더 select(keySource != null만)·모델 select(저장 목록 또는 VISION_MODELS 폴백·직접 입력)·[기본 모델로 저장] → differing 있으면 교체 질문 패널([전부 교체]/[개별 설정 유지]), 현재 기본값 + 4용도 요약 항상 하단 표기. `useTransition`으로 액션 호출(반환값 사용 위해 form action 대신 onClick). `ModelRoutingForm`(설정 화면)은 불변 — phase-0 링크 카드로 접근.
- **검증**: `npm test` **131/131**(순수 로직 신규 없음 — 배치 5는 UI·서버 액션), `tsc --noEmit`·`eslint`·`next build`(19라우트) 전부 통과.
- **특이사항/인계**: ① 문서와 코드 일치. 페이즈 섹션 래퍼는 클라이언트 컴포넌트로 두고(scrollIntoView 필요) 서버 컴포넌트 page가 children으로 서버 렌더 내용을 전달하는 표준 패턴 — [작업결과표 바로보기]는 `document.getElementById("worksheet")`로 직접 이동(별도 소자 파일 미분리, 래퍼에 인라인). ② `page.tsx`가 `ModelRouting`을 `@/lib/llm/types`에서 import(supabase/types 아님 — 캐스트 `project.model_routing as ModelRouting`). ③ default-model-picker는 액션 반환값(`differing`)이 필요해 form action이 아닌 `useTransition`+onClick으로 호출, 저장·교체는 `revalidatePath("/projects/${id}")`로 서버 재렌더(요약 갱신). ④ **배포 후 사용자 확인 항목**(브라우저 실동작): 페이즈 0→3→작업결과표 수직·각 섹션 [작업결과표 바로보기] 스크롤, 작업결과표 빈 점수/생기부 셀 클릭 → #phase-2/#phase-3 스크롤(배치 4 핸들러가 이제 동작 — 앵커 존재), 키 없음 계정 페이즈 0 인라인 등록, 기본 모델 저장 → 교체 질문 → [전부 교체] 후 설정 화면 4용도 일치. ⑤ 페이즈 1~3은 아직 링크 카드(구 페이지 살아 있음) — 구 라우트 제거는 배치 6(/ingest)·배치 7(/evaluate). /records·/profile·/submissions·/rubric·/settings는 세부 화면으로 유지.

### 2026-07-13 / 배치 6 (페이즈 1 수합 개편 — 업로드 분리 + 수합&매칭 연쇄 + OCR 드롭다운) / 완료

- **1단계 — `useSequentialRun` nextStage 연쇄**: 루프 본문을 내부 `runPlan(plan): Promise<RunOutcome>`으로 추출하고 선택 인자 `nextStage?: (r) => RunPlan | null` 추가. 첫 스테이지 정상 종료(!aborted) + nextStage 반환 계획이 있으면 `append("system","── 다음 단계 ──")` 후 두 번째 계획을 이어 실행(연쇄 1회·재귀 없음). lines는 초기화 없이 이어 붙고 progress는 스테이지마다 리셋, stop/pause는 ref 공유로 두 단계 모두 유효. `RunPlan`·`RunOutcome` 타입 export. 기존 소비자(채점 run-panel)는 nextStage 미전달로 무변경 동작(build로 확인).
- **2단계 — 서버 액션 개편(`ingest/actions.ts`)**: `ingestDocuments`·`ingestSpreadsheet` 제거, 신규 `listUploadedFiles`(storage.list(`${uid}/${pid}`, sortBy created_at desc) + submissions.storage_path로 ingested 판정)·`deleteUploadedFile`(수합분 throw로 INV-5 우회 금지·미수합만 remove)·`prepareIngest`(소유·존재 검증, prelude "수합 대상 N개"/0개 "매칭만")·`ingestOneFile`(throw 대신 {ok,message}: 시트는 mapping 필수+행 루프, 문서/이미지/PDF는 구 단일 파일 로직 이동+스캔 PDF filename 전달)·`finalizeIngest`("수합 완료 — 성공 N·실패 M"). 전 액션 `assertOwnedPath`(`${uid}/${pid}/` 접두) 소유 검증, 파일명은 `${uuid}__` 이후 복원. `saveOcrModel` 등 revalidate 경로 /ingest→프로젝트 홈. `previewSpreadsheet`·`persistSubmission`·`ocrExtract`·`parseSheet` 등 유지·재사용.
- **3단계 — OCR 동적 드롭다운(`ocr-model-select.tsx`)**: props를 `RoutableProvider[]`로 교체, 모델 후보=저장 목록 중 `isVisionCapableModel`(배치 1) 통과분, 비면 `VISION_MODELS` 폴백+"[모델 갱신] 안내", keySource=null은 비활성(회색)+"키 없음"(적용 버튼 disable+안내), [직접 입력…] 유지. 저장은 기존 `saveOcrModel`.
- **4단계 — 페이즈 1 패널(`components/projects/dashboard/phase1-panel.tsx` 신규)**: 배치 5 링크 카드 대체. 순서 = OCR 드롭다운·[수합 & 매칭]·진행 배지 → RunTerminal **상시 렌더** → 업로드 박스(Storage만·파싱 없음) → 업로드 파일 목록(체크박스+시각+수합됨 배지+[삭제]) → 확인 대기 배지+[매칭·확인 →]. 파일 기본 체크=미수합 checked·수합됨 unchecked, 삭제는 미수합만. [수합 & 매칭]=체크된 시트마다 `previewSpreadsheet`→`ColumnMapper` 순차 확정(취소 제외)→`useSequentialRun` start: stage1(prepareIngest→ingestOneFile→finalizeIngest) + nextStage로 stage2(prepareMatching→matchOneByLlm→finalizeMatching) 연쇄, stepOne마다 `emitWorksheetRefresh()`, 종료 시 파일 refetch+router.refresh. 대상 0개여도 실행(수합 스킵·매칭만). ref(targetPaths·mappings)로 훅 재생성 없이 값 전달.
- **5단계 — 구 화면 정리**: `/ingest/page.tsx`→`redirect("/projects/${id}#phase-1")`(actions.ts 폴더 유지), `staged-submissions.tsx`·`ingest-client.tsx`·`matching-panel.tsx` 삭제, `/submissions/page.tsx`에서 MatchingPanel·모델 배지 조회·maxDuration 제거(확인 큐·재귀속 전담으로 축소, 헤더 링크 `#phase-1`로). page.tsx가 `listUploadedFiles(id)`를 Promise.all에 추가해 initialFiles 전달, 페이즈 1 섹션을 `Phase1Panel`로 교체.
- **검증**: `npm test` **131/131**(순수 로직 신규 없음 — 배치 6은 훅·UI·서버 액션), `tsc --noEmit`·`eslint`·`next build`(19라우트) 전부 통과. grep 잔존 0: `ingestDocuments`·`ingestSpreadsheet`·`StagedSubmissions`·`IngestClient`·`MatchingPanel`(docs 제외).
- **특이사항/인계**: ① 문서와 코드 일치. ② `listUploadedFiles`는 "use server" 액션이지만 page.tsx(server component)의 Promise.all에서 직접 호출 — requireProjectOwner가 한 번 더 돌아 소유 재검증(중복이나 무해). ③ 실행 종료 후 갱신은 `useEffect([runState])`로 runState가 done/aborted 될 때 refetch+router.refresh(무한 루프 없음 — runState는 다음 start까지 done 유지). ④ 시트 열 매핑은 promise-resolver 패턴(`askMapping`이 previewSpreadsheet 후 Promise를 걸고 ColumnMapper의 확정/취소가 resolve)으로 순차 처리 — start() 전에 전부 확정. ⑤ **배포 후 사용자 확인 항목**(브라우저·LLM 과금): 업로드 시 토큰 미소모(네트워크 확인), 파일 체크/해제·삭제(수합분 거부), [수합 & 매칭] 한 번으로 수합→매칭 연쇄("── 다음 단계 ──"), 시트 열 매핑 흐름, 1건마다 작업결과표 갱신, 일시정지/재개/긴급 중단, 재실행 증분(중복 스킵·미매칭만), **통사 9반 스캔 PDF 재업로드→OCR 성공(배치 1 detach 수정 실증)**. ⑥ 배치 7은 페이즈 2(/evaluate 제거·루브릭 확장)·페이즈 3(생기부 일괄 생성 이식) — nextStage·phase1-panel 패턴을 참조 구현으로 쓸 수 있다.
