# 리팩토링 3 프로그레스 — 페이즈 2 실행 병렬화 + 진실성 스테이지 재편 + respread 계측

> 실행 지시서는 `docs/리팩토링_3.md`(v1 — [공통 프롬프트] + [배치 1~3 프롬프트] 구조). 이 문서는 **실제 진행의 누적 기록**이며, 배치 간 유일한 인수인계 통로다.
>
> **진행 방식**: 배치마다 **새 대화**에서 실행한다.
>
> **기록 규칙**: 배치를 끝낼 때마다 ① 아래 체크리스트 갱신 ② "진행 기록" 절에 `날짜 / 배치 / 결과 / 검증 출력 요약 / 특이사항(계획과 달라진 점·발견한 문제·다음 배치 인계)`를 **추가**한다(누적만 — 기존 기록 수정·삭제 금지).
>
> **배치 시작 전 확인**: 직전 배치가 체크리스트에서 완료 상태인지, 워킹 트리가 깨끗한지(`git status`) 확인. 어긋나면 작업을 멈추고 사용자에게 보고.

## 배치 체크리스트

- [x] **배치 1** — `useSequentialRun` 동시성(1~3) opt-in 확장 + 다단 연쇄 일반화 + 재시도(429) 계약 + 건별 소요 시간 로그
- [x] **배치 2** — 진실성 파이프라인 재편 — 식별(ISBN 정규화) → 팩트시트 준비(책 단위) → 대조, 3스테이지 분리
- [ ] **배치 3** — 페이즈 2 결선(동시성 선택 UI + 4스테이지 연쇄) + respread 계측 + 문서 정리.

## 진행 기록

### 2026-07-14 / 배치 1 / 완료

- **결과**: `useSequentialRun`의 동시성 및 다단 연쇄 구조 개편 완료.
  - `lib/hooks/run-pool.ts`로 스케줄링 코어를 순수 함수로 추출하여 React 독립적인 구조 마련 및 14건의 테스트 완료.
  - `use-sequential-run.ts`가 `runPool`과 `runPlanChain`을 래핑하도록 수정하여 기존 소비자 수정 없이 하위 호환성 유지.
  - `lib/llm/errors.ts`에 `LLMHttpError`를 신설하여 어댑터에서 HTTP 상태 코드를 유지하도록 변경.
- **검증 출력 요약**:
  - `npm test`: `tests/run-pool.test.ts`를 포함하여 14/14 테스트 전건 통과. (전체 테스트 111/111 통과)
  - `tsc --noEmit`: 오류 없음.
  - `eslint`: 0 에러, 0 경고.
  - `next build`: 컴파일 및 프로덕션 빌드 성공.
- **특이사항**:
  - `run-pool.ts` 내의 `nextStageFn` 부분에서 순환 참조 오류가 발생하여 명시적인 타입 단언 `((r: RunOutcome) => RunPlan | null) | undefined`를 추가하여 오류를 해결함.
  - 다음 배치(배치 2) 인계: `evaluate/actions.ts`의 진실성 액션 재편 및 `acquireFactsheet` 로직 스테이지 분리 진행 필요.

### 2026-07-14 / 배치 2 / 완료

- **결과**: 진실성 파이프라인을 식별(S1), 팩트시트 준비(S2), 대조(S3)의 3단계로 분리 구현 완료.
  - S1: `prepareSourceIdentify` 및 `identifySourceOne` 구현. 알라딘 검색과 `metaAgrees`를 이용한 ISBN 정규화 지원.
  - S2: `prepareFactsheetStage` 및 `prepareFactsheetOne` 구현. 고유 책 단위로 병렬/직렬 실행 시 중복 생성 차단.
  - S3: `verifyAuthenticityOne` 개편으로 팩트시트 생성/수집 로직을 제거하고 대조만 수행하도록 처리.
  - `phase2-panel.tsx`에서 S1, S2, S3, S4(채점) 4단 연쇄 적용 완료.
- **검증 출력 요약**:
  - `npm test`: `tests/authenticity.test.ts`에 추가한 8건의 신규 단위 테스트를 포함하여 전건 통과.
  - `tsc --noEmit` & `eslint` & Next.js 빌드 오류 없음.
- **특이사항**:
  - `verifyAuthenticityOne` 내의 생성 및 수집 경로가 완전히 제거되고, S2에서만 책 단위 팩트시트 확보 및 자동 생성이 수행됨.
  - 다음 배치(배치 3) 인계: 페이즈 2 UI에 동시성 선택(1~3건) 컨트롤 추가, 429 에러 status retryable 배선 마무리, respread 계측 기능 추가 및 문서 정리.
