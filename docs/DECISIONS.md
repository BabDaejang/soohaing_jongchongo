# DECISIONS — 설계 판단 기록 (약식 ADR)

> 형식: `날짜 / 판단 / 이유` 한 줄씩. 스펙에 없던 판단, 스펙 개정, 스택 선택 등을 기록한다.
> 최신 항목을 아래에 추가한다.

- 2026-07-07 / 4문서 체계(SPEC·CLAUDE·PROGRESS·DECISIONS)로 세션 간 일관성 유지 / 분리된 세션에서도 단일 진실 원천과 인계 절차가 있어야 제품이 일관되므로.
- 2026-07-07 / SPEC.md를 내용 없이 구조(템플릿)만으로 먼저 생성 / 세션 0 시점에 제품 정의가 아직 입력되지 않았고, 임의로 기능을 창작하면 SSOT의 신뢰가 깨지므로. 내용 확정 전까지 상태를 `초안 대기`로 표시.
- 2026-07-07 / SPEC.md를 사용자 제공 명세(0~11절)로 확정, DATA_MODEL.md를 5번째 필독 문서로 추가 / 제품 정의가 입력되었고, 스키마는 SSOT와 분리해 상세 관리하는 편이 세션 간 인계에 안전하므로.
- 2026-07-07 / 구현을 세션 1~8로 분할(1 스캐폴딩·DB → 2 인증 → 3 API 키 → 4 프로젝트 기초 → 5·6 Phase 1 → 7 Phase 2 → 8 Phase 3) / 의존성 순서(인프라→인증→키→도메인→파이프라인)를 따르고, 각 세션이 독립 검증 가능한 수용 기준을 갖도록. Phase 3은 분량상 8a/8b 분할 여지를 명시.
- 2026-07-07 / 합성 점수 기본값 = 평균(avg) / 스펙에 기본값 미명시. 제출물 수가 학생마다 다를 수 있어 합(sum)보다 공정하므로. `[확인 필요]` — 사용자 확인 대기.
- 2026-07-07 / 동점자 처리 기본값 = 상위 등급 부여 / 스펙에 기본값 미명시. 학생에게 유리한 방향이 교육 현장 관행에 가까우므로. `[확인 필요]` — 사용자 확인 대기.
- 2026-07-07 / 생기부 글자수 제한 기본값 = 500자(공백 포함) / 스펙에 기본값 미명시. 과목별 세부능력특기사항 관행(500자)을 따름. `[확인 필요]` — 사용자 확인 대기.
- 2026-07-07 / 원본 자동 삭제 기본값 = 끄기, 선택지 7일/30일 / 스펙에 값 미명시. 삭제는 비가역이므로 기본은 보수적으로 끔. INV-5(추출 승인 전 삭제 금지)가 자동 삭제보다 항상 우선. `[확인 필요]` — 사용자 확인 대기.
- 2026-07-07 / student_scores에 grade 컬럼을 저장하되 서버 재계산 배치 전용(RLS로 클라이언트 쓰기 차단), 등급제 토글의 즉시 반영은 rank+매핑표로 화면 파생 / INV-6("직접 저장·수정 금지")을 "시스템 파생 계산 결과의 스냅샷 저장은 허용, 사용자 수정 경로는 금지"로 해석. 토글 즉시 반영 요구(SPEC 6절)를 재계산 없이 충족하기 위해.
- 2026-07-07 / submissions.student_id를 NULL 허용으로 설계, NULL이면 평가·생성 컨텍스트에서 무조건 제외 / 매칭 확정 전 제출물을 잘못된 학생에 귀속시키지 않기 위한 보수적 설계(혼입 방지). match_status enum(auto_matched/pending_confirm/confirmed/update_pending)과 pending_content로 자동 덮어쓰기 금지를 구조화.
- 2026-07-07 / evaluations·student_scores·records(generated)·audit_logs의 쓰기는 service role 전용, 클라이언트 RLS로는 select만 허용 / 채점·등급·생성 결과를 클라이언트가 위조할 수 없어야 INV-3·INV-6과 감사 무결성이 성립하므로.
- 2026-07-07 / enum은 Postgres enum 타입 대신 text+check 제약 사용 / enum 타입은 값 추가·삭제 마이그레이션이 번거로워 초기 개발 단계에 부적합.
- 2026-07-07 / `[확인 필요]` 4건 확정: 합성 점수 기본값 평균, 동점자 상위 등급 부여, 글자수 제한 500자, 원본 자동 삭제 끄기 / 사용자가 제안값 그대로 승인. SPEC·DATA_MODEL에서 `[확인 필요]` 표시 제거.
- 2026-07-07 / 세션 구획 재편: 세션 1 = 스캐폴드+인증+가입 승인(마이그레이션은 profiles·app_settings만), 세션 2 = 관리자 패널 UI, 잔여 테이블은 해당 도메인 세션에서 마이그레이션 / 사용자 세션 지시에 따름. SPEC과 충돌 없음.
- 2026-07-07 / is_admin()·is_approved() 헬퍼를 세션 1에서 작성 (DATA_MODEL 원계획은 세션 2) / 세션 1의 RLS 정책(admin 전용 쓰기)에 즉시 필요하고, SECURITY DEFINER로 profiles 정책 재귀를 피해야 하므로.
- 2026-07-07 / 최초 관리자 지정 = 환경변수 ADMIN_EMAIL + OAuth 콜백에서 service role로 멱등 승격(role='admin', status='approved') / DB 트리거는 환경변수를 읽을 수 없고, 시드 SQL은 auth.users 생성 전에 실행 불가. 콜백 방식이 멱등·자동이므로.
- 2026-07-07 / 미들웨어 파일명을 proxy.ts로 채택 / Next.js 16이 middleware.ts를 proxy.ts로 개명(둘 다 동작). 신규 프로젝트이므로 현행 규약을 따름. 동작은 SPEC 2절 "미들웨어에서 강제" 그대로.
- 2026-07-07 / profiles에 클라이언트 insert 정책을 두지 않고 타입에서도 Insert: never / 프로필 생성 경로를 auth 트리거(handle_new_user) 하나로 한정해 위조 가입을 구조적으로 차단.
- 2026-07-07 / DB 행 타입은 interface가 아닌 type 별칭으로 선언 / postgrest-js의 Record<string, unknown> 제약은 암시적 인덱스 시그니처가 필요한데 interface는 이를 만족하지 못해 전 결과가 never로 추론되는 문제 확인.
- 2026-07-07 / 마이그레이션 적용은 supabase CLI `db push --db-url` 방식(연결 문자열은 .env.local의 SUPABASE_DB_URL, 커밋 금지) / psql 미설치 환경이고, CLI가 supabase_migrations 이력 테이블로 세션 간 증분 적용을 관리해 주므로. DB 검증용으로 pg를 devDependency에 추가.
- 2026-07-07 / 제공된 Supabase 프로젝트에 있던 이전 앱 테이블 4종(checkpoints, factsheets, projects, users) drop / 세션 4의 projects 테이블과 이름 충돌. 사용자가 "기존 테이블 삭제"를 명시 선택함.
- 2026-07-08 / 세션 2 범위 = 관리자 패널 + API 키 체계·crypto·LLM 통합 클라이언트(원 PROGRESS의 세션 2 + 세션 3 병합) / 사용자 세션 2 지시에 따름. SPEC과 충돌 없음. 잔여 세션 번호는 한 칸씩 당겨지지 않고 PROGRESS에 병합 표기.
- 2026-07-08 / audit_logs 테이블을 세션 2에서 도입(DATA_MODEL 원계획상 후속 세션 예정이었음) / 수용 기준 4(승인/거부/삭제가 audit_logs에 기록)를 충족하려면 이 세션에 필요. RLS: insert 정책 없음(=service role 전용), select는 admin, update/delete 없음(append-only).
- 2026-07-08 / callLLM 시그니처는 modelRouting({provider_id, model} per RoutingKey)를 인자로 받고, purpose 추출·매칭은 모두 extract 키를 공유 / 프로젝트 model_routing 테이블은 세션 4에서 도입되므로, 세션 2 시점엔 라우팅을 호출부가 주입하도록 설계. DEFAULT_MODELS(추출/매칭 haiku, 평가/생성/검증 sonnet-4-6)는 lib/llm/routing.ts 상수로 두어 세션 4 기본값 조립에 재사용.
- 2026-07-08 / resolveApiKey는 서비스 롤(admin 클라이언트)로 키를 조회하고 서버에서 복호화 / 기본 키(owner_id NULL)는 api_keys RLS상 admin만 select 가능하므로, 일반 교사가 기본 키를 사용하려면 서버 서비스롤 조회가 필수(INV-4: 복호화·평문은 서버 전용). 단위 테스트 가능성을 위해 fetchRows·decryptFn을 주입 가능하게 설계(기본값은 실제 구현).
- 2026-07-08 / api_keys 쓰기(개인·기본)는 사용자 세션 클라이언트(RLS 경로)로, 프로필 삭제와 audit_logs 기록은 service role로 수행 / 개인 키=본인·기본 키=admin RLS를 실제로 통과시켜 권한을 이중 검증. 계정 삭제는 auth.users 삭제가 필요해 service role 불가피, 감사 로그 insert도 service role 전용.
- 2026-07-08 / 서버 액션은 진입 시 requireAdmin()/requireApproved()로 권한을 한 번 더 강제(proxy.ts 경로 가드 + RLS와 함께 심층 방어) / 서버 액션은 경로와 무관하게 호출될 수 있으므로 미들웨어만으로는 부족.
- 2026-07-08 / 단위 테스트 러너 = node:test + tsx, `--conditions=react-server`로 실행 / 최소 의존성. server-only 패키지는 기본 export 조건에서 throw하므로 react-server 조건을 줘 empty 모듈로 해석시켜야 서버 전용 모듈을 테스트에서 로드 가능.
- 2026-07-08 / lib 구조는 crypto/·llm/ 디렉토리 채택(CLAUDE.md 디렉토리 계획), 세션 지시의 단일 파일(lib/crypto.ts·lib/llm.ts) 대신 / llm은 프로바이더 어댑터가 여러 개라 디렉토리가 자연스럽고 CLAUDE.md 구조와 일치. import 경로는 @/lib/crypto·@/lib/llm(index)로 동일하게 노출.
- 2026-07-08 / rejected 사용자 안내 문구는 이번 세션에서 별도 분리하지 않고 /waiting 공통 화면 유지 / 별도 문구 정책은 사용자 요청이 없어 보류. 필요 시 후속 세션에서 app_settings에 rejected_message 키 추가로 확장 가능.
- 2026-07-08 / (세션 4) projects에 description(text, nullable) 컬럼 추가, DATA_MODEL 5절 갱신 / 세션 4 지시가 "이름, 설명"을 명시. SPEC 4절은 컬럼을 열거하지 않아 충돌 없음. 스키마 SSOT(DATA_MODEL) 갱신 후 마이그레이션.
- 2026-07-08 / (세션 4) 세션 지시의 `student_no`는 DATA_MODEL 7절 `student_number`의 약칭으로 간주해 `student_number` 채택 / 컬럼명은 스키마 SSOT(DATA_MODEL)를 따른다.
- 2026-07-08 / (세션 4) Phase 2 전용 컬럼(students.score_override·override_reason, projects.needs_recalc)은 0003에서 제외하고 세션 7 마이그레이션으로 연기 / "각 세션에 필요한 테이블/컬럼만 추가"라는 세션 지시 준수. 세션 4엔 소비처(채점·재계산)가 없어 추가하면 강제되지 않는 dead schema가 됨. DATA_MODEL에 "세션 7 추가" 주석으로 시점만 명시(최종 스키마는 불변).
- 2026-07-08 / (세션 4) projects.model_routing은 SQL 컬럼 default 없이 not null만 두고, createProject 서버 액션이 buildDefaultModelRouting()으로 조립해 삽입 / Postgres 컬럼 default는 다른 테이블(providers)의 시드 provider_id를 서브쿼리로 참조할 수 없음. lib/llm/routing.ts의 DEFAULT_MODELS + 이름으로 조회한 anthropic provider_id로 앱에서 조립(세션 지시: 새 상수 만들지 말 것). DATA_MODEL 5절의 "default 시드값" 문구를 이에 맞게 수정.
- 2026-07-08 / (세션 4, 수용 기준 3) 프로젝트 삭제 시 하위 students·rubrics는 FK on delete cascade로 함께 삭제 / 학생·루브릭은 프로젝트 종속 데이터로 독립 존재 의미가 없고, 프로젝트별 독립(혼입 방지) 설계이므로 프로젝트 삭제 = 전부 삭제가 맞다. DATA_MODEL의 on delete cascade와 일치.
- 2026-07-08 / (세션 4) owns_project(uuid) SECURITY DEFINER 헬퍼를 0003에서 신설 / DATA_MODEL 공통 규약에 예정됐으나 미작성 상태였음. students·rubrics RLS가 projects 소유 여부를 참조할 때 정책 재귀·성능을 위해 definer 함수로 캡슐화(is_admin/is_approved와 동일 패턴).
- 2026-07-08 / (세션 6) 프롬프트 팩의 `match_status='auto'`→`auto_matched`, `extraction_method='manual'`→`source_type='manual'`로 정합, "귀속 경로 기록"은 신규 `match_method` 컬럼(0005)으로 구현 / 스키마 SSOT는 DATA_MODEL. 귀속 경로 감사(세션 9 INV 점검)에 명시 컬럼이 유용. 값: auto_number/auto_new_number/confirmed_existing/confirmed_new/manual.
- 2026-07-08 / (세션 6, 수용 1·2) 자동 귀속은 학번 존재 시로만 한정하는 순수 함수 classifyMatch로 구조화 — 이름만/식별값 없음은 무조건 pending_confirm / SPEC 5.2(b)(c) 동명이인 보호. 학번 없는 자동 귀속 코드 경로가 존재하지 않음을 단위 테스트로 고정. 신규 학번은 학생 자동 생성(SPEC 5.2-d) — 스프레드시트 매핑 학번에서만 raw_student_no가 채워져 신뢰 가능(문서/이미지는 raw 식별값 NULL→항상 확인 큐).
- 2026-07-08 / (세션 6) LLM 매칭 후보 제안은 매칭 실행이 아니라 확인 큐에서 지연(온디맨드) 실행 / 식별값 없는 문서·이미지가 다수일 때 매칭 실행마다 대량 LLM 호출을 피함. 자동 반영 없음(교사 확인 필수). 명단에 없는 id 제안은 무시(환각 차단).
- 2026-07-08 / (세션 6, INV-5) 원본 삭제 강제 지점은 서버 액션(deleteOriginal: extraction_approved_at 미승인 시 거부) + isPurgeEligible(미승인 항상 false) / Storage 삭제는 API 경로라 DB 트리거로 못 막음. 공유 storage_path(스프레드시트 1파일→다행)는 다른 제출물이 참조 시 객체 유지. N일 자동 삭제는 CRON_SECRET 보호 라우트(`/api/cron/purge-originals`), 실제 스케줄링은 운영 배선(Vercel Cron/pg_cron). proxy.ts는 /api/cron를 세션 가드에서 통과(자체 시크릿 인증).
- 2026-07-08 / (세션 5) 프롬프트 팩의 submissions 스키마를 DATA_MODEL(SSOT)로 정합 + 확장 2건 / 팩의 `submission_ref/extracted_text/extraction_method/reflect`를 DATA_MODEL의 `submission_key/content_text/source_type/include_in_eval·include_in_record`로 매핑. 매칭(세션 6)에 필요한 `raw_student_no·raw_student_name`를 신규 추가, 매칭 전 스테이징을 위해 `match_status`에 `'unmatched'`(기본값) 추가. DATA_MODEL 8절 갱신.
- 2026-07-08 / (세션 5) 세션 5는 학생 매칭을 하지 않고 모든 파싱 행을 student_id NULL·match_status='unmatched'로 스테이징 / 세션 지시("파싱된 제출물 후보를 스테이징하는 데까지, 매칭은 세션 6")·혼입 방지. 잘못된 자동 귀속 경로를 원천 차단.
- 2026-07-08 / (세션 5) 스캔 PDF·이미지 OCR은 프로바이더 네이티브 비전으로 처리(서버 PDF→이미지 렌더링·canvas 의존 없음) / 사용자 확정. lib/llm의 LLMMessage.content를 `string | LLMContentPart[]`로 확장하고 어댑터 3종에 image/document 블록 매핑 추가. 스캔 PDF는 anthropic document 블록으로 직접 전달(기본 extract=claude-haiku가 PDF·이미지 입력 지원). 텍스트 레이어 추출은 unpdf(서버리스 pdfjs).
- 2026-07-08 / (세션 5) OCR 담당 프로바이더·모델 선택기를 세션 5에 제공(신규 VISION_MODELS 카탈로그: api_format별 OCR 가능 모델) / 사용자 지시("회사+해당 회사 OCR 가능 모델을 선택"). 선택값은 프로젝트 model_routing.extract에 저장 → 세션 4에서 세션 7로 이월한 라우팅 편집기의 extract 키 부분을 세션 5에서 실현. 세션 7은 evaluate/generate/verify 담당. 카탈로그 없는 커스텀 프로바이더는 자유 입력 폴백.
- 2026-07-08 / (세션 5) Storage 경로에 submission_id 대신 파일 단위 uuid 사용(`{owner}/{project}/{uuid}__{name}`) / 스프레드시트 1파일→다행 구조라 submission_id를 경로에 쓸 수 없음. DATA_MODEL Storage 주석 갱신.
- 2026-07-08 / (세션 5) 중복 감지는 하드 unique 제약이 아닌 애플리케이션 로직(조회용 인덱스만) + decideDedup 순수 함수 / 내용 변경 시 기존 행 유지 + pending_content 스테이징(update_pending)이 필요해 unique 제약과 상충. dedup 결정은 단위 테스트 가능한 순수 함수로 분리.
- 2026-07-08 / (세션 5) xlsx=SheetJS(`xlsx`)에 알려진 취약점 advisory 존재하나 SPEC 1절 지정 라이브러리·서버 측 교사 업로드 파싱이라 채택, 세션 9 QA에서 재검토 / SPEC이 SheetJS를 명시. next.config.serverExternalPackages로 xlsx·mammoth·unpdf를 런타임 외부화(번들 회피).
- 2026-07-09 / (세션 7) 평가·점수 마이그레이션 번호는 팩의 0005가 아닌 **0006_evaluations_scores.sql** / 0005는 세션 6의 match_method가 선점. PROGRESS 세션 7 인계도 "0006 예정" 명시. 팩과 실제 진행의 번호 어긋남을 SSOT(실제 파일 순번)로 정합.
- 2026-07-09 / (세션 7) 팩 F절의 evaluations/student_scores 컬럼명을 DATA_MODEL(SSOT)로 정합 / 팩 `rubric_scores/score/raw_output`→DATA_MODEL `scores/total_score/raw_llm_output`(+project_id·is_current). override는 팩이 student_scores에 뒀으나 DATA_MODEL 7·10절대로 **students.score_override·override_reason**에 둔다(교사 개입은 학생 레코드 귀속 경로). student_scores엔 effective_score(=coalesce(score_override, composite_score))·grade 스냅샷.
- 2026-07-09 / (세션 7) evaluations에 content_hash 컬럼 신설(DATA_MODEL 9절 갱신) / 증분 재평가(수용 5)를 결정적으로 판정하기 위해 채점 당시 제출물 content_hash를 스냅샷. 현재 평가의 content_hash가 제출물 현재 해시와 같으면 재채점 스킵 — 반영 체크박스만 바뀐 경우(내용 불변)엔 재채점하지 않아 토큰 절약. updated_at 비교보다 정확(체크박스 토글이 updated_at을 바꿔도 내용은 그대로).
- 2026-07-09 / (세션 7) 합성 점수 "가중(weighted)" 정의 = 각 제출물 점수를 Σ(기준점수×기준weight)로 계산 후 학생의 제출물들에 대해 평균 / SPEC 6절이 공식을 명시하지 않음. DATA_MODEL 6절 "weight는 weighted일 때 사용"과 정합하는 유일 해석(weight 필드를 실제 사용). sum/avg는 total_score(기준 단순합)를 각각 합/평균. 사용자 확인(2026-07-09): 점수는 학생의 대략적 수준 지표이고 **상대 순위**가 목적이므로 완벽한 나열이 아니라 비슷한 수준 구분이면 충분(토큰 절약). 순위 변동은 제출물 점수 조정 또는 교사 override로 수행.
- 2026-07-09 / (세션 7) 순위 산출은 effective_score = coalesce(students.score_override, composite_score) 내림차순, 등급은 누적 백분율 파생 / 교사 override는 "B와 C 사이 점수"를 넣어 학생을 재배치하는 흐름(사용자 확인). 등급을 직접 저장·수정하지 않고(INV-6) 점수→순위→등급 파생. student_scores.grade는 재계산 배치의 스냅샷(service role만 write), 등급제(5/9) 토글 즉시 반영은 lib/grading.ts의 순수 함수 computeStandings로 배치·클라이언트가 동일 계산해 재계산 없이 파생.
- 2026-07-09 / (세션 7) 동점자: best_grade = 동점군 최상위 석차(min position)로 등급 파생(유리), mid_rank = 중간석차 p+(k-1)/2로 파생(NEIS 관행) / SPEC 6절 "상위 등급 부여/중간석차"를 백분율 입력값 차이로 구현. 배치·토글이 동일 순수 함수를 써 tie_break 무관하게 일관.
- 2026-07-09 / (세션 7) "재계산 필요"(needs_recalc) 설정은 DB 트리거로 구현(submissions insert/delete/content_hash·include_in_eval·student_id 변경 update, rubrics.criteria 변경) + updateProjectSettings의 score_aggregation/tie_break 변경 / 세션 5·6 액션 파일을 수정하지 않고(범위 밖 코드 미수정 원칙) 배지 요건을 충족하기 위해 트리거로 중앙화. 해제(false)는 재계산 배치가 담당. grading_scheme 변경은 파생 표시라 recalc 불필요(플래그 안 함).
- 2026-07-09 / (세션 7) evaluations·student_scores write는 service role(admin 클라이언트) 전용(INV-6), callLLM은 소유자 userId로 키 해석 / RLS는 두 테이블에 select 정책만 두고 insert/update/delete 정책 부재=authenticated 쓰기 불가(service role은 RLS 우회). 채점·순위·등급 위조 차단. override(students) write는 소유자 세션(RLS 통과)+감사, 이후 순위·등급 재계산만 service role.
- 2026-07-09 / (세션 7 이월분) model_routing 편집 UI의 모델 후보는 기존 VISION_MODELS 카탈로그를 재사용(자유 입력 폴백) / 팩 "새 LLM 클라이언트·상수 만들지 말고 기존 라우팅 구조 재사용" 준수. resolveApiKey가 routing.provider_id로 개인>기본 키를 고르므로 비-anthropic 프로바이더로 evaluate/generate/verify를 라우팅하면 해당 교사 개인 키가 사용됨.
- 2026-07-09 / (세션 8a) records·prompt_profiles 마이그레이션 번호는 팩의 0006이 아닌 **0007_records_profiles.sql** / 0006은 세션 7의 evaluations_scores가 선점. PROGRESS 세션 8a 인계도 0007 명시. 팩과 실제 순번의 어긋남을 SSOT(실제 파일 순번)로 정합.
- 2026-07-09 / (세션 8a) 팩 records 컬럼 명칭을 DATA_MODEL(SSOT)로 정합 / 팩 `edited_by_teacher boolean`은 `origin text check('generated','edited','manual')` + `is_current`로 실현(edited=교사 편집 버전). `sources`는 jsonb가 아닌 `uuid[]`. verification 항목에 `grounded_by_memo?`(교사 메모 근거)·`teacher_edited?`(교사 직접 수정) 필드 확장 — DATA_MODEL 11절 갱신. prompt_profiles의 팩 `guidance`→`guidelines`.
- 2026-07-09 / (세션 8a, INV-1/2) 생성 컨텍스트 조립 `buildStudentContext(studentId, source)`는 시그니처부터 단일 studentId만 받고, 학생 배열을 받는 생성 함수를 만들지 않는다 / INV-1을 코드 구조로 강제. 데이터 접근을 주입형 `ContextSource`로 분리(resolveApiKey 패턴)해 가짜 2명 데이터로 교차오염 단위 테스트. 실제 소스는 `.eq('student_id', studentId)`로만 조회(구조적 INV-2)하고, 순수 `filterRecordSubmissions`가 코드 레벨에서 한 번 더 방어(타 학생·미반영·미확정 제거). 컨텍스트 대상=반영(include_in_record)+매칭 확정(auto_matched/confirmed) 제출물만(혼입 방지, 평가와 동일 기준).
- 2026-07-09 / (세션 8a, INV-1) 일괄 생성은 클라이언트가 학생별 `generateRecord(projectId, studentId)`를 순차 단일 호출(진행률 표시) / 서버에 학생 배열 생성 함수를 두지 않아 INV-1을 구조적으로 보장. 배치 진행률을 클라이언트 루프에서 표시.
- 2026-07-09 / (세션 8a, INV-3) records generated 행 + verification 쓰기는 service role 전용(RLS insert 정책은 `origin in ('edited','manual')`만 허용), 교사 편집(edited)·수동본(manual)은 소유자 세션 RLS로 insert, is_current 관리 update는 소유자 / 생성·검증 결과 위조 차단(INV-3). DATA_MODEL 11절 "generated/verification service role, edited 소유자 허용" 준수. sources=생성 컨텍스트에 실제 사용된 제출물 id 배열.
- 2026-07-09 / (세션 8a) 교사 직접 수정·삭제 문장은 검증 재실행 없이 새 'edited' 버전 저장(`teacher_edited=true`로 표시, 하이라이트 제외), 문장 재생성 시에만 해당 문장 검증 재실행 / 사용자 확정(2026-07-09). 검증은 LLM 호출 비용이 있어 매 편집마다 재검증하지 않고, 교사가 책임지는 편집분은 표시만. 재생성은 `regenerateSentence`가 생성+검증을 다시 수행하되 DB엔 쓰지 않고 결과를 반환(클라이언트가 `saveRecordEdit`로 반영).
- 2026-07-09 / (세션 8a) 프롬프트 프로필 편집은 프로젝트 내 통합 화면 `/projects/[id]/profile`에서 계정 기본 + 프로젝트 오버라이드를 한 화면으로(계층 전환·적용 순서 표기) / 사용자 확정(2026-07-09). 생기부 생성이 프로젝트 스코프라 생성 맥락과 가깝고 실사용이 편함. SPEC 7.5 "계정별 관리"는 owner_id 소유·RLS로 충족.
- 2026-07-09 / (세션 8a) `docs/SEED_PROFILE.md` 신설(참고 6 + 금지 6) + `lib/prompts/seed-profile.ts`에 미러 / 팩이 지시한 "docs/SEED_PROFILE.md가 없으면 원 팩 L절 내용으로 생성"을 따르되, 원 팩 L절 원문이 저장소에 없어 SPEC 7.5가 명시한 특성(문체 '-함/-임/-됨', 성명·인칭 미표기 등)에 맞춰 작성. 로딩용 상수는 TS 파일, 사람용 사본은 md(둘 일치).
- 2026-07-09 / (세션 8a) 프로필 계층 병합 규칙: 계정 기본(base)을 먼저, 프로젝트 오버라이드(우선)를 뒤에 이어 붙여 모델이 최종 지침으로 읽게 함(`mergeProfileLayers`, source 태그로 UI 적용 순서 표기) / SPEC 7.5 "오버라이드 우선"을 순서(recency)로 구현. 배치·UI 공용 순수 함수로 단위 테스트.
- 2026-07-09 / (세션 8a) 예시 인제스트는 `analyzeExample`(쓰기 없음, 제안만 반환)과 `applyProfileSuggestions`(승인분만 upsert)로 분리 / SPEC 7.5 "자동 반영 금지"를 구조로 강제(수용 5). 분석 제안의 modify 대상 id는 실제 존재하는 항목만 유효(환각 id는 add로 강등, `parseSuggestions`).
- 2026-07-09 / (세션 8a) 생성·검증 프롬프트 템플릿을 `lib/prompts/`(generation·verification·example-ingest)에 파일 분리, 새 LLM 클라이언트 없이 `callLLM(purpose='생성'/'검증')` 라우팅 재사용 / 팩 지시(코드 인라인 금지, 기존 라우팅 재사용) 준수. 프롬프트는 `import type`으로만 서버 전용 context 타입을 참조(런타임 server-only 미유입).
- 2026-07-10 / (세션 8a 확장, 사용자 요청·승인) 프롬프트 프로필에 **버전 번호 + 업데이트 날짜시간 + 이력 열람·복원 + MD 내보내기/가져오기** 추가 / 사용자가 "권장/금지 항목이 md로 저장돼 편하게 확인·편집되고, 업데이트 시 버전·날짜시간이 표시되길" 요청. SPEC 7.5를 넘어서므로 제품 의뢰자 승인 후 SPEC 7.5에 근거 명시·이 항목 기록. 마이그레이션 `0008_prompt_profile_versions.sql`(prompt_profiles.version + prompt_profile_versions 이력 테이블). 세션 8b의 ui_layouts는 0009로 밀린다.
- 2026-07-10 / (세션 8a 확장) 프로필을 저장소 MD 파일로 두지 않고 **DB를 단일 진실 원천**으로 유지하되 MD 내보내기/가져오기(다운로드/업로드)로 파일 편집 경험 제공 / Vercel 런타임 파일시스템은 읽기 전용이라 앱이 repo MD를 못 씀. 또 프로필은 교사별·프로젝트별·RLS 보호 데이터라 단일 repo 파일로 표현 불가. `docs/SEED_PROFILE.md`는 시드 사본일 뿐. MD 왕복은 순수 `lib/records/profile-markdown.ts`(render/parse), 가져오기는 서버 재파싱(신뢰 경계)+미리보기 확인.
- 2026-07-10 / (세션 8a 확장) 프로필 버전 저장은 현재 상태(prompt_profiles) update + 이력(prompt_profile_versions) insert 2단계로, 트랜잭션 없이 현재 상태 먼저 확정 / supabase-js 다중문 트랜잭션이 번거롭고, 교사 본인만 자기 프로필을 편집해 경쟁이 사실상 없음. 이력 insert 실패는 현재 상태를 되돌리지 않고 로깅만(이력은 보조). 복원은 삭제 없이 새 버전으로 저장.
- 2026-07-10 / (세션 8a 확장) 편집 패널 로컬 상태를 서버 변경(가져오기·복원 후 새 버전) 시 재동기화하는데, `useEffect+setState` 대신 React 권장 "렌더 중 시그니처 비교 리셋" 패턴 사용 / 린트 규칙 `react-hooks/set-state-in-effect` 준수 + 타이핑 중(부모 미갱신) 클로버 방지. 시그니처=계층별 version|updated_at.
- 2026-07-10 / (세션 8a 확장, 사용자 지시) 예시 생기부 인제스트에 **파일 업로드**(txt/md/docx/pdf/xlsx/csv — 사용자가 Excel 포함 지시) 추가. 파일→서버 추출(`extractTextFromExampleFile`, 세션 5 lib/parsing 재사용)→입력창 채움→교사 확인 후 기존 분석·승인 파이프라인 / 새 파싱 라이브러리 없이 기존 파서 재사용. hwp는 스택 밖(SPEC 1절)이라 pdf/docx 변환 안내, 스캔 PDF는 OCR 비용 회피로 붙여넣기 안내(명시적 한국어 에러). 스프레드시트는 셀 평탄화(`spreadsheetToText`, 순수). next.config `serverActions.bodySizeLimit`를 10mb로 상향(기본 1MB — 제출물 업로드는 Storage 직행이라 무관), 파일 자체는 8MB 상한.
- 2026-07-10 / (세션 8b) ui_layouts 마이그레이션 번호는 팩의 0007이 아닌 **0009_ui_layouts.sql** / 0007(records_profiles)·0008(prompt_profile_versions)이 선점. PROGRESS 8b 인계도 0009 명시. 팩과 실제 순번의 어긋남을 SSOT(실제 파일 순번)로 정합.
- 2026-07-10 / (세션 8b) ui_layouts.layout jsonb 형태 = `{columnWidths: {student,grade,memo,record}, cells: {[studentId]: {memo:{mode,height?}, record:{mode,height?}}}}` (mode='collapsed'|'full'|'custom') / SPEC 8절 "열 너비·셀 표시 모드(접기/전체/커스텀 높이)·전체 토글"을 담는 최소 스키마. 순수 `lib/records/layout.ts`(normalizeLayout/setAllCellModes/withMode/clamp)로 저장·복원 양쪽에서 동일 새니타이즈. 저장 jsonb는 사용자 입력이라 읽을 때·서버 저장 시 항상 normalizeLayout으로 열 너비 클램프·stale studentId 제거. 전체 토글 상태는 별도 저장 대신 모든 행의 cells 모드로 실체화(setAllCellModes)해 영속.
- 2026-07-10 / (세션 8b) ui_layouts RLS는 DATA_MODEL 13절대로 `user_id = auth.uid()`(소유 축)만 두되, insert/update의 with_check에 `is_approved()`를 추가 / 0008 ppv_insert와 동일 패턴의 심층 방어. 승인 안 된 사용자의 쓰기를 차단하는 상위 조건이라 SSOT의 소유 축과 상충 없음. select/delete는 소유만.
- 2026-07-10 / (세션 8b) saveLayout은 소유자 세션(RLS 경로) upsert(onConflict user_id,project_id) + 서버 재정규화(신뢰 경계) / 클라이언트가 보낸 layout을 서버가 프로젝트 학생 목록 기준으로 normalizeLayout(열 너비 클램프·존재하지 않는 학생 셀 제거)해 저장. requireProjectOwner로 프로젝트 소유까지 재확인(ui_layouts RLS는 프로젝트 스코프가 아니므로 액션 가드로 보강). 디바운스(700ms)는 클라이언트.
- 2026-07-10 / (세션 8b) 생기부 셀 직접 수정은 세션 8a `saveRecordEdit`(origin='edited', version+1, 검증 배열 빈 값) 재사용 — 8b는 새 생성/편집 액션을 만들지 않음 / 팩의 `edited_by_teacher=true`는 origin='edited'로 실현(DECISIONS 2026-07-09). 내용이 실제로 바뀌고 비어 있지 않을 때만 blur에서 1회 저장(버전 남발 방지). 교사 메모 셀은 세션 4 `saveTeacherMemo` 재사용. 등급은 student_scores 스냅샷 + `lib/grading.computeStandings` 파생 표시(평가 화면과 동일 순수 함수, INV-6 — 저장·수정 없음).
- 2026-07-10 / (세션 8b) 마이그레이션 0009 원격 적용은 `.env.local`의 SUPABASE_DB_URL(직접 호스트, IPv6 전용)이 여전히 라우팅 불가라, 동일 비밀번호로 **IPv4 pooler URL을 구성**해 `supabase db push`(적용 성공) + pg(pooler 경유)로 스키마·제약(unique user_id,project_id)·RLS 4정책 검증 / 8a와 동일 경로. 행위 RLS는 롤백 트랜잭션 안에 임시 프로젝트를 만들어 실증: 소유자 upsert 1행·소유자 select 1행(복원)·타 계정 select 0행(격리)·타 계정 위조 insert 42501(차단). 테스트 데이터 미영속 확인.
- 2026-07-08 / (세션 4) 프로젝트 설정 화면에 용도별 model_routing 편집 UI는 세션 4에서 만들지 않고 기본값 자동 조립(buildDefaultModelRouting)까지만 구현. 실제 LLM 파이프라인이 붙는 후속 세션(추천: 세션 7 Phase 2)에서 프로바이더·모델 선택 목록과 함께 구현 / 라우팅 편집은 `{provider_id, model}` 선택이 필요한데 현재 선택 가능한 모델 카탈로그가 없어(모델명이 자유 문자열) UX를 제대로 만들려면 프로바이더·모델 목록 정비가 선행돼야 함. 기본값(anthropic haiku/sonnet)이 조립되어 파이프라인은 동작하므로 기능 공백은 없음. SPEC 4절의 '모델 라우팅' 설정 항목은 데이터·기본값은 충족되고 편집 UI만 후속 배치되는 것으로, 세션 9 QA 시 SPEC 커버리지 점검에서 이 위임을 확인할 것. resolveApiKey가 routing.provider_id로 키를 고르므로, 비-anthropic 개인 키를 쓰는 교사를 위해 라우팅 편집이 실질적으로 필요해진다(단순 장식 아님).
- 2026-07-10 / (세션 9) 세션 9(통합 QA + 불변조건 감사 + Vercel 배포 준비)를 계획에 복원 / 재편된 PROGRESS 계획에서 누락됐던 원 팩 S8. 혼입 방지(INV-1/2)·키 보호(INV-4)를 최종 점검하지 않고 배포하는 것은 위험하므로 되살림(프롬프트팩 I절 지시). 세션 7의 `ModelRoutingForm`으로 세션 4가 위임한 model_routing 편집 UI가 실현됐음을 확인(SPEC 4절 커버리지 충족).
- 2026-07-10 / (세션 9, 세션 5 이월) SheetJS(xlsx) advisory 재검토 결론 = **CDN 0.20.3 배포판으로 업그레이드(완화)** / 설치판 0.18.5에 high 2건 확인: 프로토타입 오염 GHSA-4r6h-8v6p-xvw6(CVSS 7.8, <0.19.3), ReDoS GHSA-5pgg-2g8v-p4x9(CVSS 7.5, <0.20.2). 노출면은 서버 측·승인 교사 업로드 파싱 한정(serverExternalPackages 외부화, 공개 입력 아님)이나 심각도 high이고, npm 배포는 0.18.5로 동결(수정본은 SheetJS CDN에만 존재, `npm audit fix` 불가)이라 위험 수용보다 업그레이드가 낫다고 판단(사용자 승인 2026-07-10). `package.json`의 xlsx를 `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`로 교체 → 두 advisory 해소, parseXlsx 테스트·typecheck·build 재통과. SPEC 1절(SheetJS 지정) 유지. npm audit 잔여는 next→postcss moderate(빌드툴 전이 의존, 세션 범위 밖 — Next 메이저 변경 필요).
- 2026-07-10 / (세션 9) QA/감사 방법: 자동 구간은 에이전트가 직접(typecheck/lint/build/test, .next/static 번들 스캔, pooler pg로 RLS 전수 점검, 실 LLM 라이브 1회), 브라우저 구간(OAuth 가입→승인)은 사용자 수행 체크리스트로 위임 / 승인 사용자 1명·2번째 Google 계정 필요·에이전트 브라우저 불가 제약. RLS 타 계정은 임의 uuid JWT 클레임 시뮬(세션 8a·8b 방식), 한계는 INVARIANTS_AUDIT.md 명시. 실 LLM은 service role 하니스로 실 lib 파이프라인(평가·생성·검증) 1회 구동 후 임시 데이터·등록 키 삭제(미영속). 결과는 SMOKE_TEST.md.
- 2026-07-10 / (세션 9) 원본 자동 삭제 Cron을 `vercel.json` crons로 배선(`/api/cron/purge-originals`, 매일 03:00 UTC) + `.env.example`에 `SUPABASE_DB_URL`(마이그레이션용) 추가 / 세션 6에서 라우트·CRON_SECRET Bearer 검증은 구현됐으나 스케줄 배선·환경변수 목록이 미문서화였음. Vercel은 CRON_SECRET 설정 시 이를 Bearer로 자동 첨부. 시드 3경로(ADMIN_EMAIL 콜백 승격/0002 프로바이더/ensureDefaultProfile)는 확인 결과 부족분 없음 — 새 시드 스크립트를 만들지 않고 README 운영 가이드에 문서화.
- 2026-07-10 / (세션 9) 라이브 검증에 사용한 OpenAI 키가 대화에 평문 노출되어 사용자에게 폐기(회전) 권고, 라이브 후 등록 키는 DB에서 삭제(미영속) / INV-4 원칙(평문 미보관)과 노출 사고 대응. 키는 커밋 파일·로그·audit에 남기지 않고 AES-256-GCM 암호문으로만 잠시 저장 후 제거.
- 2026-07-10 / (핫픽스, 사용자 보고 버그) 프로젝트 생성 폼 중복 제출 방지 — 코드베이스 표준 useFormStatus 패턴(제출 중 버튼 비활성 + "만드는 중…" 표시, settings-form의 SubmitButton과 동일 관용구) + 액션 래퍼 `handleCreate`의 useRef 동기 가드 2중 / 서버 액션(createProject) 응답 지연 중 피드백이 없어 연타 시 클릭 수만큼 프로젝트가 중복 insert되는 버그. pending 상태는 재렌더 후에야 버튼을 비활성화하므로 그 사이의 연타는 state로 막을 수 없어 ref(동기 check-and-set)로 액션 레벨에서 차단. 제출 중에는 취소도 비활성(폼 언마운트 후 서버 생성이 계속 진행돼 생기는 혼란 방지). 같은 파일의 수정·삭제 폼도 pending 미표시는 동일하나 중복 실행이 데이터 중복을 만들지 않아(동일값 update 멱등, 2번째 delete는 에러/0행) 이번 범위에서 제외 — 후속 세션에서 공용 SubmitButton 추출을 제안만 기록.
- 2026-07-10 / (모델 목록 동적 조회, 사용자 요청·SPEC 3절 확장 승인) 모델 후보를 정적 `VISION_MODELS` 카탈로그 대신 프로바이더 API에서 조회 / 정적 카탈로그는 필연적으로 낡는다(gpt-5·claude-opus-4-8 계열 부재). 키 등록 시 모델 목록 엔드포인트를 호출해 **키 유효성 검증을 겸하고**, 받은 목록을 저장한다. `lib/llm/models.ts`(서버 전용): anthropic `GET {base}/v1/models`(x-api-key + anthropic-version), openai `GET {base}/models`(Bearer), google `GET {base}/v1beta/models?key=`(supportedGenerationMethods에 generateContent 포함만). 마이그레이션 0010.
- 2026-07-10 / 모델 목록을 `providers`가 아니라 `api_keys.models jsonb`에 저장 / 같은 프로바이더라도 키(조직·요금제)에 따라 접근 가능한 모델이 다르다. `models_synced_at`으로 조회 시각 표시, [모델 갱신] 버튼으로 재조회(`refreshKeyModels` — 저장된 암호문을 복호화해 재조회, 평문은 반환하지 않음).
- 2026-07-10 / OCR 모델 선택기는 `VISION_MODELS` 정적 카탈로그를 유지하고 라우팅만 동적으로 전환 / OCR은 "비전 가능 모델"만 제시해야 하는데 능력 메타데이터를 주는 건 anthropic의 `capabilities`뿐이다. OpenAI `/v1/models`는 능력을 알려주지 않아 동적 목록에서 비전 가능 여부를 판별할 수 없다. 결과적으로 `VISION_MODELS`는 OCR 전용 + 라우팅의 폴백(0010 이전 키)으로만 쓰인다.
- 2026-07-10 / OpenAI 모델 필터는 프리픽스 허용(`gpt-`/`chatgpt-`/`o1`/`o3`/`o4`) + 부분문자열 제외 휴리스틱 / `/v1/models`가 임베딩·TTS·whisper·realtime·image·sora·moderation까지 전부 반환하고 능력 필드가 없다. 실제 응답 120개로 검증: 56개 제외 → 64개 통과. `-codex`·`-search-api`는 responses API 전용이라 chat/completions 어댑터로 호출 시 실패하므로 제외. 휴리스틱이므로 누락 대비 [직접 입력…] 폴백을 유지한다.
- 2026-07-10 / 라우팅 화면에서 쓸 수 있는 키가 없는 프로바이더는 목록에서 숨기고, 키가 하나도 없으면 전 프로바이더를 비활성(회색)으로 노출 + 계정 옵션 안내 / 사용자 지시. "선택은 되는데 실행하면 키 없음 에러"를 원천 차단. 사용 가능 여부 판정은 `lib/llm/available.ts`의 `listRoutableProviders`(resolveApiKey와 같은 개인>기본 우선순위, service role 조회, encrypted_key 미select).
- 2026-07-10 / `setPersonalKey`·`setDefaultKey`를 `useActionState` 시그니처로 변경(throw → 결과 반환) / 키 검증 실패(오타·권한 없음·네트워크)는 정상 경로다. `app/` 아래에 `error.tsx`가 없어 throw 시 오타 하나로 Next.js 기본 digest 에러 페이지가 뜨던 문제를 해소. 삭제 액션은 기존대로 throw 유지.
- 2026-07-10 / `providers` 조회 정렬을 `created_at` → `name`으로 변경(account·admin 페이지) / 시드 3행이 한 INSERT로 들어가 `created_at`이 동일해 표시 순서가 비결정적이었다. settings 페이지는 이미 name 정렬이었다.
- 2026-07-11 / (매칭 자동화, 사용자 요청·SPEC 5.2 개정 승인) 자동 귀속 범위를 "학번 일치"에서 "명단과 모호하지 않게 일치"로 확대 / 교사가 제출물을 하나씩 승인하는 부담을 덜기 위함. 자동 = 학번 완전 일치(auto_number) 또는 **이름이 명단에 정확히 1명만 일치**(auto_name). SPEC 5.2-b의 원래 취지(동명이인 보호)는 "2명 이상 일치 시 자동 금지"로 정확히 보존 — 유일 일치는 애초에 동명이인 상황이 아니다. 확인 큐에는 동명이인·명단 미일치·학번 충돌·식별 불가·내용 변경만 남긴다. tests/matching.test.ts의 "이름 1명 일치여도 자동 금지"(구 수용 2)를 반전.
- 2026-07-11 / 개별 파일(docx·pdf·이미지)의 식별값을 파일명×명단 교차 대조로 확보 / 기존 ingest는 개별 파일에 raw_student_no/name을 하드코딩 null로 넣어 전부 "식별 불가"로 분류됐다(자동화 대상 자체가 없던 근본 원인). 학교별 파일명 규칙을 가정하지 않고, **명단에 실재하는** 학번·이름이 파일명에 온전한 토큰(숫자 경계·한글 경계)으로 들어 있을 때만 인정 → "수행평가최종.docx"의 "수행평가"는 자동 탈락, "이서준" 파일이 "이서"에게 가지 않음. 서로 다른 학생 2명 이상 걸리면 포기(모둠 과제). 파일명으로 못 찾으면 문서 앞부분(1500자)을 callLLM(purpose='매칭')으로 읽어 명단의 학생 1명 지목, 명단에 없는 응답은 버림. 실행당 LLM 20건 상한·동시 4, 초과분은 재실행 유도.
- 2026-07-11 / 식별값 출처를 submissions.identity_source(column/filename/llm)에 기록(마이그레이션 0011) / 자동 귀속 범위를 넓힌 이상 교사가 "무엇이 어떻게 자동 처리됐는지" 훑어볼 수 있어야 한다. match_method(귀속 경로)와 별개 축. 화면에 배지로 표시하고, LLM 유래 자동 귀속은 앰버로 강조(가장 검토 필요). 신규 학생 자동 생성은 identity_source='column'에 한정 — 파일명·LLM 오타로 유령 학생이 생기는 것을 막는다(classifyMatch가 number_unknown으로 큐행). 신규 학번인데 이름이 명단의 다른 학생과 일치하면 학번 오타 의심(number_conflict)으로 큐행.
- 2026-07-11 / 재귀속(reassignSubmission) 추가 — 자동 귀속의 오류를 교사가 사후에 바로잡는 경로(match_method='reassigned') / 자동화를 넓히는 이상 되돌릴 방법은 선택이 아니라 필수 안전장치다. 학생별 제출물 화면의 [다른 학생으로 이동] select. audit_logs에 이전/이후 student_id 기록(submission.reassign). needs_recalc 배지는 기존 DB 트리거(0006)가 submissions.student_id 변경을 감지해 자동으로 세운다 — 채점·생기부 재계산 유도.
- 2026-07-11 / /submissions 라우트에 maxDuration=60 / runMatching이 미식별 제출물마다 LLM 1회 호출(최대 20건·동시 4)이라 Vercel 기본 함수 타임아웃으로 모자란다.
- 2026-07-11 / (리팩토링 배치 1, 1단계) OpenAI 어댑터가 모델 계열에 따라 파라미터를 분기 — gpt-5·o 계열(`isOpenAIReasoningFamily` 정규식 `^(gpt-5|o\d)`)은 `max_completion_tokens`만 보내고 `temperature`를 생략, 그 외(gpt-4o·커스텀 호환)는 기존 `max_tokens`+`temperature` 유지 / 2026-07-11 탐침 확정: 추론 모델은 `max_tokens`→400("Use 'max_completion_tokens'"), `temperature`(기본값 외)→400("Only the default (1)"). 채점 158건 전멸의 직접 원인. `buildOpenAIChatBody`·`isOpenAIReasoningFamily`를 순수 export 헬퍼로 분리해 단위 테스트(tests/openai-params.test.ts).
- 2026-07-11 / (리팩토링 배치 1, 1단계) OpenAI 어댑터가 빈 응답(content 공백)을 조용히 통과시키지 않고 throw / 추론 토큰이 한도를 소진하면 content가 비는데, 기존엔 빈 문자열이 파서에 흘러가 전 기준 0점으로 조용히 저장됐다. `finish_reason`을 담아 에러를 던져 배치 2 터미널·서킷 브레이커가 원인을 표시하게 한다.
- 2026-07-11 / (리팩토링 배치 1, 2단계) `OPENAI_EXCLUDE_PARTS`에 `-pro` 추가 + `listRoutableProviders`가 openai 프로바이더의 저장 모델 목록을 소비 시점에 `isOpenAIChatModel`로 재필터 / `-pro`는 responses API 전용이라 chat/completions에서 404(탐침 C). "선택 가능 = 호출 가능"을 보장하려면 카탈로그 필터(신규 [모델 갱신]분)와 소비 시점 재필터(낡은 저장 목록 방어) 두 겹이 필요. [직접 입력…] 폴백은 커스텀 프로바이더용으로 유지(오입력은 배치 2 서킷 브레이커가 잡음).
- 2026-07-11 / (리팩토링 배치 1, 3단계) Tongsa `model_routing`의 generate·verify를 `gpt-4o-mini`로 변경(provider_id 유지, extract·evaluate 불변) / 지시서 스냅샷은 generate·verify=`gpt-5.5-pro`였으나 실제 DB는 스냅샷 이후 사용자가 UI에서 4개 용도를 전부 `gpt-5.4`로 바꾼 상태였음(문서-실제 불일치, 실제 상태가 기준). 배치 1 어댑터 수정으로 `gpt-5.4`도 정상 호출되나, 생성·검증에 저비용·저지연 chat 모델을 쓰는 '사용자 확정' 결정(2026-07-11 재확인)을 적용. pooler 세션 모드+.cjs 스크립트로 조회→수정→재조회 검증.
- 2026-07-11 / (리팩토링 배치 2, 4단계) 실행 관찰가능성을 **클라이언트 구동 1건 단위 실행**(공용 `useSequentialRun` 훅 + 표시 전용 `RunTerminal`)으로 구현 — 서버 상주 잡·폴링·잡 테이블·DB 스키마 변경 없음 / 채점을 전건 한 서버 액션에서 돌리면 진행 표시·일시정지 불가 + 성공 시 타임아웃이 확정이었다(전멸 원인 ④). prepare(대상)→stepOne(1건)×N→finalize(재계산·감사·revalidate)를 클라이언트 루프가 구동해 건별 로그·진행·중단을 얻고, 서버는 무상태로 유지한다. pause/stop은 상태 지연을 피하려 ref 플래그로 루프에서 직접 읽고(pause 중 250ms 폴링 sleep), 연속 실패 `maxConsecutiveFailures`(기본 3)에서 서킷 브레이커로 자동 중단한다.
- 2026-07-11 / (리팩토링 배치 2, 4단계) `RunTerminal`을 채점 전용 명칭·로직 없이 표시 전용(props: lines·runState·progress·onPause·onResume·onStop)으로 분리 / 배치 3에서 매칭이 동일 컴포넌트를 재사용하려면 도메인 중립이어야 한다. 로그 창 크기 조절은 Tailwind `resize`(요구 ③), 로그 색은 level별(ok=emerald/error=red/system=amber/info=zinc), lines 변경 시 자동 스크롤. 실행 로직은 훅이 갖고 컴포넌트는 렌더만 한다(관심사 분리).
- 2026-07-11 / (리팩토링 배치 2, 5단계) `runEvaluation`(전건 단일 액션)을 제거하고 `prepareEvaluation`·`evaluateOne`·`finalizeEvaluation`로 3분할 / 터미널이 건별로 호출하는 구조. `evaluateOne`의 클라이언트 입력은 projectId·submissionId(**id뿐**)이고 루브릭·모델 라우팅은 서버가 DB에서 재조립한다(INV-2). 제출물 소속·자격(반영+매칭+귀속)을 재확인해 다른 프로젝트·미매칭 제출물을 차단하고, 채점 직전 현재 평가 해시를 재확인해 동시 탭 중복 채점을 방어한다(같으면 ok:true "이미 채점됨"). 증분 보존(skipped)은 prepare 시점 content_hash 비교로 산출해 targets에서 제외. revalidatePath는 finalize에서만(건별 호출마다 RSC 재조회 낭비 방지).
- 2026-07-11 / (리팩토링 배치 2, 5단계) `evaluateOne`은 인증 실패를 제외하고 throw하지 않고 `{ ok, message }`로 반환하며, LLM·삽입 실패 원문을 300자로 절단해 message에 담는다 / 전멸의 원인 ③(에러 메시지가 `catch {}`에서 버려짐)을 정면으로 고침. 터미널이 실패 원문("'max_tokens' is not supported…")을 error 색으로 표시하고 서킷 브레이커가 연속 실패를 센다(수용 1). 실패 message에 "실패:" 프리픽스를 붙이지 않는 것은 지시서 예시(예시 표기)보다 수용 기준(원문 노출)을 따른 것 — 대표 실패 사유는 클라이언트가 첫 실패 message를 모아 finalize로 넘겨 audit `sample_failure`(300자)에 기록한다. API 키 평문은 로그 라인·감사에 넣지 않는다.
- 2026-07-11 / (리팩토링 배치 2, 5단계) 평가 화면에 evaluate 라우팅의 모델 배지("평가 모델: {providerName}/{model}" + 설정 링크)를 표시하고 문구를 "temperature 0으로 결정적"→"temperature를 지원하는 모델은 0으로 고정(gpt-5 계열 미지원)"으로 수정 / 요구 ②(선택된 모델 화면 표시)와 배치 1 탐침 D(gpt-5 계열 temperature 0 미지원)의 외부 노출 문구 정합. 배지용 providerName은 page.tsx가 providers(id,name) 조회로 해석. evaluate 라우트 `maxDuration=120`은 1건 단위라도 finalize 재계산·조회 여유용.
- 2026-07-11 / (리팩토링 배치 3, 6단계) `runMatching`(전건 단일 액션 + LLM 20건 상한)을 `prepareMatching`·`matchOneByLlm`·`finalizeMatching`로 3분할하고 상한 장치(`LLM_BUDGET_PER_RUN`·`LLM_CONCURRENCY`·`mapPool`)를 제거 / 채점(배치 2)과 동일한 클라이언트 구동 1건 단위 실행 패턴. 결정적(열·파일명) 처리는 `prepareMatching`이 한 번에 끝내 prelude 로그로 돌려주고, LLM 추정이 필요한 건만 실행 터미널이 1건씩 호출한다 → 서버리스 타임아웃을 피하려던 20건 상한이 소멸(요구 ③)하고 재실행 유도 안내도 불필요. 재실행은 여전히 `match_status='unmatched'`만 대상이라 증분 안전.
- 2026-07-11 / (리팩토링 배치 3, 6단계) classify→DB 반영 로직을 `applyClassification` 헬퍼로 추출해 prepare(결정적)·matchOneByLlm(LLM) 두 경로가 공유 / 자동 귀속·신규 학생 생성·큐행의 DB 반영을 한 곳으로 모아 기존 3단계 동작을 그대로 보존. prepare는 파일명 대조(원본 명단 기준)를 전부 끝낸 뒤 반영해, 반영 중 생성된 학생이 뒤따르는 파일명 대조에 끼어들지 않게 한다(기존 runMatching의 1·3단계 분리를 유지). 신규 학생 자동 생성은 여전히 column 출처에 한정(유령 학생 방지).
- 2026-07-11 / (리팩토링 배치 3, 6단계) `matchOneByLlm`은 인증 실패를 제외하고 throw하지 않고 `{ ok, message }` 반환 — 자동 귀속·큐행 모두 ok:true("→ 결과"), LLM·DB 오류만 ok:false(300자 절단) / 서킷 브레이커가 진짜 오류만 세도록. 클라이언트 입력은 submissionId뿐이고 명단·라우팅은 서버가 DB에서 재조립(INV-2). 동시 실행 방어로 진입 시 `match_status='unmatched'` 재확인(이미 처리 시 ok:true "이미 처리됨"). API 키 평문은 로그 라인에 넣지 않는다(extractIdentityByLLM 재사용, 에러 메시지만 노출).
- 2026-07-11 / (리팩토링 배치 3, 6단계) 매칭 화면(matching-panel)을 `useSequentialRun`+`RunTerminal`로 개편, `LLM_NOTE`·"20건까지만 처리" 안내 삭제, extract 라우팅 모델 배지 표시 / 채점 run-panel을 참조 구현으로 동일 패턴. prepare의 prelude(건별 결정적 로그 + 요약)·finalize 로그가 기존 summary 문구를 대체. `submissions/page.tsx`가 model_routing+providers 조회로 배지("추출·매칭 모델: {provider}/{model}")를 조립(매칭은 추출과 같은 extract 키). maxDuration=60 유지(1건 단위).
- 2026-07-11 / (리팩토링 배치 3, 7단계) records 화면 헤더 아래 "생성 모델 / 검증 모델" 배지 한 줄 추가(라우팅+프로바이더명 조회) / 요구 ②(선택 모델 화면 표시). 일괄 생성이 없고 학생 1명=LLM 1회(INV-1)라 실행 터미널은 비적용 — 생성·검증 에러는 이미 검증 뷰에 표시되므로 배지만 추가.
- 2026-07-11 / (리팩토링 배치 4, 8단계) SPEC 6절 파이프라인의 "temperature 0" → "temperature를 지원하는 모델은 0 고정(미지원 추론 모델은 모델 기본값)"으로 완화, 실행 시점 항목에 채점 실행의 진행 터미널·일시정지/재개/긴급 중단·중단 시 처리분 반영·재실행 증분 이어감을 명문화 / 배치 1~3에서 외부로 드러나는 동작이 이미 바뀌었으므로(어댑터가 gpt-5 계열에 temperature 생략, 채점을 1건 단위 실행 터미널로 전환) SSOT에 근거를 남긴다. SPEC은 읽기 전용이나 이 리팩토링은 plan 모드에서 사용자 승인을 받았고 배치 4 지시서가 정확한 개정 문구를 지정한 것이라 그대로 반영. DATA_MODEL 변경 없음(스키마 불변).
- 2026-07-11 / (리팩토링 배치 4, 8단계) SPEC 5.2 확인 대기 큐 문단 뒤에 "매칭 실행은 1건 단위" 문단 추가 — 결정적 규칙(열·파일명)은 실행 시작 시 일괄, LLM 추출 건만 진행 터미널로 건별 실행·중단 가능, 재실행 시 미매칭만 증분 / 배치 3(runMatching 20건 상한 제거 + prepareMatching/matchOneByLlm/finalizeMatching 3분할)의 실제 동작을 SPEC에 명문화. 채점(6절)과 동일 취지 문장으로 정합.
- 2026-07-11 / (리팩토링 배치 4, 9단계) 최종 검증 = npm test 102/102·tsc·eslint·next build(19라우트) 전부 통과 + INV-4 번들 스캔(.next/static의 x-api-key·api.anthropic.com·generativelanguage·encrypted_key·APP_ENCRYPTION_KEY·createDecipheriv·SUPABASE_SERVICE_ROLE_KEY) 7종 전부 0건 / 배치 4는 코드 변경이 SPEC 문서뿐이라 회귀 없음을 재확인. 브라우저·실 LLM 구간(로그 흐름·일시정지/재개/중단·재실행 이어가기)은 OAuth·과금으로 에이전트가 못 하므로 PROGRESS 배포 후 사용자 체크리스트로 위임.
- 2026-07-12 / (리팩토링 2 준비) Google "The document has no pages"(400)의 원인 = `parsePdfText`(unpdf/pdf.js)가 입력 ArrayBuffer를 detach시켜 이후 스캔 PDF OCR이 빈 base64를 전송하는 것 — 탐침으로 실증(호출 후 bytes.length 328→0, `bytes.slice()` 전달 시 보존) / 모델 무관 재현이 증거(데이터 자체가 빈 것). 수정 전략은 사용자 선택으로 **라이브러리 경계 방어**(parsePdfText 내부 사본 + ocrExtract 빈 데이터 가드 + 회귀 테스트) 확정 — 모든 현재·미래 호출처를 한 번에 보호.
- 2026-07-12 / (리팩토링 2 준비) OpenAI 형식의 PDF 하드코딩 거부(`toOpenAIContent` throw)를 file 콘텐츠 파트(`{type:"file", file:{filename, file_data:"data:application/pdf;base64,…"}}`) 매핑으로 교체하기로 / 실제 OpenAI Chat Completions는 비전 모델에서 PDF 입력을 지원 — "OCR 가능한 모델이면 전부 가능해야 한다"는 사용자 지시.
- 2026-07-12 / (리팩토링 2 준비) 프로젝트 대시보드를 페이즈 0(준비)→1→2→3 수직 단일 작업 화면으로 재구성하고 실행 UI(터미널 포함)를 대시보드로 통합, /ingest·/evaluate는 앵커 리다이렉트로 제거(사용자 선택) / 실행 화면이 한 곳이어야 상태 분산·이중 유지보수가 없고 "빈 칸 클릭 → 해당 페이즈로 스크롤" UX가 성립. 매칭·확인(/submissions)·제출물 상세·루브릭(/rubric)·프로필(/profile)·문장 검증(/records)은 세부 화면으로 유지.
- 2026-07-12 / (리팩토링 2 준비) [작업결과표](8열 엑셀 시트형·전폭·정렬/필터/감춤/행높이/다운로드)가 기존 /results 결과 표와 /students 학생 명단을 흡수·대체(사용자 선택, SPEC 8절 개정 예정) / 표 두 벌의 이중 유지보수 제거. 학생 추가·수정·삭제는 [학생별 데이터 편집] 모달이 기존 students 액션을 재사용, ui_layouts 저장도 계승.
- 2026-07-12 / (리팩토링 2 준비) 999점 표시 점수 설계: 초기 확정 인원 = clamp(round(대상×0.25), 15, 25)(15명 미만이면 전원), 충족 전 student_scores 행 미생성(=점수 미확정, 컬럼 nullable 변경 불필요), 충족 시 원점수 순위로 800→200 등간격(≤41명이면 간격 ≥15 자동 보장), 이후 이웃 중간값 삽입(최상위=(최고+999)/2, 최하위=최저/2), 정수 소진 시 순서 보존 전체 재배치, 배정값은 재계산에도 유지(sticky) / 사용자 지시("좁은 점수 폭 때문에 상대 조정 불가")의 명문화. 등급은 여전히 effective(=override??display) 순위에서 파생(INV-6 불변). override는 0~999 정수로 재정의, 구 스케일 보정값은 0012에서 초기화.
- 2026-07-12 / (리팩토링 2 준비) model_routing jsonb에 선택 키 `default`(페이즈 0 기본 AI 모델)·`rubric`(루브릭 전담, 폴백 rubric??default??evaluate) 추가 — 마이그레이션 불필요, DATA_MODEL 5절 갱신은 배치에서 / 기존 `updateModelRouting`이 4키로 재조립해 통째 덮어쓰는 구조라 확장 키가 소실되는 잠재 버그 → 스프레드 보존으로 수정하는 단계를 배치 1에 포함.
- 2026-07-12 / (리팩토링 2 준비) OCR 모델 선택을 정적 `VISION_MODELS`에서 "키에 저장된 모델 목록 × `isVisionCapableModel`(형식별 휴리스틱: anthropic=claude-*, google=gemini-*, openai=gpt-4o/4.x/4-turbo/5/chatgpt-4o/o1·o3(미니 제외)/o4)" 동적 드롭다운으로 전환, 정적 카탈로그는 저장 목록 부재 시 폴백으로 강등 / "프로바이더로부터 받은 모델을 드롭다운으로" 사용자 지시. 리팩토링 1의 불가침 지정은 그 스코프 제한이었음을 지시서에 명시.
- 2026-07-12 / (리팩토링 2 준비) 수합→매칭 자동 연쇄는 `useSequentialRun`에 선택 인자 `nextStage`(finalize 후 다음 RunPlan 1회 연쇄, lines 보존) 최소 확장으로 구현 — 서버 상주 잡·잡 테이블 여전히 없음 / 업로드-즉시-파싱(토큰 즉시 소모)을 제거하고 [수합 & 매칭] 명시 실행으로 바꾸는 사용자 지시와, 매칭이 수합 결과에 의존하는 순서 제약을 한 터미널에서 충족.
- 2026-07-12 / (리팩토링 2 준비) 작업결과표 행 즉시 갱신은 window CustomEvent("worksheet:refresh") + 디바운스 refetch로 구현(Supabase Realtime 비채택) / 실행 루프가 같은 브라우저 탭에서 돌므로(1건 단위 클라이언트 구동) 로컬 이벤트로 충분하고, Realtime은 구독·정책 표면만 늘린다. 채점은 표시 점수가 finalize 재계산에서 산출되므로 finalize 후 갱신이 정확함을 지시서에 명시.
- 2026-07-12 / (리팩토링 2 준비) 다운로드 파일명 `프로젝트명-yymmddHHmm`(로컬 시간), 확장자는 사용자 예시 `.xls` 대신 `.xlsx` 채택 / SheetJS 기본·현대 엑셀 표준이며 한글 호환(csv는 BOM). md·csv·xlsx 3형식, 전체/선택 행 내보내기.
- 2026-07-12 / (리팩토링 2 준비) [설정] 카드는 페이즈 0(준비)에 배치 / 사용자가 루브릭(페이즈 2)·프로필(페이즈 3)·학생 명단(작업결과표 대체)과 달리 설정의 단계를 지정하지 않았고, 등급제·글자수·모델 라우팅은 작업 시작 전 준비 성격이므로.
- 2026-07-12 / (리팩토링 2 준비) 실행 지시서를 배치 8개(버그·기반→점수→표 코어→표 상호작용→대시보드·페이즈0→페이즈1→페이즈2·3→SPEC·최종)로 편성, 배치마다 새 대화 실행 + 공통 프롬프트에 배치 간 고정 계약(앵커 id·열 키·이벤트명·점수 상수·라우팅 키·파일명 규칙) 명문화 / 리팩토링 1의 검증된 워크플로 계승 + 하위 모델이 이어받을 때 이름 불일치(할루시네이션 최다 지점)를 차단.
- 2026-07-13 / (리팩토링 2 준비 v2) 페이즈 2에 진실성 검증 추가(사용자 지시): 채점 전 스테이지로 인용 출처 식별 → 도서는 도서팩트시트, URL은 원문 fetch와 대조 → 확인/의심/판정 불가/해당 없음 4상태 / "점수 부여에 앞서 진실성 파악" 지시의 구조화. 채점 프롬프트와는 분리(판정 기준이 다르고, 검증 실패해도 채점은 진행돼야 하며, 증분 로직이 따로 돌기 때문). 배치 6의 `nextStage` 연쇄를 재사용해 진실성→채점 2스테이지를 한 터미널에서.
- 2026-07-13 / (리팩토링 2 준비 v2) 팩트시트 수집 채널 = 오픈 API 직접 수집(사용자 선택): 알라딘 Open API(메타·목차·ISBN 확정) + 네이버 검색 API(블로그·뉴스·책 URL) + 원문 fetch, 키는 관리자 환경변수 3종(ALADIN_TTB_KEY·NAVER_CLIENT_ID/SECRET, 교사별 불필요) / LLM 웹서치 도구 대안은 원문 대조 검증이 약하고(스니펫만 제공) 검색마다 과금·어댑터 확장이 필요해 배제. 미설정 시 자동 수집만 비활성(수동·촬영본 보강은 동작).
- 2026-07-13 / (리팩토링 2 준비 v2) 팩트시트 무할루시네이션은 프롬프트가 아니라 **서버 필터로 강제**: LLM이 반환한 항목의 인용 스니펫(quote)이 수집 원문에 정규화 부분일치로 실존해야만 저장(`filterBySnippetMatch`), 12자 미만·부재는 폐기, 메타(목차·소개)는 알라딘 원본을 LLM 비경유 저장 / "절대로 할루시네이션으로 팩트시트를 만들지 말 것" 지시를 검증 패스의 환각 id 제거와 같은 구조적 방식으로 실현 — 프롬프트 준수에 의존하지 않는다.
- 2026-07-13 / (리팩토링 2 준비 v2) 진실성 '의심' 판정은 플래그+근거 표시만, 자동 감점·채점 제외·반영 해제 없음(사용자 선택) / CLAUDE.md 보수 원칙(애매하면 자동 처리 금지·교사 확인). 조치는 기존 수단(반영 체크 해제·점수 보정·재귀속)으로 교사가 결정. 오판(false positive) 시 학생 불이익이 자동 전파되는 경로를 만들지 않는다.
- 2026-07-13 / (리팩토링 2 준비 v2) 팩트시트 공유 모델(사용자 확정): 생성 계정 소유(private) → 교사 신청(pending_review) → 관리자 승인(shared, 전 계정 읽기 전용) / 승인 전 관리자 화면에서 AI 엄격 자동 검증(entry별 출처 재fetch → 스니펫 실존 결정적 대조 → LLM 뒷받침 판정 리포트, user_upload는 unfetchable로 육안 판단 위임). shared는 소유 교사도 수정 불가(RLS with check로 교사의 shared 전이 차단) — 오염 방지. 보강이 필요하면 내 계정으로 복제(forked_from 기록) 후 보강·재신청.
- 2026-07-13 / (리팩토링 2 준비 v2) 진실성 검증 대상 팩트시트 탐색 순서 = 내 소유 → shared → 자동 생성(private) / shared 재사용이 토큰 절약의 핵심 경로. shared가 부실해도 평가 중 자동 fork는 하지 않고 그대로 대조(부실 로그만) — 사일런트 복제 남발 방지. 작업결과표 8열 계약 불변 — 진실성 배지는 제출물 펼침 서브행·/submissions·페이즈 2 요약에 표시(열 추가 아님).
- 2026-07-13 / (리팩토링 2 준비 v2) 논문은 URL·DOI 명시분만 fetch 대조, 제목만 인용은 unverifiable로 정직 표시(비범위: 논문 전문 검색·구글 커스텀서치) / 검증 불가를 검증됨처럼 표기하지 않는 것이 혼입 방지 다음의 제품 원칙(할루시네이션 금지)과 정합. 촬영본 업로드 보강은 텍스트만 저장(원본 파일 Storage 미보관 — INV-5 표면 확장 방지). 배치는 8~11 신설·구 8은 12로 개명(마이그레이션 0013은 배치 8에서 스키마 일괄 확정, 이후 배치 스키마 불변).
- 2026-07-13 / (리팩토링 2 배치 1) PDF detach 방어를 `parsePdfText` 내부 `getDocumentProxy(bytes.slice())` 사본으로 구현 + `ocrExtract` 서두에 빈 dataBase64 가드(`throw "원본 파일 데이터가 비어 있습니다…"`) / 준비 단계에서 확정한 라이브러리 경계 방어를 이행 — 사본 전달로 detach 자체를 제거하고, 가드는 향후 다른 detach·다운로드 실패 시 프로바이더 400 대신 명확한 원인을 표시(이중 안전망). 회귀 테스트 `tests/parsing.test.ts`에 최소 PDF로 호출 후 bytes.length 보존·pages=1·isLikelyScan=true 검증 추가.
- 2026-07-13 / (리팩토링 2 배치 1) `toOpenAIContent`의 PDF throw 제거 → `{type:"file", file:{filename(기본 "document.pdf"), file_data:"data:application/pdf;base64,…"}}` 매핑, `LLMDocumentPart.filename?` 선택 필드 추가, 스캔 PDF `ocrExtract`가 `filename: f.filename` 전달 / OpenAI Chat Completions의 file 콘텐츠 파트 지원 반영(비전 모델 전 프로바이더 OCR). `tests/vision-content.test.ts`의 throw 기대를 file 파트 매핑 기대로 반전(기본값·전달값 2케이스). adapters/openai.ts·vision-models.ts 주석 갱신(로직 불변).
- 2026-07-13 / (리팩토링 2 배치 1) 비전 판별 순수 함수 `lib/llm/vision-capability.ts`(`isVisionCapableModel(format, modelId)`) 신설 — server-only 아님(클라이언트 select 필터 공용), openai는 gpt-4o/4.x/4-turbo/5/chatgpt-4o/o1·o3(미니 제외)/o4 휴리스틱, 확신 없는 모델은 제외(보수 원칙, [직접 입력] 폴백) / 배치 6의 OCR 동적 드롭다운 기반. `tests/vision-capability.test.ts` 14건(package.json test 목록 추가).
- 2026-07-13 / (리팩토링 2 배치 1) `ModelRouting`에 선택 키 `default?`·`rubric?` 추가, `updateModelRouting`이 저장 전 현재 model_routing을 조회해 스프레드 보존하도록 수정(4키만 재조립하던 통째 덮어쓰기 → 확장 키 소실 버그 제거). `saveOcrModel`은 이미 `...routing` 스프레드라 안전(확인만) / 준비 단계 판단의 이행 — 배치 5(default)·배치 7(rubric)이 설정 저장 때마다 소실되지 않게 기반을 먼저 놓는다.
- 2026-07-13 / (리팩토링 2 배치 2) 표시 점수 순수 모듈 `lib/scores/display.ts`(DISPLAY_MAX=999·SPREAD 800/200·MIN_INITIAL_GAP=15, initialConfirmCount·spreadScores·insertBetween·assignDisplayScores) 신설 — 미확정 국면은 빈 Map+confirmed=false, 초기 스프레드는 원점수 동점 그룹을 그룹 첫 위치 값으로 통일, sticky 유지 중 신규는 이웃 중간값 삽입, 정수 소진 시 순서 보존 전체 재배치(respread) / 준비 단계 설계(2026-07-12)의 이행. 등급·순위는 여전히 effective(=override??display)에서 파생(INV-6 불변) — 표시 점수만 다루는 순수 함수라 클라이언트·서버 공용 가능. 테스트 18건으로 경계(동점·최상/최하 진입·붙은 이웃 respread·탈락자·결정성) 고정.
- 2026-07-13 / (리팩토링 2 배치 2) `recomputeAndSave` 반환을 `number`→`RecomputeResult{ranked, pendingConfirm}`로 변경, 미확정(초기 확정 인원 미달)이면 student_scores 삭제만 하고 삽입하지 않음(=표시 점수 미확정을 "행 미생성"으로 실현, 컬럼 nullable 변경 불필요) / 사용자 지시 "인원 충족 전 점수 미확정·메모리에만 기록". totalTargets=제출물 귀속 학생 고유 수(override만 있는 학생 제외), rawRanked=평가 있는 학생 composite 내림차순(동률 studentId 사전순=결정성). effective=override??display로 재정의(구 override??composite에서 전환) — override는 이제 999 스케일. 호출부(finalize/recalculate는 결과 반환, override set/clear는 무시)·run-panel 메시지 분기 갱신.
- 2026-07-13 / (리팩토링 2 배치 2) override 검증을 정수 0~999로 좁힘(setScoreOverride 서버 + results-table 클라 동시), 표시 점수 스케일과 일치 / 좁은 루브릭 합산 스케일 폐기(0012가 구 보정값 초기화). evaluateOne 메시지 "원점수 N점"으로 명확화(확정 표시 점수는 finalize 재계산 산출). 현행 results-table은 배치 7에서 작업결과표로 대체 예정이라 헤더 relabel·기본값·display_score 컬럼 추가만(최소 수정) — 표시 점수 UI 전면은 배치 3~5.
- 2026-07-13 / (리팩토링 2 배치 2) 마이그레이션 0012는 `supabase db push` 대신 pg .cjs raw SQL로 직접 적용 — dry-run에서 원격 히스토리에 0010·0011이 미기록(prior 배치가 out-of-band raw 적용)이라 db push가 이미 존재하는 컬럼 재적용으로 실패하기 때문. 실측으로 api_keys.models·submissions.identity_source 컬럼 존재·override 0행 확인 후 적용, display_score integer/nullable+주석 재조회 검증 / 리팩토링 1의 DB 접속 관행(pooler 치환·.cjs)과 정합, 코드=진실 원칙.
