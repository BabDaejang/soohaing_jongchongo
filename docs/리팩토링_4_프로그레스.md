# 리팩토링 4 프로그레스 — 수합 학생 발견(명단 부트스트랩) + 평가 리뷰·수정 + 생기부 브리프(MD) + IA 재편

> 실행 지시서는 `docs/리팩토링_4.md`(v1 — [문제 진단] + [공통 프롬프트] + [배치 1~7 프롬프트] 구조). 이 문서는 **실제 진행의 누적 기록**이며, 배치 간 유일한 인수인계 통로다.
>
> **진행 방식**: 배치마다 **새 대화**에서 실행한다. 지시는 *"docs/리팩토링_4.md의 공통 프롬프트를 따르고 배치 N을 실행해"*.
>
> **기록 규칙**: 배치를 끝낼 때마다 ① 아래 체크리스트 갱신 ② "진행 기록" 절에 `날짜 / 배치 / 결과 / 검증 출력 요약 / 특이사항(계획과 달라진 점·발견한 문제·다음 배치 인계)`를 **추가**한다(누적만 — 기존 기록 수정·삭제 금지).
>
> **배치 시작 전 확인**: 직전 배치가 체크리스트에서 완료 상태인지, 워킹 트리가 깨끗한지(`git status`) 확인. 어긋나면 작업을 멈추고 사용자에게 보고.

## 배치 체크리스트

- [x] **배치 1** — 전역 내비게이션 신설 + IA 재편(팩트시트 1급 목적지·페이즈 2 '(선택)' 표기). 데이터·파이프라인 무변경.
- [x] **배치 2** — 수합 학생 발견(discovery) 스테이지 — 명단 없이 식별값 추출 + 토큰 실존 검증 → 교사 일괄 승인 로스터 부트스트랩(마이그레이션 없음)
- [x] **배치 3** — 작업결과표 허브화 — 미귀속 배너·매칭 배지·[학생 이동]·수정 즉시 피드백
- [x] **배치 4** — 평가 리뷰·수정 — 기준별 채점 결과 표시 + 교사 수정(origin='teacher') + 목표 등급→보정 점수 헬퍼 (마이그레이션 `0014_evaluations_origin.sql`)
- [x] **배치 5** — 생기부 작성 브리프(MD) — `brief_md` 컬럼·웹 편집·생성 프롬프트 주입·프롬프트 미리보기 (마이그레이션 `0015_prompt_profile_brief.sql`)
- [x] **배치 6** — 브리프 AI 협업 작성 — 초안 생성·다듬기(자동 반영 금지)
- [x] **배치 7** — SPEC/DATA_MODEL 개정 + 코드 정리(기존 `any`) + 최종 검증 — **리팩토링 4 완료**

## 진행 기록

### 2026-08-20 / 배치 1 / 완료

- **결과**: 전역 헤더 내비게이션 신설 및 IA 재편 완료(P-5·P-6). **데이터·파이프라인·스키마 무변경**.
  - `lib/nav.ts` 신설 — 순수 함수 `navRoleFor(profile)`(비로그인·미승인 → `null`) + `navLinksFor(role)`(`null`→빈 배열 / user→3링크 / admin→4링크). 공용 상수를 스프레드로 복사해 반환 배열 변형이 다음 호출에 새지 않는다.
  - `components/global-nav.tsx` 신설 — 서버 컴포넌트. 세션 + `profiles(role,status)` 조회 → `navLinksFor(navRoleFor(...))`가 빈 배열이면 `null` 반환(로그인·대기 화면 무내비). 승인 사용자에게 상단 고정(`sticky top-0 z-40`) 헤더: 좌측 앱명(홈 링크) / 우측 프로젝트·팩트시트·계정(+admin이면 관리자) + 기존 `SignOutButton` 재사용. 스타일은 대시보드 neo-brutalism 관용구(`border-2/4 border-black`·`shadow-neo-sm`·`bg-neo-muted`) 그대로.
  - `app/layout.tsx` — body 최상단에 `<GlobalNav />` 렌더(주석 1줄). 그 외 무변경.
  - `app/projects/[id]/page.tsx` — 페이즈 2 제목 "평가 **(선택)**" + desc에 "이 단계는 선택입니다 — 평가 없이도 페이즈 3에서 생기부를 생성할 수 있습니다." 추가. 페이즈 3 desc에 "평가를 건너뛴 경우 점수·등급 없이 제출물·메모만으로 생성됩니다." 추가.
  - `components/projects/dashboard/phase2-panel.tsx` — 팩트시트 링크 문구 "도서팩트시트 →" → "도서팩트시트 **관리(전역)** →"(링크·스타일 불변).
  - `app/factsheets/page.tsx` — 부제에 "계정 단위로 관리되며 모든 프로젝트에서 함께 쓰입니다." 한 줄 추가(위상 명시).
  - `tests/nav.test.ts` 신설 6건 + `package.json` test 스크립트에 등록.
- **회귀 방지 코드 확인(수정 아님 — 2단계 2항)**: **생기부 생성은 평가와 무관하게 동작한다.**
  - `app/projects/[id]/records/actions.ts` 전체에 `student_scores`·`evaluations` 참조가 **0건**(grep 확인). `prepareRecordRun`의 대상 선정 기준은 "반영(include_in_record)·매칭 확정 제출물 ≥ 1건 **또는** 교사 메모 존재"뿐이다.
  - `generateRecord`는 `buildStudentContext` → `callLLM(생성)` → `callLLM(검증)` → records insert 경로이며 점수·등급을 읽지 않는다.
  - `lib/records/context.ts`의 `StudentContext`에 점수·등급 필드 자체가 없다(submissions·teacherMemo·guidelines·prohibitions·charLimit·countMethod). 따라서 페이즈 2를 건너뛰어도 페이즈 3은 온전히 동작한다 — UI 표기(P-5)는 코드 현실을 반영한 것이지 새 동작이 아니다.
- **검증 출력 요약**:
  - `npm test`: **224/224 통과**(신규 `tests/nav.test.ts` 6건 포함, 8 suites).
  - `npx tsc --noEmit`: 오류 없음.
  - `npx eslint`: **14 problems (11 errors, 3 warnings)** — 전부 **기존 코드**의 것(`app/factsheets/actions.ts`·`worksheet-actions.ts`·`column-mapper.tsx`·`confirm-queue.tsx`·`student-submissions.tsx`·`lib/worksheet/assemble.ts`·`submissions/page.tsx` 등 `no-explicit-any`·`no-unescaped-entities`). `git stash`로 HEAD만 두고 다시 돌려 **동일하게 14 problems**임을 대조 확인 — 이번 배치가 추가한 오류·경고는 **0건**이다. 정리는 지시서대로 배치 7 소관.
  - `npx next build`: 성공(21 라우트).
  - **런타임 확인**(`npm run dev` + 브라우저): `/login`에서 `document.querySelectorAll('body > header').length === 0` — 비로그인 화면에 전역 내비가 렌더되지 않음을 실제로 확인했다. 콘솔 오류 0건. 승인 사용자 화면은 Google OAuth 로그인이 필요해 에이전트가 확인할 수 없다(아래 사용자 체크리스트).
- **특이사항**:
  - **eslint가 이미 실패 상태였다.** 리팩토링 3 종료 시점 기록은 "0 에러, 1 경고"였으나 이후 커밋(`6cfe393` 선택 도서 파이프라인, `5438feb` 팩트시트 엑셀 다운로드)에서 11개 오류가 유입됐다. 배치 종료 절차의 "eslint 통과"를 문자 그대로 만족시킬 수 없어, **이번 배치가 오류를 늘리지 않았음을 HEAD 대조로 입증**하는 것으로 대체했다. 배치 7은 이 11건(+3 경고)을 실제로 정리해야 한다.
  - **`/_not-found`가 static(○) → dynamic(ƒ)으로 바뀌었다.** 루트 레이아웃의 `GlobalNav`가 `cookies()`를 읽으므로 전 라우트가 동적 렌더가 된다. 나머지 20개 라우트는 **이전에도 전부 ƒ**였으므로(HEAD 빌드 대조) 실질 영향은 404 페이지 1건뿐이다. 앱 전체가 인증 게이트 뒤에 있어 정적 캐싱 이점이 없는 구조라 수용했다.
  - **지시서와 코드 차이**: 지시서 3단계는 `navLinksFor(role: string | null)` 하나만 명시했으나, 그 시그니처만으로는 "미승인 → 빈 배열"을 비로그인과 구분해 검증할 수 없다. 계약 이름·시그니처는 그대로 두고 상태 판정만 `navRoleFor(profile)`로 분리해 4개 분기를 각각 테스트했다(순수 함수 2개, 과설계 회피).
  - **유지한 것(회귀 방지)**: 각 페이지의 기존 상단 내비(`app/page.tsx`의 계정 옵션·관리자 패널·로그아웃, 프로젝트 대시보드의 "← 프로젝트 목록", `/factsheets`의 "홈")는 지시서대로 **제거하지 않았다**. `/factsheets` 상단의 "홈" 링크는 프로젝트 종속 요소가 아니라 정리 대상이 아니라고 판단했다(전역 내비와 중복이지만 무해).
  - **다음 배치(2) 인계**: 이 배치는 `Phase1Panel`·`submissions/actions.ts`·`lib/matching.ts`를 **일절 건드리지 않았다**(지시서 인계 사항 준수). 배치 2는 깨끗한 상태에서 발견 스테이지를 얹으면 된다. 전역 내비는 `sticky top-0 z-40`이며 body 직계 자식이라 기존 `z-40` 사용처(`worksheet-table.tsx`의 전체 화면 모달, `student-submissions.tsx`의 드롭다운)보다 DOM 순서상 앞에 온다 — 동일 z-index에서는 뒤에 오는 `main` 쪽이 위로 그려지므로 모달이 내비를 덮는다(의도한 동작).

#### 사용자 확인 필요 (에이전트 미수행 — Google OAuth 로그인 필요)

1. **전역 내비 표시**: 승인 계정으로 로그인 → 프로젝트 목록·프로젝트 대시보드·`/factsheets`·`/account`(·admin이면 `/admin`) 어느 화면에서든 상단 고정 헤더가 보이고, 각 링크로 한 클릭 이동되는지.
2. **무내비 화면**: 로그아웃 후 `/login`, 그리고 미승인(pending) 계정의 `/waiting`에 헤더가 **뜨지 않는지**(`/login`은 런타임 확인 완료, `/waiting`은 미승인 계정 필요).
3. **관리자 링크 분기**: 일반 교사 계정에는 [관리자]가 없고 admin 계정에만 보이는지.
4. **페이즈 표기**: 프로젝트 대시보드 페이즈 2 제목이 "평가 (선택)"이고 안내 문구가 보이는지, 페이즈 3 안내에 평가 미실행 시 문구가 보이는지.
5. **스크롤 겹침**: 대시보드를 아래로 스크롤할 때 고정 헤더가 작업결과표 조작(정렬 드롭다운·모달)을 가리지 않는지.

### 2026-08-20 / 배치 2 / 완료

- **결과**: 수합 실행에 **발견(discovery) 스테이지**를 넣어 명단 없이도 학생을 찾고, 교사 일괄 승인으로 명단을 만드는 로스터 부트스트랩을 구현했다(P-1). **마이그레이션 없음** — 기존 `raw_student_no/raw_student_name/identity_source`만 재사용.
  - **순수 함수(`lib/matching.ts` 확장, 기존 함수 무수정)**:
    - `extractIdentityCandidatesFromFilename(filename)` — 최대 연속 구간 토큰화로 4~6자리 숫자가 **정확히 1개**일 때만 학번 채택(2개면 포기), 한글 2~4자 토큰에서 상용어(`FILENAME_STOPWORDS` 24개) 제외. 숫자·한글의 "온전한 토큰" 경계는 최대 구간이 자연히 보장한다(`containsToken`과 같은 규칙, 1234567은 후보 아님).
    - `verifyIdentityTokens(head, extracted)` — LLM이 뽑은 값이 **문서 앞부분에 실존하는 토큰일 때만** 통과(팩트시트 `filterBySnippetMatch`와 같은 구조적 할루시네이션 차단).
    - `aggregateDiscoveredIdentities(rows, roster)` — 학번 우선·이름 폴백 그룹핑, 대표 이름은 최다 등장(동수면 가나다), 같은 학번에 다른 이름이 모이면 `conflict`, 명단에 이미 있는 학번·이름은 제외, 학번 우선 정렬.
  - **서버 액션(`submissions/actions.ts`)**: `prepareDiscovery` · `discoverOne` · `finalizeDiscovery` · `listDiscoveredStudents` · `createDiscoveredStudents`. `discoverOne`은 **`student_id`·`match_status`를 일절 쓰지 않고** raw 식별값만 채운다 — 귀속은 뒤따르는 매칭의 몫이고, 학생 생성은 오직 `createDiscoveredStudents`(교사 승인)뿐이다.
  - **실행 체인**: 수합(ingest) → **발견(discovery)** → 매칭(matching) 3스테이지(`RunPlan.nextStage` 중첩, 상한 5 이내). 발견 stepOne도 `emitWorksheetRefresh()` 호출.
  - **검토 UI**: `components/projects/dashboard/discovery-review.tsx` — 기본 전체 선택 체크박스, 이름 충돌 시 라디오 선택, 제출물 수·출처 배지(열/파일명/LLM), 승인 후 "학생 N명 추가 · 제출물 M건 귀속" 인라인 피드백 + `emitWorksheetRefresh()` + `router.refresh()`. Phase1Panel에 "명단 검토 대기 N명" 배지 추가.
- **검증 출력 요약**:
  - `npm test`: **246/246 통과**(신규 `tests/discovery.test.ts` **22건** 포함 — 지시서 요구 ≥12건 초과). 기존 `tests/matching.test.ts` 전건 통과 = `classifyMatch` 불변 증명.
  - `npx tsc --noEmit`: 오류 없음. `npx next build`: 성공.
  - `npx eslint`: 기존 14 problems(11 errors) 그대로 — **이번 배치가 추가한 것 0건**(변경 파일 4개 전부 무경고 확인).
  - **PostgREST 문법 오프라인 검증**: 타입체크로는 잡히지 않는 필터 문자열을 더미 클라이언트로 조립해 URL을 확인했다 — `source_type=not.in.(xlsx,csv)` · `or=(raw_student_no.not.is.null,raw_student_name.not.is.null)` 둘 다 정상.
- **특이사항**:
  - **지시서 ↔ 코드 불일치 ①(발견 대상 상태)**: 지시서 2.1은 대상을 `match_status='unmatched'`로 한정했지만, 3.4는 "다인용 PDF의 모호 페이지가 발견 스테이지에 자연 편입된다"고 적었다. 실제 코드(`ingest/actions.ts`의 `ingestMultiPagePdf`)는 모호 페이지를 **`pending_confirm` + raw NULL**로 넣으므로 'unmatched'만 보면 편입되지 않는다. 게다가 지시서 2.3의 `listDiscoveredStudents`는 이미 `IN ('unmatched','pending_confirm')`이다. 두 지시가 모순이라 **3.4의 명시된 목표를 만족하는 쪽**으로 통일해 `DISCOVERY_STATUSES = ['unmatched','pending_confirm']` 상수를 세 액션이 공유하게 했다. 발견은 raw만 채우므로 pending 행을 대상에 넣어도 큐 상태·귀속에 영향이 없다(정보만 추가).
  - **지시서 ↔ 설계 판단 ②(집계 제외 규칙 확장)**: 지시서는 "명단과 **모호하지 않게** 일치하는 그룹 제외"였으나, 구현은 **명단에 학번이 있거나 이름이 있으면(동명이인 포함) 제외**하는 더 넓은 규칙을 썼다. 이유: 검토 표는 기본 전체 선택이라, 동명이인 후보가 섞여 있으면 교사가 그대로 승인해 **같은 이름의 중복 학생**이 생긴다. 발견의 목적은 *신규* 학생 발굴이고, 명단에 이미 있는 이름의 귀속은 확인 대기 큐(`attributeExisting`/`attributeNew`)가 담당한다 — CLAUDE.md 보수 원칙("애매하면 자동 처리하지 않고 교사 확인 큐로")에 맞춘 선택. 테스트로 고정했다.
  - **`applyClassification` 재사용 안 함**: 지시서 2.4는 재사용을 허용했으나, 이 헬퍼는 `classifyMatch`를 타서 **column 출처 신규 학번이면 학생을 자동 생성**한다. 교사 승인 게이트를 세우는 액션 내부에서 그 경로가 도는 것은 취지에 어긋나므로, 재귀속은 "학번 완전 일치 또는 이름 유일 일치"만 반영하는 별도 결정적 루프로 구현했다. **기존 호출부·시그니처는 무변경**.
  - **`finalizeDiscovery` 신설(공유 계약에 없던 이름)**: `RunPlan`은 `finalize`가 필수 필드라 스테이지마다 하나가 필요하다. 기존 `finalizeIngest`·`finalizeMatching` 관행을 그대로 따랐다(revalidate만).
  - **LLM 실패 처리**: 429/503/529는 `retryable: true` 실패로 반환해 풀의 백오프 재시도에 맡기고, 그 외 오류는 **파일명 폴백**으로 살린 뒤 메시지에 실패 사유를 덧붙인다(지시서의 "LLM 실패 시 파일명 폴백"과 "429 retryable"을 둘 다 만족시키는 절충). 식별 실패는 오류가 아니므로 `ok: true`(매칭 관행과 동일 — 서킷 브레이커 오발 방지).
  - **eslint 신규 규칙 대응**: 처음 작성한 검토 패널이 `react-hooks/set-state-in-effect`·`react-hooks/refs`에 걸렸다(effect 본문에서 동기 setState / 렌더 중 ref 쓰기). 조회 경로를 effect 하나로 단일화하고(`reloadNonce` 트리거 + cancelled 가드) 기존 `book-select-modal.tsx`의 프라미스 체인 관용구를 따라 해소했다. `onCountChange`는 effect 의존성이므로 **참조가 안정된 함수**여야 한다는 주석을 prop에 달았다(현재 소비자는 useState setter).
  - **다인용 PDF 경로 무변경 확인**: `ingestMultiPagePdf`는 로직을 건드리지 않았다. 모호 페이지는 위 ①에 따라 발견 대상에 포함된다.
  - **다음 배치(3) 인계**: 작업결과표는 여전히 `student_id NOT NULL`만 조회하므로(대시보드 `page.tsx`의 wsSubs), 발견·승인 전 제출물은 표에 보이지 않는다 — 배치 3의 미귀속 배너(`unassignedCount`)가 이 구멍을 메운다. 미귀속 카운트를 셀 때 발견 스테이지가 raw를 채워도 `match_status`는 그대로라는 점(unmatched/pending_confirm 유지)을 전제로 하면 된다. `components/projects/dashboard/discovery-review.tsx`는 배치 3의 [학생 이동] UI와 겹치지 않는다(검토=신규 학생 생성, 이동=기존 학생 재귀속).

#### 사용자 확인 필요 (에이전트 미수행 — OAuth·실 LLM 과금 필요)

1. **명단 빈 프로젝트 부트스트랩**: 학생 A만 있는 파일 + B만 있는 파일 + C 섞인 파일을 업로드 → [수합 & 매칭] → 터미널에 **수합 → "── 다음 단계 ──" → 발견 → "── 다음 단계 ──" → 매칭** 3연쇄가 흐르는지.
2. **발견 로그**: 건별로 `→ 학번 10203·이름 홍길동 (LLM)` / `(파일명)` / `→ 식별 실패`가 찍히는지. 문서에 없는 값을 LLM이 지어내면 저장되지 않아야 한다(실존 대조).
3. **검토·승인**: 실행 종료 후 "학생 명단 검토 (N)" 표에 A·B·C 합집합이 뜨고, [선택 학생을 명단에 추가하고 재매칭] 한 번으로 "학생 N명 추가 · 제출물 M건 귀속" 피드백 + 작업결과표에 행이 생기는지.
4. **이름 충돌**: 같은 학번에 다른 이름이 섞인 경우 라디오로 이름을 고를 수 있는지.
5. **중단·증분**: 발견 스테이지 진행 중 일시정지·재개·긴급 중단이 동작하고, 재실행 시 이미 식별값이 있는 건은 `→ 이미 식별값 있음`으로 건너뛰는지.
6. **자동 생성 불확대**: 검토 표에서 아무도 선택하지 않으면 학생이 **하나도** 생기지 않는지(스프레드시트 열 학번의 기존 자동 생성만 유지).

### 2026-08-20 / 배치 3 / 완료

- **결과**: 작업결과표를 매칭 확인·수정의 허브로 만들었다(P-2). **매칭 규칙·스키마 변경 없음** — 기존 `reassignSubmission`을 재사용하고 조회·표시·피드백만 손봤다.
  - **미귀속 배너**: `lib/worksheet/unassigned.ts` 신설(`unassignedTotal`·`unassignedSummary`, 음수·NaN 방어). 대시보드 `page.tsx`가 미귀속 2쿼리(head count)를 추가해 `WorksheetTable`에 `unassignedCount={{ unmatched, pending }}` 전달(공유 계약). 배너는 총합 > 0일 때만 뜨고 [명단 검토](#phase-1 스크롤)·[확인 대기 큐](/submissions) 동선을 준다.
  - **카운트 재조회**: `fetchUnassignedCount(projectId)` 액션 신설. `WORKSHEET_REFRESH_EVENT` 수신 시 `reload()`가 행과 카운트를 `Promise.all`로 함께 읽는다.
  - **매칭 배지**: `WorksheetSubmission`에 `matchMethod`·`identitySource` 추가(8열 키 계약 불변 — 행 타입 확장 관행). `assemble.ts`의 `SubmissionRaw`는 **선택 필드**로 받아 미전달 호출부가 그대로 동작한다. 제출물 펼침 헤더에 귀속 경로 배지(학번 자동·이름 자동·신규 학번 자동·교사 확정·수동 추가·교사 이동)와 출처 배지(열·파일명·**LLM 추정은 앰버 강조**)를 표시.
  - **[학생 이동]**: 펼침 헤더의 학생 select → `reassignSubmission` → `emitWorksheetRefresh()` + `reload()` + "○○ 학생으로 이동됨 ✓" 피드백. 학생 목록은 이미 조회한 `rows`에서 조립(추가 조회 없음).
  - **피드백 정합**: `attributeExisting`·`attributeNew`·`reassignSubmission`·`acceptPendingContent`·`rejectPendingContent`·`deleteSubmission`·`updateSubmissionText` 7개 액션에 **대시보드 경로 revalidate 추가**. 확인 큐는 확정 시 "→ ○○ 학생에게 귀속됨 ✓"를 1.5초 보여 준 뒤 행을 거두고(기존 `hidden` 상태 재사용) `emitWorksheetRefresh()`도 함께 호출. 제출물 상세의 평가·생기부 반영 체크박스에 "저장됨"(실패 시 "저장 실패") 1.5초 표기 — **액션은 불변, UI만**.
- **검증 출력 요약**:
  - `npm test`: **254/254 통과**(신규 `tests/worksheet-unassigned.test.ts` 8건 — 지시서 요구 ≥5건 초과).
  - `npx tsc --noEmit` 무오류 · `npx next build` 성공 · `npx eslint` **기존 14 problems 그대로(신규 0건)**.
- **특이사항**:
  - **`fetchWorksheetRows`의 반환 형태를 바꾸지 않았다**: 지시서 1.2는 "fetch 액션 반환을 확장(반환 타입에 카운트 필드 추가)"과 "하위 호환 유지"를 동시에 요구했는데, 현재 반환은 `WorksheetRow[]` 배열이라 필드를 더하려면 형태를 깨야 한다(모순). 소비자가 `worksheet-table.tsx` 한 곳뿐이라 형태 변경도 가능했지만, **배열 계약을 지키는 쪽**을 골라 카운트만 별도 액션(`fetchUnassignedCount`)으로 분리하고 갱신 시 나란히 호출했다. 요구된 동작(갱신 시 카운트도 재조회)은 그대로 충족한다.
  - **미귀속 정의를 `student_id IS NULL` 기준으로 잡았다**: 배너 문구가 "표에 보이지 않습니다"이므로, 카운트가 실제로 표에서 빠진 행 수와 정확히 일치해야 한다. 작업결과표는 `student_id NOT NULL`만 조립하므로 `unmatched = student_id IS NULL AND status='unmatched'`, `pending = student_id IS NULL AND status IN ('pending_confirm','update_pending')`으로 정의했다(기존 페이즈 1 배지의 `pendingCount`는 귀속 여부와 무관한 별개 카운트라 그대로 두었다 — 두 숫자가 다를 수 있다).
  - **매칭 배지 문구의 출처**: 지시서는 "submissions 화면의 기존 배지 문구 관행 재사용"이라 했으나, `student-submissions.tsx`는 `match_method`·`identity_source`를 **타입으로만 들고 있고 화면에 렌더하지 않는다**(엑셀 그리드 개편 때 사라진 것으로 보임 — 커밋 `8a6f863`). 그래서 서버가 실행 터미널에 찍는 어휘(`applyClassification`의 "학번 자동"·"이름 자동"·"신규 학생 자동 생성", `sourceLabel`의 "열"·"파일명"·"LLM 추정")를 배지 문구의 기준으로 삼았다.
  - **[학생 이동] 완료 문구의 위치**: 처음엔 서브행 헤더 안에 뒀는데, 이동에 성공하면 그 제출물이 이 학생의 열에서 빠져 **헤더 자체가 사라지므로 문구가 보이지 않는다**. 표 위 상시 영역으로 옮기고 4초 후 자동 소거하도록 고쳤다(타이머는 언마운트 시 정리).
  - **prop → state 동기화는 갱신 이벤트에 맡긴다**: `unassignedCount` prop은 초기값으로만 쓰고, 이후에는 `WORKSHEET_REFRESH_EVENT` → `fetchUnassignedCount`로 갱신한다. 같은 화면에서 `router.refresh()`만 도는 경로(예: 발견 검토 승인)도 `emitWorksheetRefresh()`를 함께 호출하므로 카운트가 따라온다. prop 변화를 effect로 state에 되쓰면 `react-hooks/set-state-in-effect` 위반이라 택하지 않았다.
  - **범위 밖으로 남긴 것**: `addManualSubmission`·`deleteSubmissionsByFile`도 작업결과표 내용을 바꾸지만 지시서 3.1의 7개 목록에 없어 revalidate를 넣지 않았다. 같은 이유로 대시보드가 잠시 구 데이터를 보일 수 있다 — 배치 7에서 정리 대상 후보.
  - **다음 배치(4) 인계**: 제출물 펼침 헤더는 이제 배지 + [학생 이동]을 담는 자리다. 배치 4의 기준별 채점 결과 표시·수정 UI는 **셀(`Cell`의 `sub_detail_` 분기, 현재 `TextCell`로 본문만 표시)** 쪽에 붙이는 것이 자연스럽다(헤더는 이미 밀도가 높다). `WorksheetSubmission` 확장 방식(선택 필드 + assemble 매핑 + 두 조회부의 select 추가)은 이 배치가 밟은 그대로 따르면 된다 — 조회부는 `app/projects/[id]/page.tsx`와 `app/projects/[id]/worksheet-actions.ts` **두 곳**이다(둘 다 고쳐야 새 필드가 초기 렌더·갱신 양쪽에서 산다).

#### 사용자 확인 필요 (에이전트 미수행 — OAuth 로그인 필요)

1. **배너**: 미귀속 제출물이 있는 프로젝트에서 작업결과표 위에 "⚠ 아직 학생에게 귀속되지 않은 제출물 N건 (미매칭 a건 · 확인 대기 b건)"이 뜨고, [명단 검토]가 페이즈 1로 스크롤되는지 · [확인 대기 큐]가 이동하는지. 전부 귀속되면 배너가 사라지는지.
2. **배지**: 제출물 갯수 셀을 눌러 펼치면 헤더에 귀속 경로·출처 배지가 뜨는지, LLM 유래가 앰버로 구분되는지.
3. **[학생 이동]**: 잘못 귀속된 제출물의 헤더 select에서 다른 학생을 고르면 표가 즉시 갱신되고 "○○ 학생으로 이동됨 ✓"이 표 위에 보이는지(4초 후 소거).
4. **확인 큐 확정**: [선택 학생으로 확정] 후 "→ ○○ 학생에게 귀속됨 ✓"이 잠깐 보인 뒤 행이 사라지고, 대시보드로 돌아갔을 때 작업결과표가 **최신**인지(revalidate 확인).
5. **반영 체크박스**: 평가·생기부 체크를 켜고 끌 때 "저장됨"이 잠깐 보이는지.

### 2026-08-20 / 배치 4 / 완료

- **결과**: 채점 결과를 작업결과표에서 보고 고칠 수 있게 했다(P-3) + 목표 등급 → 보정 점수 헬퍼(P-4). **INV-6 불변** — 등급 입력 경로는 만들지 않았다.
  - **마이그레이션 0014**: `evaluations.origin`(not null default 'llm', check in ('llm','teacher')) + `model`·`raw_llm_output` nullable 완화 + `evaluations_llm_fields` check(LLM 행은 여전히 원문·모델 필수). **RLS 정책 무변경** — 쓰기는 그대로 service role 전용.
  - **서버 액션**: `saveEvaluationEdit`(루브릭 실존·범위 검증 → `origin='teacher'` 새 행 insert → 감사 로그 `evaluation.teacher_edit` → `recomputeAndSave`로 즉시 재파생) · `rescoreOne`(증분 무시 강제 AI 재채점 → 재계산). `evaluateOne`은 공용 코어 `scoreSubmissionByAI(…, force)`에 위임만 하고 **시그니처·동작·프롬프트 전부 불변**.
  - **순수 함수**: `overrideRangeForGrade`(lib/grading.ts, 이진 탐색 — 등급이 점수에 단조라는 성질 이용) · `validateTeacherScores`(lib/scoring.ts, **클램프가 아니라 거부**).
  - **UI**: `WorksheetSubmission.evaluation` 확장(8열 키 계약 불변) → 제출물 펼침 **셀 하단**에 기준별 `이름 점수/만점` 목록(근거는 hover title + 💬 표식)·합계·origin 배지(AI/교사 수정)·[점수 수정] 인라인 편집·[AI 재채점], 미채점은 "미채점 — [채점 실행]" 앵커. `ScoreCell`에 목표 등급 select(구간 계산 후 min 자동 입력, 불가하면 "현재 인원 기준 불가"). Phase2Panel 안내에 확인·수정 위치 문구 추가.
- **검증 출력 요약**:
  - `npm test`: **274/274 통과**(신규 `tests/override-grade.test.ts` 10건 + `tests/evaluation-edit.test.ts` 10건 — 지시서 요구 ≥8/≥3/≥3 전부 초과).
  - `npx tsc --noEmit` 무오류 · `npx next build` 성공 · `npx eslint` **기존 14 problems 그대로(신규 0건)**.
  - **0014 원격 적용 + pg 검증**: `origin` not null·default `'llm'`·check 2종(`evaluations_origin_check`·`evaluations_llm_fields`) 존재, `model`·`raw_llm_output` nullable YES, **RLS 정책은 `evaluations_select`(SELECT) 하나뿐**(쓰기 정책 없음 = 변경 없음), 기존 **187행 전부 `origin='llm'`**, `origin='llm'`인데 model/raw가 빈 행 **0건**.
  - **INV-6 코드 리딩 확인**: `student_scores` 쓰기는 `recomputeAndSave`의 admin(service role) `insert`/`delete` 한 곳뿐이고 `grade` 값은 `computeStandings` 파생값(`standings[i].grade`)이다. 작업결과표의 등급 셀은 편집 컨트롤 없는 `<span>{row.grade}등급</span>`(worksheet-table.tsx `case "grade"`). 이번 배치가 추가한 경로는 **점수만** 쓴다(evaluations.scores) — 등급 저장·수정 경로 없음.
- **특이사항**:
  - **마이그레이션 이력 드리프트(이번 배치가 만든 것이 아님)**: `db push`가 0010부터 다시 적용하려다 `column "models" of relation "api_keys" already exists`로 실패했다. 원격 `supabase_migrations.schema_migrations`에는 **0001~0009만 기록**돼 있었지만 실제 스키마에는 0010~0013이 전부 적용돼 있었다(PROGRESS의 "0010~0013 적용 완료" 기록과 일치). pg로 0010(`api_keys.models`·`models_synced_at`)·0011(`submissions.identity_source`)·0012(`student_scores.display_score`)·0013(`factsheets`·`factsheet_entries`·`submissions.authenticity_status`) 객체 실존을 **먼저 전수 확인한 뒤**, `supabase migration repair --status applied 0010 0011 0012 0013`으로 **기록만** 정정하고(스키마·데이터 무변경) 0014를 적용했다. 이제 로컬 14개 = 원격 14개로 일치한다.
  - **1인 모집단은 최하 등급**(테스트 중 발견): `deriveGrade`는 백분위 = rank/total×100이라 학생이 1명뿐이면 100% → **최하 등급**이 된다(5등급제에서 5등급). 상대평가의 정의상 자연스러운 결과이고 `computeStandings`·`GRADE_BOUNDARIES`는 불가침이라 손대지 않았다. 결과적으로 **소인원 프로젝트에서는 목표 등급 대부분이 "현재 인원 기준 불가"로 표시된다** — 헬퍼가 현실을 정확히 반영한 것이며, 테스트(`다른 학생이 없으면 …`)로 이 의미를 고정해 두었다.
  - **`evaluateOne` 분리 방식**: 지시서 2.2의 "최소 중복으로 분리"를 따라 본문을 `scoreSubmissionByAI(projectId, submissionId, force)`로 옮기고 `evaluateOne`은 `force=false`로 위임만 한다. 증분 스킵 가드에 `!force` 조건 하나만 더했고 채점 프롬프트(`buildEvalPrompt`)는 한 글자도 바꾸지 않았다. 강제 재채점이 교사 수정본을 대체하면 메시지에 "교사 수정본을 AI 채점으로 대체함"을 덧붙인다.
  - **증분 판정 확인 결과(지시서 2.3)**: `evaluateOne`의 스킵 조건은 `current.content_hash === sub.content_hash`다. 교사 수정 행도 **제출물의 현재 해시**를 그대로 갖게 저장하므로 일괄 [채점 실행]이 그 건을 건너뛴다 → **교사 수정이 AI에 덮이지 않는다**. 반대로 제출물 내용이 바뀌면 해시가 달라져 AI가 재채점하며 교사 수정본을 대체한다(근거가 바뀌었으므로 의도된 규칙). 이 규칙을 DATA_MODEL 9절과 [AI 재채점] 버튼 title 문구에 명문화했다.
  - **기준 목록은 루브릭 순서로 조립**: `assembleWorksheetRows`가 evaluation의 scores를 그대로 쓰지 않고 **루브릭 기준 순서**로 다시 세운다(빠진 기준은 0점, 루브릭에서 사라진 기준은 목록에서 제외). 편집기가 보내는 payload가 항상 루브릭과 1:1이어야 `validateTeacherScores`(전 기준 필수·미지 기준 거부)를 통과하기 때문이다. 합계는 조립값이 아니라 **저장된 `total_score`**를 표시한다 — 루브릭이 바뀐 뒤라면 둘이 다를 수 있고, 그 불일치는 감추지 않는 편이 정직하다.
  - **교사 입력은 거부가 원칙**: AI 응답을 다루는 `parseEvalScores`는 관대하게 클램프하지만, 교사 입력은 클램프하면 "저장한 값과 화면 값이 다른" 상태가 된다. `validateTeacherScores`는 범위 초과·소수·미지 기준·중복·누락을 전부 **에러로 되돌린다**.
  - **평가 조회는 owner 세션으로 충분**: `evaluations`에는 `evaluations_select`(소유자 select) 정책이 있어 작업결과표 조회에 admin 클라이언트가 필요 없다. 쓰기만 service role이다 — 정책은 그대로 두었다.
  - **`gradingScheme`·`tieBreak`는 선택 prop**: 미전달이면 목표 등급 UI를 숨긴다(다른 소비자가 생겨도 깨지지 않게).
  - **다음 배치(5) 인계**: 마이그레이션 번호는 예정대로 `0015_prompt_profile_brief.sql`이고, **이력 드리프트는 이 배치에서 해소됐으므로 `db push`가 0015만 적용한다**(다시 repair할 필요 없음). 새 서브행 필드를 더할 때 조회부가 `page.tsx`·`worksheet-actions.ts` 두 곳이라는 점은 배치 3·4에서 그대로 반복됐다.

#### 사용자 확인 필요 (에이전트 미수행 — OAuth·실 LLM 과금 필요)

1. **기준별 결과 표시**: 작업결과표에서 제출물 갯수 셀을 눌러 펼치면 각 셀 하단에 `기준명 점수/만점` 목록·합계·AI 배지가 보이는지. 근거 인용이 있는 항목에 💬가 뜨고 hover로 원문이 보이는지.
2. **교사 수정 → 즉시 재파생**: [점수 수정] → 값 변경 → [저장] → "저장됨 · 재계산 완료(N명)" 피드백 + 표의 반영 점수·등급이 갱신되는지. 배지가 "교사 수정"으로 바뀌는지.
3. **거부 동작**: 만점을 넘는 값을 넣고 저장하면 클램프되지 않고 `'기준명' 점수는 0~N 범위여야 합니다`로 거부되는지.
4. **덮어쓰기 규칙**: 교사 수정 후 [채점 실행]을 다시 돌려도 그 건이 "이미 채점됨(증분)"으로 보존되는지 / [AI 재채점]을 누르면 AI 점수로 대체되며 "교사 수정본을 AI 채점으로 대체함" 문구가 뜨는지.
5. **목표 등급 헬퍼**: 반영 점수 셀 → 편집 → "목표 등급으로 점수 계산…"에서 등급 선택 → "N등급이 되는 보정 점수: min~max"가 뜨고 입력란에 min이 채워지는지. 소인원이면 "현재 인원 기준 불가"가 뜨는지. **등급을 직접 입력하는 칸은 어디에도 없어야 한다**(INV-6).
6. **감사 로그**: 교사 수정 후 `audit_logs`에 `evaluation.teacher_edit`(before/after total·변경 기준 수)이 남는지 — 점수 원문·인용은 들어가지 않는다.

### 2026-08-21 / 배치 5 / 완료

- **결과**: 생기부 작성 브리프(markdown 자유 서술)를 프롬프트 프로필에 얹었다(P-7). 데이터(0015) → 병합 → 프롬프트 주입 → 웹 편집·MD 왕복·버전 이력 → 최종 프롬프트 미리보기까지 한 줄로 이어진다. **AI 협업 작성은 배치 6.**
  - **마이그레이션 0015**: `prompt_profiles.brief_md`·`prompt_profile_versions.brief_md`(둘 다 `text not null default ''`). RLS 불변 — 행 단위 owner-only 정책이 컬럼을 자동 포함.
  - **병합(`mergeProfileLayers`)**: `MergedProfile.brief` 추가 — **오버라이드가 공백 제거 후 비어 있지 않으면 그것만, 아니면 계정 기본**(목록처럼 이어 붙이지 않는다 — 활동 맥락은 프로젝트마다 달라 겹쳐 읽히면 안 됨). 입력 타입 `ProfileLayerInput`의 `brief_md`는 선택 필드라 기존 호출부·픽스처 호환.
  - **주입(`generation.ts`)**: `buildGenerationMessages`·`buildSentenceRegenMessages` user 프롬프트에 `[활동·작성 관점 브리프]` 섹션(참고사항 **앞**, 빈 문자열이면 섹션 생략). system에 우선순위 문구 "브리프는 … 근거 요건보다 우선하지 않는다 — 근거 자료에 없으면 서술하지 않는다"(브리프 있을 때만 — 가드 명문화).
  - **MD 왕복(`profile-markdown.ts`)**: `## 활동·작성 브리프` 섹션을 **문서 마지막**에 렌더(본문 속 `###` 하위 헤더가 뒤 섹션을 오염시킬 수 없는 위치). parse는 정확히 2단계(`##`) 헤더만 섹션 전환으로 보고, 브리프 구간은 목록·헤더·빈 줄 전부 원문 보존. `(없음)` ↔ 빈 문자열 왕복. 구 MD(브리프 섹션 없음)는 `brief: ""` 하위 호환.
  - **저장 경로(records/actions.ts)**: `saveProfileLayer`에 `briefMd` 파라미터 — 항목과 브리프가 **한 버전**으로 저장된다(부분 저장 없음). `saveProfileItems`(+brief) · `applyProfileSuggestions`(브리프 현재 값 보존) · `importProfileFromMarkdown`(브리프 포함 파싱·brief-only MD 허용) · `listProfileVersions`/`restoreProfileVersion`(브리프 왕복) 전부 확장. 버전 source 체계('edit'/'import'/'restore'/'ingest')는 재사용(신규 값 없음).
  - **UI**: ProfileEditor에 **브리프 패널**(참고/금지 2패널 위) — textarea(P-9, 렌더러 없음)·글자수·[저장(새 버전)]·[MD 파일 업로드](편집기 채움만 — 저장은 명시적). MD 내보내기·가져오기 미리보기·버전 이력 펼침에 브리프 포함. `previewGenerationPrompt(projectId)` 신설(조회만 — 쓰기·LLM 없음, 플레이스홀더 학생 표기) + 공용 `PromptPreviewButton`(zinc/neo variant)을 ProfileEditor·Phase3Panel에 장착.
- **검증 출력 요약**:
  - `npm test`: **285/285 통과**(신규 `tests/profile-brief.test.ts` **11건** — 지시서 요구 ≥8건 초과). 기존 record-context·prompt-profile·profile-markdown 테스트 전건 통과.
  - `npx tsc --noEmit` 무오류 · `npx next build` 성공 · `npx eslint` **기존 14 problems 그대로(신규 0건)**.
  - **0015 원격 적용 + pg 검증**: 두 테이블 `brief_md` 존재(`text NOT NULL default ''`), RLS 정책 목록 불변(prompt_profiles 4정책·ppv 2정책), 기존 행 전부 `''`. 이력 드리프트 없이 0015만 적용됨(배치 4의 repair가 유효).
- **특이사항**:
  - **파서 섹션 규칙 개정(하위 호환 유지)**: 기존 `sectionOf`는 `###`도 `##`로 취급해 섹션을 끊었다. 브리프 본문의 하위 헤더가 왕복을 깨므로 **정확히 2단계 헤더만** 전환으로 보게 고쳤다(`h2Section`). 참고/금지 구간의 동작은 실질 불변(목록 파싱은 헤더와 무관)이고 기존 테스트 전건 통과로 확인. **알려진 한계**: 브리프 본문에 `## …참고…`/`## …금지…` 2단계 헤더가 있으면 섹션 전환으로 읽힌다 — 내보내기가 브리프를 마지막에 두므로 정상 왕복에서는 발생하지 않으며, 코드 주석·DATA_MODEL에 명문화했다.
  - **브리프 저장은 부분 저장이 아니다**: 브리프 패널의 [저장]도 `saveProfileItems`(항목+브리프 전체)를 부른다. 별도 브리프 전용 액션을 두면 항목 편집 중 브리프만 저장할 때 편집 중이던 항목이 유실되는 경쟁이 생긴다 — 한 버전 = 한 스냅샷 원칙(버전 이력의 의미 보존).
  - **import 빈 검사 완화**: 기존 "항목 0개면 거부"에 브리프를 더해 **항목 0 + 브리프 공백일 때만** 거부한다. 브리프만 담은 MD 업로드(사용자 보고 7번의 "MD 파일 업로드" 시나리오)가 유효한 입력이 됐다. 단 이 경로는 항목을 비우므로 UI 미리보기가 참고/금지 0개를 함께 보여 준다(교사 확인 후 반영).
  - **미리보기의 INV-2 정합**: `previewGenerationPrompt`는 서버가 prompt_profiles·projects에서 조회한 것만 조립하고 클라이언트 텍스트를 받지 않는다. 학생 자리는 고정 플레이스홀더 문구(가짜 이름 없음). 반환은 문자열 2개뿐 — 쓰기·LLM 호출 없음.
  - **`ensureDefaultProfile` 무수정**: 시드는 brief_md 기본값('')에 맡겼다 — 시드 프로필에 브리프를 넣을 이유가 없다(계정마다 활동이 다르다).
  - **다음 배치(6) 인계**: 공유 계약대로 `draftBrief(projectId, input)`·`refineBrief(projectId, currentMd, request)`를 records/actions.ts에 추가한다(둘 다 DB 쓰기 없음 — AI 출력은 BriefPanel의 textarea를 채울 뿐). BriefPanel은 `onChange`로 외부에서 값을 밀어 넣을 수 있는 제어 컴포넌트라 그대로 재사용 가능. 평가계획서류 파일 텍스트 추출은 기존 `extractExampleText`(추출만, 쓰기 없음) 관행을 참고.

#### 사용자 확인 필요 (에이전트 미수행 — OAuth 로그인 필요, LLM 과금 없음)

1. **브리프 편집·저장**: `/projects/[id]/profile`에서 프로젝트 오버라이드 계층 선택 → 브리프 textarea에 markdown 작성 → [저장(새 버전)] → 버전이 +1 되고 이력에 남는지.
2. **프롬프트 주입 확인**: [최종 프롬프트 미리보기] → user 프롬프트에 `[활동·작성 관점 브리프]` 섹션이 참고사항 앞에 보이고, system에 근거 요건 우선 문구가 보이는지. 브리프를 비우고 저장하면 섹션이 사라지는지.
3. **계층 병합**: 계정 기본에만 브리프를 두면 그것이 보이고, 프로젝트 오버라이드에 쓰면 오버라이드만 보이는지(이어 붙지 않음).
4. **MD 왕복**: [MD 내보내기] → 파일에 `## 활동·작성 브리프` 섹션 포함 → 편집 후 MD 가져오기 미리보기에 브리프가 보이고 반영되는지. 브리프 패널의 [MD 파일 업로드]는 편집기만 채우고 저장 전까지 반영되지 않는지.
5. **버전 복원**: 브리프가 있던 과거 버전으로 복원하면 브리프도 함께 돌아오는지.
6. **생성 반영**: 브리프 저장 후 생기부를 생성하면 서술 관점이 바뀌되, 근거 없는 내용은 여전히 검증 패스에서 하이라이트되는지(가드 불변).

### 2026-08-21 / 배치 6 / 완료

- **결과**: 브리프를 AI와 함께 쓰는 경로를 붙였다(P-8). **DB 쓰기 없는 제안 전용** — AI 출력은 편집기를 채울 뿐이고 저장은 언제나 교사의 [저장(새 버전)](배치 5 경로)이다.
  - **프롬프트(`lib/prompts/brief.ts` 신설)**: `buildBriefDraftMessages`(폼 5입력 → 4섹션 구조 지시: 활동 개요 / 강조 포인트 / 찾아야 할 에피소드 유형 / 서술 지침) · `buildBriefRefineMessages`(현재 브리프 + 요청 → **전체 MD** 반환 지시, 부분 diff 아님). 두 프롬프트 공통 system에 자기 제약 "근거 자료에 없는 내용을 강조하도록 지시하지 말 것" — 배치 5의 생성 프롬프트 우선순위 문구와 짝을 이룬다. 부수적으로 `stripCodeFence`(모델이 감싸는 ```` ``` ```` 제거 — 펜스가 남으면 브리프 원문째 프롬프트에 실린다).
  - **서버 액션(records/actions.ts)**: `draftBrief(projectId, input)` · `refineBrief(projectId, currentMd, request)` — 둘 다 `requireProjectOwner` → `callLLM(purpose='생성')` → MD 반환, **저장·revalidate 없음**. 재료가 하나도 없으면 호출 전에 거부(모델이 활동을 통째로 지어내는 것 방지). 첨부 파일은 `extractBriefSourceText`가 서버에서 기존 `extractTextFromExampleFile`로 추출(지원 형식·에러 문구 재사용).
  - **UI(profile-editor.tsx)**: BriefPanel에 [AI로 초안 만들기]·[AI로 다듬기] → `BriefAiModal`. 초안은 활동명/설명/강조 포인트(플레이스홀더에 JAYUL 예시)/자유 보충/(선택) 파일 첨부 폼, 다듬기는 요청 한 줄 + **현재/제안 나란히** 비교. 결과는 미리보기로만 보여 주고 [편집기에 반영]이 편집기 내용만 교체한다(모달·버튼 문구에 "저장되지 않습니다" 명시). 실행 중 비활성·에러 인라인 300자 절단.
- **검증 출력 요약**:
  - `npm test`: **292/292 통과**(신규 `tests/brief-prompt.test.ts` **7건** — 지시서 요구 ≥4건 초과).
  - `npx tsc --noEmit` 무오류 · `npx next build` 성공 · `npx eslint` **기존 14 problems 그대로(신규 0건)**.
  - **무자동반영 코드 리딩 확인(완료 기준)**: `draftBrief`·`refineBrief` 본문에 `.from(`·insert/update/upsert/delete·`saveProfileLayer`·`revalidatePath`가 **0건**(grep 확인). `saveProfileLayer` 호출자는 여전히 `saveProfileItems`·`applyProfileSuggestions`·`importProfileFromMarkdown`·`restoreProfileVersion` 넷뿐이고 전부 교사 명시 액션이다. UI에서도 모달의 `onApply`는 `onChange(md)`만 호출하며, 저장은 [저장(새 버전)] → `saveProfileItems` 한 경로뿐이다.
- **특이사항**:
  - **`stripCodeFence`는 지시서에 없던 추가**: 모델이 Markdown 요청에 습관적으로 코드 펜스를 두르는데, 그대로 두면 브리프 본문에 ```` ``` ````가 섞여 생성 프롬프트까지 오염된다. 바깥 펜스만 벗기고 **본문 중간의 코드 블록은 보존**한다(테스트로 고정).
  - **`extractBriefSourceText` 신설(공유 계약에 없던 이름)**: 지시서 2단계는 "기존 `extractTextFromExampleFile` 재사용으로 서버에서 추출"을 요구했으나, 기존 서버 액션 `extractExampleText`는 예시 인제스트 전용 이름이라 브리프 첨부에 그대로 쓰면 의미가 어긋난다. 같은 추출 함수를 부르는 얇은 래퍼를 별도 이름으로 두었다(지원 형식·에러 문구·8MB 상한 전부 공유).
  - **재료 없는 초안 호출 거부**: 폼 5개가 전부 비면 `draftBrief`가 LLM을 부르기 전에 에러를 던진다. 빈 입력으로 부르면 모델이 활동 자체를 창작하는데, 그 결과가 편집기에 들어가면 교사가 "AI가 준 사실"로 오인할 여지가 생긴다(가드 원칙의 입력 측면).
  - **다듬기는 전문 반환**: 부분 diff·조각 병합을 쓰지 않는다. 조각을 클라이언트에서 합치면 또 다른 오적용 경로가 생기고, 교사가 최종 형태를 통째로 보고 판단하는 편이 무자동반영 원칙과 정합적이다. 대신 현재/제안을 나란히 보여 차이를 눈으로 비교하게 했다.
  - **다음 배치(7) 인계**: 남은 일은 SPEC/DATA_MODEL 개정 + 기존 `any` 11건 정리 + 최종 검증이다. **eslint 기존 14 problems(11 errors·3 warnings)의 정확한 목록**은 `app/factsheets/actions.ts`(2)·`app/projects/[id]/submissions/page.tsx`(1 error+1 warning)·`app/projects/[id]/worksheet-actions.ts`(1)·`components/projects/ingest/column-mapper.tsx`(1)·`components/projects/submissions/confirm-queue.tsx`(1)·`components/projects/submissions/student-submissions.tsx`(2 any + 2 unescaped-entities)·`lib/worksheet/assemble.ts`(1)·`components/projects/worksheet/worksheet-table.tsx`(unused AuthenticityBadge)·`tests/run-pool.test.ts`(unused `_r`)다. SPEC 개정 대상은 7.5절(브리프·AI 협업)·5.2절(발견 스테이지)·6절(교사 채점 수정)·8절(작업결과표 허브)이다.

#### 사용자 확인 필요 (에이전트 미수행 — OAuth 로그인 + LLM 과금 필요)

1. **JAYUL 시나리오**: `/projects/[id]/profile` → 프로젝트 오버라이드 → [AI로 초안 만들기] → 활동명 "자율 동아리 JAYUL", 강조 포인트에 "협업 과정, 친구 결과물에 대한 판단과 피드백, 상호 발전, 희생·봉사 정신" → [초안 생성] → 4개 섹션(활동 개요/강조 포인트/찾아야 할 에피소드 유형/서술 지침)으로 구조화된 MD가 미리보기에 뜨는지.
2. **무자동반영**: [편집기에 반영]을 눌러도 **저장되지 않고**(버전 그대로), [저장(새 버전)]을 눌러야 버전이 오르는지. 모달을 그냥 닫으면 편집기가 그대로인지.
3. **다듬기**: 브리프 작성 후 [AI로 다듬기] → "봉사 강조를 더 구체적으로" → 현재/제안이 나란히 보이고, 요청과 무관한 섹션은 유지되는지.
4. **첨부**: 평가계획서를 txt·docx·pdf로 첨부 → 추출 글자수 표시 → 초안에 그 맥락이 반영되는지. hwp는 안내 메시지가 뜨는지.
5. **코드 펜스**: 모델이 ```` ```markdown ````로 감싸 답해도 편집기에는 펜스 없이 본문만 들어가는지.
6. **끝단 반영**: 반영·저장한 브리프가 [최종 프롬프트 미리보기](배치 5)의 `[활동·작성 관점 브리프]` 섹션에 그대로 보이는지.

### 2026-08-21 / 배치 7 / 완료 — 리팩토링 4 종료

- **결과**: 문서 정합·코드 정리·전체 검증을 마쳤다. **새 기능 없음.**
  - **SPEC 개정(9개 지점, 각주 `(2026-08-21 리팩토링 4, 사용자 승인)`)**: 헤더에 개정 이력 / 4절 전역 내비게이션·페이즈 2 "(선택)" / 5.2절 **발견 스테이지**(명단 무관 추출·토큰 실존 대조·파일명 폴백·교사 승인 경유·자동 생성 불변·3스테이지 연쇄) / 5.4절 수정 허브 / 6절 **평가는 선택**·교사 채점 수정(origin='teacher' 이력·거부 원칙·증분 규칙)·목표 등급 헬퍼 / 7.5절 **작성 브리프**(계층 병합·프롬프트 주입과 근거 요건 우선·MD 왕복·AI 무자동반영·미리보기) / 8절 작업결과표 허브(배너·배지·이동·평가 상세).
  - **DATA_MODEL 검수·보완**: 8절에 발견 스테이지의 `raw_*` 재사용·`identity_source` 의미 확장·`match_method`에 check 제약이 없다는 사실을 명문화. 9절(0014)·12/12-1절(0015)은 배치 4·5에서 이미 갱신됨을 확인.
  - **README**: "무엇을 하는가" 기능 개요 신설(수합·평가(선택)·생기부·작업결과표), 마이그레이션 목록 0001~0015로 갱신 + 이력 드리프트 시 `migration repair` 절차 수록.
  - **코드 정리(기능 불변)**: `any` **11건 전부 제거** — 공용 `readClaimTitle(authenticity: unknown)`를 `lib/factsheet/authenticity.ts`에 신설해 3곳(assemble·confirm-queue)의 jsonb 파고들기를 한곳으로 모으고, 나머지는 정식 타입·`unknown` 좁히기로 교체(submissions/page.tsx `as any[]` 제거, worksheet-actions는 `SubmissionRaw`, factsheets 내보내기는 캐스트 삭제, column-mapper `unknown`, student-submissions 정렬 값 `string`). 미사용 import·인자 3건 제거, `react/no-unescaped-entities` 2건 수정.
  - **다인용 PDF 라벨 정합**: LLM 페이지 귀속의 `match_method`를 `st?.student_number ? "auto_number" : "auto_name"`으로 바꿨다 — 기존에는 학번 유무와 무관하게 `auto_name`이라 "이름 유일 일치"를 과장했다. 일반 매칭 경로(classifyMatch)가 같은 식별값에 대해 내렸을 값과 일치시킨 것이며, `match_method`에 check 제약이 없어 DDL이 필요 없음을 0005·0011에서 확인했다. **기존 데이터는 소급하지 않는다.**
  - **구 라우트 확인(수정 없음)**: `ingest`·`evaluate`·`results`·`students`의 `page.tsx`는 이미 redirect 전용 11~12줄(각 사유 주석 포함)이라 정리할 죽은 코드가 없었다. 액션 파일은 대시보드가 소비 중이므로 보존.
- **검증 출력 요약**:
  - `npm test` **292/292** · `npx tsc --noEmit` 무오류 · `npx next build` 성공.
  - **`npx eslint` 0 errors 0 warnings** — 리팩토링 3 이후 처음으로 완전히 깨끗해졌다(기존 14 problems 전부 해소).
  - **INV-4 번들 스캔**(.next/static) 9패턴 전부 **0건**.
  - **INV-1**: `buildStudentContext(studentId, source)` 단일 시그니처 불변, 일괄 생성은 학생별 순차 호출.
  - **INV-6**: pg로 `evaluations`·`student_scores` 정책이 **SELECT뿐**(쓰기 정책 0건) 확인, `student_scores` 쓰기는 `recomputeAndSave`의 service role 3줄뿐, 등급 입력 컨트롤 grep 0건.
  - **발견 경로 자동 생성 부재**: 학생 insert는 `createDiscoveredStudents`(교사 승인)·`applyClassification`의 column 학번·`attributeNew`(교사 확정)뿐 — 배치 2 이후 불변.
  - **마이그레이션 정합**: 원격 기록 15개 = 로컬 파일 15개(0001~0015).
  - `docs/SMOKE_TEST.md`에 **[D] 리팩토링 4 브라우저 체크리스트 12항목** 수록.
- **특이사항**:
  - **`AuthenticityBadge` 미사용 발견(정리만, 재배선 안 함)**: `worksheet-table.tsx`가 이 컴포넌트를 import만 하고 쓰지 않았다(리팩토링 2 이후 계속 경고). `WorksheetSubmission.authenticityStatus`는 조립되지만 작업결과표 어디에도 렌더되지 않는다 — 즉 **진실성 배지가 작업결과표에서는 보이지 않는다**(제출물 상세 화면에는 보인다). 배치 7은 "새 기능 금지"라 import만 제거하고 배선은 하지 않았다. **후속 과제 후보**: 작업결과표 제출물 펼침에 진실성 배지를 되살릴지 사용자 판단 필요.
  - **eslint를 실제로 통과시킨 이유**: 배치 1~6은 "기존 오류 무증가"로 대체 판정했으나 배치 종료 절차의 문자 그대로는 `npx eslint` 통과다. 배치 7이 정리 담당이므로 11건을 전부 해소해 규약을 원상 복구했다.
  - **`as any` 제거가 타입 오류를 낳지 않았다**: 3건(submissions/page·worksheet-actions·factsheets/actions) 모두 캐스트를 지우거나 정식 타입으로 바꿔도 tsc가 통과했다 — 애초에 불필요한 캐스트였다는 뜻이다(worksheet-actions만 `SubmissionRaw` 명시 필요).

## 리팩토링 4 종료 요약

| 배치 | 내용 | 마이그레이션 | 신규 테스트 |
| --- | --- | --- | --- |
| 1 | 전역 내비게이션 + IA 재편(팩트시트 1급·평가 "(선택)") | — | 6 |
| 2 | 수합 학생 발견 — 명단 부트스트랩(교사 승인 경유) | — | 22 |
| 3 | 작업결과표 허브화(미귀속 배너·매칭 배지·학생 이동) | — | 8 |
| 4 | 평가 리뷰·수정 + 목표 등급 보정 헬퍼 | 0014 | 20 |
| 5 | 생기부 작성 브리프(MD) — 데이터·병합·주입·미리보기 | 0015 | 11 |
| 6 | 브리프 AI 협업 작성(자동 반영 금지) | — | 7 |
| 7 | SPEC/DATA_MODEL 개정 + 코드 정리 + 최종 검증 | — | — |

- 테스트 **224 → 292**(+68), eslint **14 problems → 0**, 마이그레이션 0013 → 0015.
- 사용자 보고 7건 대응 완료: 합집합 명단(2) · 매칭 수정 신뢰(3) · 평가 선택화(1) · 팩트시트 독립(1) · 전 단계 AI 가드(2·6) · 채점 확인·수정(4) · 활동 유연 생기부와 MD 브리프(5·6).
- **남은 것은 브라우저 실검증뿐**(SMOKE_TEST [D] 12항목) — OAuth·LLM 과금이 필요해 에이전트가 수행하지 않았다.
