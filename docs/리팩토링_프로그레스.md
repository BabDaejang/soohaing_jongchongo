# 리팩토링 프로그레스 — 채점 전멸 수정 + 실행 관찰가능성

> 실행 지시서는 `docs/리팩토링.md`(v2 — [공통 프롬프트] + [배치 1~4 프롬프트] 구조). 이 문서는 **실제 진행의 누적 기록**이며, 배치 간 유일한 인수인계 통로다.
>
> **진행 방식**: 배치마다 **새 대화**에서 실행한다. 새 대화에는 리팩토링.md의 [공통 프롬프트] + 해당 [배치 N 프롬프트]를 붙여넣거나, 파일 접근이 되는 환경이면 "docs/리팩토링.md의 공통 프롬프트를 따르고 배치 N을 실행해"라고 지시한다. 각 배치는 검증(test/typecheck/lint/build) 통과 → 이 문서 기록 → DECISIONS 기록 → **커밋·main 푸시**까지 마치고 대화를 끝낸다.
>
> **기록 규칙**: 배치를 끝낼 때마다 ① 아래 체크리스트 갱신 ② "진행 기록" 절에 `날짜 / 배치 / 결과 / 검증 출력 요약 / 특이사항(계획과 달라진 점·발견한 문제·다음 배치 인계)`를 **추가**한다(누적만 — 기존 기록 수정·삭제 금지). 계획과 다르게 구현했으면 반드시 사유를 남기고, 설계 자체가 바뀌면 리팩토링.md도 함께 고친 뒤 그 사실을 기록한다.
>
> **배치 시작 전 확인**: 직전 배치가 체크리스트에서 완료 상태인지, 워킹 트리가 깨끗한지(`git status`) 확인. 어긋나면 작업을 멈추고 사용자에게 보고.

## 배치 체크리스트

- [x] 준비 — 문서 생성·개정 (리팩토링.md v2 + 이 문서)
- [x] **배치 1** (1~3단계) — OpenAI 어댑터 현대화 + 테스트 / -pro 제외·소비 시점 재필터 + 테스트 / Tongsa generate·verify → gpt-4o-mini (DB)
- [x] **배치 2** (4~5단계) — `useSequentialRun` + `RunTerminal` 신설 / 채점 3분할(prepare·evaluateOne·finalize) + run-panel 개편 + 모델 배지 + maxDuration
- [ ] **배치 3** (6~7단계) — 매칭 3분할(prepareMatching·matchOneByLlm·finalizeMatching) + matching-panel 개편 + 상한 제거 / records 모델 배지
- [ ] **배치 4** (8~9단계) — SPEC 5.2·6절 갱신 / INV-4 번들 스캔 포함 최종 검증 / DECISIONS 점검·PROGRESS 요약·배포 후 사용자 체크리스트

## 진행 기록

### 2026-07-11 / 준비(문서) / 완료

- plan 모드에서 사용자와 계획 합의(승인) 후 `docs/리팩토링.md`(실행 지시서)와 이 문서를 생성.
- 진단 근거는 리팩토링.md 배치 1 절에 고정: 탐침 A(400 max_tokens)·B(200)·D(400 temperature)·C(404 -pro), audit_logs `evaluation.run` 실패 158건 2회, DB 스냅샷(라우팅·키 64모델·evaluations 0행).
- 특이사항: 이후 배치는 **다른 모델이 새 대화에서** 실행하기로 사용자가 결정. DB 접속 관행(pooler 치환·.cjs 스크립트)·프로젝트 관행(테스트 명시 등록·커밋 규칙)을 지시서에 포함함.

### 2026-07-11 / 준비(문서 개정 v2) / 완료

- 사용자 피드백 반영: 지시서를 **[공통 프롬프트] + [배치별 실행 프롬프트]** 구조로 전면 개정 — 배치마다 새 대화를 시작해 맥락 오염·할루시네이션을 방지하고 하위 모델이 복붙만으로 실행 가능하게 함.
- 배치 편성(작업량·opus급 감당 수준 기준): 배치 1 = 1~3단계(소규모·상호 연관), 배치 2 = 4~5단계(최대 규모 — 터미널과 첫 소비자를 묶어 단독 검증 가능하게), 배치 3 = 6~7단계(배치 2를 참조 구현으로 반복 + 소형 배지), 배치 4 = 8~9단계(문서·검증). 단계별 커밋 대신 **배치별 커밋·푸시**로 변경 — 대화가 끊겨도 다음 배치가 깨끗한 트리에서 시작하도록.
- 이 두 문서는 아직 **커밋되지 않음** — 배치 1 실행 세션이 첫 커밋에 포함할 것.

### 2026-07-11 / 배치 1 (1~3단계) / 완료

- **1단계 — OpenAI 어댑터 현대화**: `lib/llm/adapters/openai.ts`에 순수 export 헬퍼 `isOpenAIReasoningFamily`(정규식 `^(gpt-5|o\d)`)·`buildOpenAIChatBody`(추론 계열→`max_completion_tokens`만, temperature 생략 / 그 외→`max_tokens`+temperature 유지) 추가, 어댑터 본문이 이를 사용. 빈 응답(content 공백) 시 `finish_reason` 담아 throw(전 기준 0점 무음 저장 방어). 신규 `tests/openai-params.test.ts` 4건.
- **2단계 — 선택 가능 = 호출 가능**: `lib/llm/models.ts` `OPENAI_EXCLUDE_PARTS`에 `-pro` 추가(주석: responses API 전용·404 탐침 C). `lib/llm/available.ts` `listRoutableProviders`가 openai 프로바이더 저장 목록을 소비 시점에 `isOpenAIChatModel`로 재필터(낡은 -pro 포함 목록 방어). `tests/provider-models.test.ts`에 -pro 제외 검증 1건.
- **3단계 — Tongsa 라우팅 DB 수정**: pooler 세션 모드(`aws-1-ap-northeast-2.pooler.supabase.com:5432`, user `postgres.<ref>`)+.cjs 스크립트로 generate·verify → `gpt-4o-mini`(provider_id 유지). 재조회 검증: `AFTER [Tongsa] extract=…/gpt-5.4 · evaluate=…/gpt-5.4 · generate=…/gpt-4o-mini · verify=…/gpt-4o-mini`.
- **검증 출력 요약**: `npm test` **102/102 통과**(신규 openai-params 4 + provider-models -pro 1 포함), `tsc --noEmit` OK, `eslint` OK, `next build` 성공(19라우트).
- **특이사항(문서-실제 불일치)**: 지시서 배치 1 스냅샷은 Tongsa generate·verify=`gpt-5.5-pro`(404)였으나, **실제 DB는 스냅샷 이후 사용자가 UI에서 4개 용도를 전부 `gpt-5.4`로 이미 변경**한 상태였다(extract=gpt-4o·evaluate=gpt-5.5도 아님). 공통 규칙(문서≠실제 시 실제가 기준)에 따라 사용자에게 확인 → "지시서대로 gpt-4o-mini로 변경"을 재확인받고 3단계 적용. 배치 1 어댑터 수정으로 gpt-5.4(추론 계열)도 정상 호출되므로, generate/verify를 gpt-4o-mini로 둔 것은 비용·지연 절감 목적('사용자 확정' 결정)이지 404 회피가 아니다.
- **배치 2 인계**: ① 배포 후 사용자는 계정 옵션 **[모델 갱신]** 1회로 저장된 openai 모델 목록에서 -pro를 제거해야 한다(현재 저장 64개에 -pro 포함 — available.ts 재필터가 화면에서는 가려주지만 저장 목록 자체는 갱신 전까지 남음). ② **배치 2 전까지 대량 채점 실행은 비권장** — 기존 `runEvaluation`은 여전히 전건을 한 서버 액션에서 돌리는 단일 장기 실행이라(진행 표시·중단 불가) 타임아웃 위험. evaluate 모델이 현재 gpt-5.4(추론)라 건당 지연도 큼. 배치 2에서 1건 단위 실행(prepare·evaluateOne·finalize)으로 전환 예정.

### 2026-07-11 / 배치 2 (4~5단계) / 완료

- **4단계 — 공용 실행 터미널**: 신규 `lib/hooks/use-sequential-run.ts`(`useSequentialRun` — prepare→stepOne×N→finalize 루프, pause/stop은 ref 플래그로 즉시 반영, pause 중 250ms 폴링 sleep, `maxConsecutiveFailures` 기본 3 서킷 브레이커, prepare/finalize throw는 error 라인+aborted, 스텝 로그 `${label} — ${message}`). 신규 `components/projects/run-terminal.tsx`(표시 전용 — 채점 전용 명칭 없음, 배치 3 매칭 재사용 가능. 로그 창 `bg-zinc-950` + Tailwind `resize`로 크기 조절, `[HH:MM:SS]` 프리픽스, level 색 ok/error/system/info, lines 변경 시 자동 스크롤, 헤더 진행 카운터 + running [일시정지]·[긴급중단]/paused [다시시작]·[긴급중단]).
- **5단계 — 채점 전환**: `app/projects/[id]/evaluate/actions.ts`에서 `runEvaluation`·`EvalSummary` **제거** → `prepareEvaluation`(대상·라벨·증분 보존 건수, LLM 없음)·`evaluateOne`(1건 채점, 소속·자격 재확인, 동시 탭 방어 해시 재확인, 에러를 message로 반환·throw 안 함, 300자 절단)·`finalizeEvaluation`(recomputeAndSave→audit `evaluation.run`(counts+aborted+sample_failure)→revalidate)로 3분할. `recalculate`·override·`updateModelRouting`·`recomputeAndSave`·`parseEvalScores`·`buildEvalPrompt`는 유지. `page.tsx`에 `maxDuration=120` + `model_routing`·providers 조회 → 모델 배지 prop. `run-panel.tsx`를 `useSequentialRun`+`RunTerminal`로 개편(모델 배지 "평가 모델: {provider}/{model}"+설정 링크, prelude "채점 대상 N건·증분 보존 M건", 건별 "학번 이름 · 파일명 — N점", finalize "재계산 — N명 순위 산출", 문구 "temperature를 지원하는 모델은 0으로 고정").
- **검증 출력 요약**: `npm test` **102/102 통과**(배치 2는 순수 로직 아님 — 훅·액션은 브라우저/서버 통합 경로라 기존 테스트 유지), `tsc --noEmit` OK, `eslint` OK, `next build` 성공(19라우트). `runEvaluation`/`EvalSummary` 저장소 잔존 없음(docs 제외 grep 0건).
- **특이사항(계획과 달라진 점)**: ① `evaluateOne`의 스텝 실패 message는 "실패:" 프리픽스 없이 원문 그대로 반환(터미널이 error 색으로 표시) — 지시서 run-panel 예시의 "실패: Unsupported parameter…"는 예시 표기이고, 수용 기준 1(원문 노출)에는 프리픽스 없는 원문이 더 부합. ② 대표 실패 사유(sampleFailure)는 클라이언트 `stepOne` 래퍼가 첫 실패 message를 ref에 수집해 finalize로 전달, 서버가 audit `sample_failure`(300자)에 기록. ③ 증분 보존(skipped)은 prepare 시점 content_hash 비교로 산출해 targets에서 제외, `evaluateOne`의 동시 탭 "이미 채점됨(증분)"은 ok:true로 succeeded에 합산(드묾, 로그로 구분됨).
- **배치 3 인계**: `components/projects/evaluate/run-panel.tsx`와 `evaluateOne` 3분할이 **참조 구현**. 매칭은 동일 패턴(`prepareMatching`·`matchOneByLlm`·`finalizeMatching` + `MatchingPanel` 개편 + `RunTerminal` 재사용 + extract 모델 배지 + `LLM_NOTE`/"20건까지" 안내 삭제)으로. `useSequentialRun`·`RunTerminal`은 채점 전용 명칭이 없어 그대로 재사용 가능. **브라우저 실검증(로그 흐름·일시정지·재개·중단·재실행 이어가기)은 OAuth·LLM 비용으로 에이전트가 못 함 — 사용자 확인 항목**(아래).

### 배치 2 배포 후 사용자 확인 필요(인계)

- 평가 화면 진입 → **모델 배지** "평가 모델: {provider}/{model}" 표시 확인(요구 ②).
- [채점 실행] → 실행 터미널에 prelude("채점 대상 N건·증분 보존 M건") → 건별 로그("학번 이름 — N점" 또는 실패 원문 빨강) → finalize("재계산 — N명 순위 산출") 흐름 확인(요구 ③).
- **일시정지** → 진행 중 1건 완료 후 멈춤, **다시 시작** → 이어감, **긴급 중단** → 루프 종료 + 처리분 반영, 재실행 시 증분으로 이어감(이미 채점된 건은 대상에서 빠짐).
- 오늘 같은 실패라면 터미널에 `'max_tokens' is not supported…` **원문이 보이고** 연속 3건에서 자동 중단(수용 1) — evaluate 모델을 gpt-5 계열로 두고도 배치 1 어댑터로 정상 호출되는지 겸사 확인.
- 로그 창을 마우스로 **크기 조절**(우하단 핸들, Tailwind `resize`) 확인.
